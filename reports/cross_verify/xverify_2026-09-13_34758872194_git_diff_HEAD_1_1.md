# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 34758872194

---

## ① 수치·단위·논리 오류

- **[높음] 월평균 기온에 일중 고온 임계값 35°C를 직접 연결함**
  - `T2M_MatoGrosso`는 월평균 2m 기온인데 설명은 `>35°C` 고온 스트레스를 근거로 든다. 35°C는 통상 일 최고기온 또는 시간별 노출에 적용되는 임계값으로, 월평균값과 동일하게 해석할 수 없다. 착협기 판별 변수도 없어 비생육기 월까지 같은 의미로 읽힐 수 있다.

- **[높음] 마투그로수 강수 변수의 집계 단위가 불명확하거나 잘못 표기됨**
  - Open-Meteo의 일별 `precipitation_sum`은 기본적으로 각 유효일의 일 누적 강수량이다. 그런데 `FCST_precipitation_sum_BR_MatoGrosso`는 “향후 15일 누적”으로 정의했다. 15개 일별 값을 별도로 합산하는 산출 로직은 diff에 없고, `freq`도 `일간(15일 선행)`이어서 일별 값인지 매일 산출하는 15일 누적인지 구분되지 않는다.

- **[높음] BDI z-점수를 95% 신뢰구간 및 통계적 유의성으로 해석함**
  - `|z|>2`는 정규성·독립성 등을 가정한 단일 관측치의 경험적 범위일 뿐, 시계열에서 자동으로 “통계적으로 유의”하거나 “95% CI”가 되지 않는다. BDI는 자기상관·비정규성·반복 검정 문제가 있으며, 90일 이동 평균·표준편차도 추정치다.
  - 또한 `|z|>2`에는 급락인 `z<-2`도 포함되지만 후속 설명과 조달 액션은 운임 상승만 전제한다. 90일 이동 z-점수가 연간 계절성을 “제거”한다는 주장도 성립하지 않는다.

- **[높음] `GDELT_EVENT_SCORE`의 단위와 의미를 조용히 변경함**
  - 기존 라벨은 “국제 사건 지수”였는데 이번 diff에서 “기사 수”로 바뀌었다. 동시에 구 코드 `GDELT_SBO_EVENT_COUNT`도 별도 라벨로 남아 있으나 코드 alias, 과거 데이터 migration, 산출기 변경이 보이지 않는다. 과거 `GDELT_EVENT_SCORE` 값이 있으면 점수를 건수로 오해하거나 동일 계열이 두 코드로 단절될 수 있다.
  - 5개 검색어 결과의 단순 합은 동일 기사가 여러 검색어에 걸릴 경우 중복 계수된다. 중복 제거가 명시되지 않은 상태에서 이를 “기사 수”라고 부르는 것은 부정확하다. `0~25` 캡도 실제 기사량이 25건을 넘는 구간을 모두 같은 값으로 만드는 우측 절단이다.

- **[높음] RSS 폴백 간 기사 수가 동일 측정량이라는 전제가 성립하지 않음**
  - Google News, GDELT, Bing, 원문 섹션은 색인 범위·게시시각·중복 처리·페이지 제한이 서로 다르다. 같은 indicator 코드 아래 폴백이 바뀌면 실제 뉴스 변화가 없어도 기사 수에 구조적 단절이 생긴다. 출처 변경 보정이나 계열 분리 규칙이 없다.

- **[높음] 일별 신호의 ‘신선도’와 ‘무결성’이 실제 관측 품질을 나타내지 못함**
  - `_freshness_flag`에는 관측일 `date`가 아니라 `appended_at`만 전달된다. 오래된 데이터를 오늘 재적재하면 `✅ OK`가 된다.
  - `_data_integrity_flag`에 전달하는 proxy에는 `date`가 없어 잘못된 날짜나 날짜-지표 중복을 검사할 수 없다. 숫자와 적재시각만 정상이라면 날짜범위가 `N/A`여도 무결성이 정상으로 표시될 수 있다.

- **[참고] CSV 스키마 오류가 비치명 처리되지 않음**
  - `try`는 `pd.read_csv`까지만 감싼다. `date` 컬럼이 없으면 `pd.to_datetime(raw.get("date"), ...).dropna()`에서 예외가 발생할 수 있어, 주석의 “현황 표는 비치명”이라는 동작과 다르다.

- **[참고] 전문매체 변수 식별자가 설명 및 테스트 데이터와 불일치함**
  - 카탈로그는 단일 코드 `RSS_전문매체_10종`을 정의하면서 “매체별 1행”이라고 설명한다. 반면 테스트 데이터는 `RSS_FARMDOC_DAILY`처럼 매체별 코드를 사용한다. 단일 코드를 쓰면 동일 날짜에 indicator 중복이 생기고, 매체별 코드를 쓰면 카탈로그 항목과 매칭되지 않는다.

- **[참고] 아이오와를 고정적으로 “미국 최대 대두 생산주”라고 단정함**
  - 주별 생산 순위는 작황연도마다 바뀌며 최근 여러 연도에는 일리노이가 1위인 경우가 있다. 기준 연도나 지표 없이 Iowa를 유일한 최상위 주로 표현하는 것은 재현 가능한 사실 정의가 아니다.

## ② 시계열 누수 위험

- **[높음] 뉴스 변수에 시장 마감 기준의 가용시점 컷오프가 없음**
  - GDELT는 “최근 24시간”, RSS는 “일간”만 정의되고 수집 시각·시간대·CBOT 마감 전 컷오프가 없다. 장 마감 후 수집한 기사를 같은 날짜의 CBOT 종가 예측 변수로 결합하면 명백한 look-ahead가 된다. 현재 CSV가 G1 `frames`에서 제외된다는 주석은 다른 daily brief 경로나 향후 모델 활성화 시의 정렬 문제를 해결하지 않는다.

- **[참고] 15일 예보에 forecast vintage 키가 없음**
  - 코드와 메타데이터에 `issued_at/model_run`, `valid_time`, `lead_day`가 구분되어 있지 않다. 과거 검증 시 유효일 기준으로 최신 예보를 덮어쓰거나 실현 후 갱신된 값을 조인하면 당시 이용 가능하지 않았던 정보가 들어간다. 현재 “모델 투입 금지” 문구는 즉시 누수만 막을 뿐, 누수 없는 검증 설계를 제공하지 않는다.

## ③ 상관을 인과로 과장한 해석

- **[높음] BDI 상승에서 액체 벌크 탱커 운임 상승으로 직접 인과를 연결함**
  - BDI는 건화물선 지수이고 대두유는 액체 벌크 운송시장이다. BDI는 건화물 수요뿐 아니라 선복 공급, 항만 체선, 연료비 등에도 영향을 받는다. 동기간 상관만으로 “액체 벌크 탱커 운임도 동반 상승” 및 선도계약 액션을 정당화할 수 없다.

- **[높음] 기사 수를 지정학 위험 및 해상 위협 점수로 변환하는 경로가 근거 없음**
  - 카탈로그는 GDELT 값을 “기사 수이며 주제 신호 아님”이라고 명시하지만 `_ONTOLOGY_CHAINS`는 곧바로 `지정학 위험 태그 → 해상 위협 점수`로 연결한다. 보도량은 매체 관심도·중복 색인 효과일 수 있으며 사건 발생 가능성이나 심각도의 인과 측정치가 아니다.
  - RSS 변수도 주제·감성 분석이 아직 없는데 `뉴스 감성 신호`로 연결한다. 주제 태깅만으로 감성 방향이나 강도가 산출되지는 않는다.

## ④ 재현 불가능한 주장

- **[높음] “CFR 운임 프리미엄 +5~12%, 2020~2022 검증”을 재현할 근거가 없음**
  - CFR 프리미엄의 기준가격·항로·선적 규모·통화·빈도, BDI와의 lag, 표본 수, 상관계수 및 불확실성, 사용 데이터 파일·코드가 제시되지 않았다. “2026-09-13 정정 계열”도 파일 버전이나 해시가 없어 2020~2022 검증을 동일하게 재실행할 수 없다.

- **[높음] RSS/GDELT 기사 수 산출 정의가 부족함**
  - RSS 키워드 게이트, GDELT 5개 검색어, 도메인 조건, 시간대, 중복 제거, 수정·삭제 기사 처리, 폴백 전환 조건이 없다. 검색엔진 결과는 시점에 따라 바뀌므로 검색식과 원문 ID 스냅샷 없이는 동일 날짜의 건수를 재현할 수 없다.

- **[높음] Open-Meteo `best_match`를 ECMWF IFS/AIFS 계열로 특정함**
  - `best_match`는 제공자가 위치별로 선택하는 가변 선택자이므로 명시적으로 모델을 고정하지 않는 한 ECMWF IFS/AIFS 산출물임을 보장하지 않는다. 모델명·run 시각·API 응답 메타데이터가 없으면 동일 예보를 재현할 수 없다.

- **[높음] NASA POWER 지역값의 공간 정의가 없음**
  - `MatoGrosso`, `Iowa`가 주 전체 면적가중 평균인지 특정 격자·중심점인지 명시되지 않았다. 좌표, 공간 집계법, 월 집계법, 승인 xlsx 버전이 없으므로 “마투그로수 월평균”, “아이오와 근권 토양수분” 값을 동일하게 재생성할 수 없다.

- **[참고] 일별 신호 파일 위치가 실행 디렉터리에 의존함**
  - `Path("data/processed/unstructured_daily_signals.csv")`는 프로젝트 루트가 아니라 현재 작업 디렉터리 기준이다. 같은 코드와 데이터가 있어도 실행 위치에 따라 “미수집” 또는 “수집”으로 달라져 현황표 결과가 재현되지 않는다.
