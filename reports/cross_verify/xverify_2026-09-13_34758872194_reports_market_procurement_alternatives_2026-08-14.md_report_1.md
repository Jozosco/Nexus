# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: reports/market/procurement_alternatives_2026-08-14.md, reports/market/procurement_alternatives_2026-08-19.md, reports/market/procurement_alternatives_2026-08-25.md
- 런: 34758872194

---

## ① 수치·단위·논리 오류

- **[높음 | 전 파일] 하방 거리 산술이 표시값과 불일치한다.**  
  표시된 값 기준으로 `1,564−1,290=274 $/MT`인데 모두 `273 $/MT`로 기재했다. 따라서 8/14의 비대칭도 `206−274=−68 $/MT`이지 `−67 $/MT`가 아니다. 비표시 소수점 원값으로 계산했다면 해당 원값과 반올림 순서를 공개하지 않아 보고서 수치끼리 검산되지 않는다.

- **[치명 | 8/14] 현시점 가격 밴드로 2·4주 대기의 손익 방향을 추론했다.**  
  `[1,290, 1,770]`은 2·4주 후 조건부 가격분포가 아닌 현시점 밴드다. 중앙값 아래 거리가 더 크다는 사실만으로 가격 하락 확률이나 기대 하락폭이 더 크다고 볼 수 없으므로, `하방 꼬리 우세 → 대기 여지 있음`은 성립하지 않는다. 또한 P50은 기대값이 아니라 중앙값이고, `+206`은 상단 분위수까지의 거리이지 “기대 최대 보호액”이나 최댓값이 아니다. 8/19·8/25 문서가 동일 밴드로는 대기 손익을 판단할 수 없다고 명시해 8/14 해석 오류를 스스로 확인한다.

- **[높음 | 8/14] 1개월 커버리지 연장의 비교 델타를 현 P50 가격으로 대체했다.**  
  `1,564 $/MT`는 현재 가격 수준일 뿐 선매입과 1개월 후 구매의 비용 차이가 아니다. 연장 델타에는 미래 구매가격, 금융·보관비, 계약 프리미엄 및 물량이 필요하다. 8/19·8/25에서 동일 이유로 “산출 불가”라고 정정됐다. `+206 $/MT` 역시 현 밴드의 분위수 거리이므로 커버리지 연장의 기대 보호액이 아니다.

- **[높음 | 전 파일] 실제 커버리지를 알 수 없는데 부족분을 `0.0개월`로 단정했다.**  
  입력표에서 `1.5개월`은 “사내 실값 아님”인 기본값이다. 따라서 이를 사용한 `권장 하한까지 부족 0.0개월`은 실제 조달 상태에 관한 수치가 아니다. 특히 물리 리드타임이 40~50일인데 권장 하한은 1개월이므로, 입고 예정·파이프라인 재고 없이 부족분이 0이라는 결론도 보장되지 않는다.

- **[참고 | 전 파일] `$ /MT 델타` 열에 서로 다른 단위와 성격의 값이 혼재한다.**  
  같은 열에 가격 거리 `$ /MT`, 커버리지 부족분 `개월`, 단순 가격 수준, 계산 불가 문구, 전환 임계값이 들어 있다. 특히 `175 $/MT`는 현재 대안 간 비용 델타가 아니라 트리거이고, `0.0개월`은 열의 선언 단위와 다르다.

- **[높음 | 8/14] `P90−P50=155 $/MT`를 운임 프리미엄으로 귀속한 것은 분해 오류다.**  
  해당 값은 급등 레짐 전환 전후의 운임 차이가 아니라 잔차 또는 내재층의 분위수 폭이다. 원재료·FX·품질·공분산이 섞여 있어 운임 증분으로 해석할 수 없다. 8/19·8/25도 이를 명시적으로 인정한다. 또한 8/14의 basis 최저가와 운임 최저가 독립 결합은 원산지·laycan·신용조건이 호환되지 않을 수 있는 합성 최저가 오류다.

- **[참고 | 8/19·8/25] 분해 불가능하다고 인정한 `155 $/MT`를 계속 Incoterms 정량 델타로 제시했다.**  
  면책 문구가 있더라도 이 값은 `CFR 비용−(FOB+스팟 운임 비용)`을 추정하지 않으므로 Incoterms 대안의 규모감으로도 직접 대응하지 않는다.

- **[높음 | 8/14] 시장별 z점수 차이를 대체유의 경제적 저가 신호로 해석했다.**  
  각 시장의 자체 평균·변동성으로 표준화한 z점수 차이는 USD/MT 가격 차이가 아니다. 음의 z격차가 나와도 대체유가 실제로 더 싸다는 보장이 없어 배합 전환 신호로 사용할 수 없다. 더구나 대체유 관측은 2026-07-01로 보고일보다 44일 지연됐다. CBOT z의 기준일이 명시되지 않아 현재 CBOT과 7/1 대체유를 비교한 것인지, 모두 7/1로 맞춘 것인지도 확인할 수 없다.

- **[참고 | 8/19·8/25] 시점 축 설명이 내부적으로 충돌한다.**  
  본문은 “현 밴드는 예측이 아니므로 대기 방향 판단 불가”라고 하지만, 신선도·한계 절은 여전히 “①시점 축은 무추세 가정”이라고 적는다. 무추세 가정을 실제 적용했다면 미래 중앙경로를 가정한 것이고, 적용하지 않았다면 해당 문구는 삭제돼야 한다.

- **[높음 | 8/25] 최대 괴리 `+113%`만으로 ‘공행성 붕괴’와 평균회귀 금지를 결론냈다.**  
  단일 최대 괴리는 장기 공행성 또는 공적분의 구조적 붕괴를 입증하지 않는다. 기간별 상관·공적분 검정, 구조변화 검정이나 레짐별 안정성 검정 없이 평균회귀를 전면 금지하는 것은 통계적 결론을 과장한 것이다.

## ② 시계열 누수 위험

- **[높음 | 전 파일] point-in-time 데이터 차단 기준이 없다.**  
  보고일만 있고 생성 시각, 각 원천의 `observed_at`·`known_at`, 장 마감 시각 및 공표 지연이 없다. 따라서 당일 CBOT·BDI 종가나 관세청 수정치가 실제 의사결정 시각 이후에 공개됐는데도 당일 신호로 사용될 수 있다.

- **[높음 | 전 파일] 사후 CI 재실행이 역사 시점에 미래 데이터를 주입할 위험이 있다.**  
  “FX parquet 확보 후 CI 재실행 시 산출”이라고만 되어 있고 `observation_date ≤ report decision timestamp` 및 당시 데이터 빈티지를 고정한다는 규칙이 없다. 나중에 적재된 FX, 정정 가격 또는 수정 통계를 사용하면 8/14·8/19·8/25 당시에는 알 수 없던 값으로 과거 스프레드를 소급 계산하게 된다.

## ③ 상관을 인과로 과장한 해석

- **[높음 | 전 파일] BDI 하나로 대두유 운임 레짐과 커버리지·Incoterms 대응을 인과적으로 연결했다.**  
  BDI는 광범위한 건화물선 지수이며 실제 대두유 운송 선형·항로·laycan의 운임을 직접 측정하지 않는다. `BDI z=-0.24 → 평시 → 1~2개월`, `z>1/2 → 운임 경계/급등`, `평시 → FOB+스팟 검토`의 정책 연결에는 경로별 운임 실측, 시차, 효과크기 또는 비용성과 검증이 없다.

- **[높음 | 전 파일] CE-ID와 온톨로지 경로를 ‘인과 근거’로 취급했다.**  
  `해협→운임→CIF`, `BRL 약세→하방`, `B40→스프레드 축소` 같은 경로는 가능한 메커니즘을 표현할 뿐 인과 식별 결과가 아니다. 통제변수, 반사실 비교, 사건연구, 효과추정치가 없으며 CE-ID가 어떤 정량 델타를 산출했는지도 연결되지 않는다. 특히 온톨로지의 전파 구조 자체는 경험적 인과 증거가 될 수 없다.

## ④ 재현 불가능한 주장

- **[높음 | 전 파일] `SBO−CPO >175 $/MT` 전환 임계값을 재현할 근거가 없다.**  
  CE-015 외에 표본기간, 추정식, 손실함수, 가격 기준이 없고 문서 자체가 “통계 검증 대기”라고 인정한다. SBO·CPO의 선물/현물, 원산지, 정제도, 인도조건, FX 기준일, 관세·운임·수율·품질 전환비용도 정의되지 않았다. “GPT 교차검증 부호 정정”은 부호 검토일 뿐 임계값 검증이 아니다.

- **[높음 | 전 파일] 핵심 z점수와 `155 $/MT`를 재계산할 사양이 없다.**  
  BDI z의 윈도, 90일의 거래일/달력일 여부, 연속선물 롤 방식, 결측치 처리, 표준편차 정의, 가격 필드와 시점 정렬이 없다. `내재층 P90−P50=155`도 해당 층의 P50·P90 원값과 산식이 없어 표시된 전체 밴드로는 재계산할 수 없다.

- **[높음 | 8/25] `최대 +113%` 주장을 재현할 수 없다.**  
  분모가 CPO인지 SBO인지, 가격비율인지 스프레드 변화율인지, 비교 통화·등급·빈도·표본기간이 명시되지 않았다. `farmdoc 2025·A-201`만으로는 문헌의 제목·표·데이터 열을 특정할 수 없다.

- **[높음 | 전 파일] 날짜가 다른데 핵심 ‘현재’ 수치가 모두 동일하며 데이터 스냅샷이 없다.**  
  8/14·8/19·8/25에 P50 `1,564`, 밴드 `[1,290, 1,770]`, BDI z `−0.24`, CBOT z `−0.70`, 대체유 z격차가 그대로 반복된다. 대체유 외에는 최종 관측 시각·데이터 해시·생성 커밋이 없어 정상적인 무변동인지, stale 입력 또는 복사 결과인지 검증할 수 없다.
