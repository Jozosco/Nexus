# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/g1_publication_schedule_panel_2026_08_14.md
- 런: 34758941481

---

## ① 수치·단위·논리 오류

- **[높음] 분석 기간과 표본 비율이 서로 맞지 않음**  
  `2010-01~2025-12`는 192개월, 즉 **16개년**이다. 약 4,000거래일을 기준으로 한 `1/4,000≈0.025%` 역시 16개년 쪽에 가깝고, 15개년이면 약 `0.026~0.027%`다. 또한 2025-12로 고정된 창이면 2026년 신규 관측은 창에 들어가지 않으므로 “하루치 신규 관측” 논리와도 충돌한다. 레짐별 표본에서는 하루의 비중이 전체 표본의 0.025%보다 훨씬 클 수 있다.

- **[참고] “시장 개장 KST 07:00”의 기준 시장이 불명확하거나 시각이 틀림**  
  CBOT 대두유 전자장의 19:00 CT 재개장은 미국 서머타임 기준 KST 09:00, 표준시 기준 KST 10:00이다. CBOT 주간장 개장도 KST 07:00이 아니다. 다른 시장을 뜻한다면 시장명과 거래시간 근거가 없어 CI-001의 시간 논리를 검증할 수 없다.

- **[높음] ERA5-Land를 전일 확정치가 가능한 일별 지표로 분류한 것은 부정확함**  
  ERA5-Land의 준실시간 자료도 통상 수일의 지연이 있고 최종 자료는 더 늦게 확정된다. 따라서 KST 05:30에 “전일 확정치 가용”이라는 §2.1의 일괄 설명은 ERA5-Land에는 성립하지 않는다. AIS·GeoIntel 역시 원천과 수정 정책이 없어 “확정치” 여부를 확인할 수 없다.

- **[치명] 월별 심층판의 명세와 실제 계산 기능이 불일치함**  
  §4·§5는 월별판이 `horizon×레짐 전면 재산출`과 Granger 재검정을 수행한다고 승인 대상으로 제시하지만, §7.1 #5는 해당 계산이 아직 구현되지 않아 M-009에서 확장할 예정이라고 명시한다. 현재 구현으로는 “monthly” 명칭의 핵심 산출물을 제공하지 못하며, §5의 반일 작업량도 M-009 구현 비용을 포함하지 않는다.

- **[높음] 최종 구현 규격이 문서 내부에서 충돌함**  
  §5.3은 “`available_at` 최신값이 당일이면 익영업일 트리거”라고 규정하지만 §7.1 #2는 이를 폐기하고 “직전 스케줄 이후 신규 발생” 방식으로 바꿨다고 한다. 어느 규칙이 최종 승인 대상인지 본문에 통합되지 않았다. 또한 문서 머리말은 “구현 착수 금지”인데 §7은 3계층 구현 diff가 이미 반영됐다고 기술한다.

- **[높음] `available_at` 시간창만으로는 “정확히 1회”를 보장하지 못함**  
  공식 발표 후 수집 장애로 이틀 늦게 parquet에 들어온 행은 `available_at`이 직전 실행보다 이전이어서 트리거에서 누락될 수 있다. 반대로 동일 스케줄의 재실행·수동 재실행은 같은 시간창을 다시 평가하여 중복 발행할 수 있다. 소비 완료한 발표 ID를 영속적으로 기록하는 멱등성 규칙이 없다.

- **[높음] “익영업일” 처리가 실제로는 평일 처리에 불과함**  
  `30 20 * * 0-4`는 KST 월~금 실행만 보장하며 한국·미국 공휴일을 처리하지 않는다. 어느 국가의 영업일인지도 정의되지 않았다. 금요일 KST 실행은 UTC 기준 목요일이므로 요일 분기가 러너의 기본 UTC 날짜를 사용하면 주별판이 발행되지 않을 위험도 있다.

- **[높음] 금요일과 WASDE 트리거가 겹칠 때의 합성 규칙이 없음**  
  §5는 금요일을 `①+②`, WASDE 익영업일을 `①+③`으로 각각 적었다. WASDE 익영업일이 금요일이면 `①+②+③`이어야 하지만, 조건 분기가 `if/elif`인지 독립 조건인지 명시되지 않아 주별판 또는 월별판이 누락될 수 있다.

- **[높음] 신선도 게이트가 주별판의 데이터 포함 주장과 충돌함**  
  목표변수 신선도를 5영업일까지 허용하면 금요일 보고서가 직전 금요일 수준의 CBOT 값으로도 통과할 수 있다. 이는 “미국 목요일 마감분까지 포함” 및 전주 대비 변동을 제공한다는 §2.3·§4의 설명과 양립하지 않는다.

- **[높음] 예측 지평과 경보 수치의 단위가 확정되지 않음**  
  §2.3은 60 **거래일**이라고 하지만 §4의 `1·5·20·60일`은 거래일인지 달력일인지 명시하지 않는다. 또한 `2σ 등`은 수준·수익률·잔차 중 무엇의 표준편차인지, 산출창이 무엇인지 불명확하다. §7.2도 경보판 단위 표기가 아직 후속 과제임을 인정하므로 현재 경보 수치를 운영자가 일관되게 해석할 수 없다.

- **[높음] G2의 독립 컨볼루션은 주요 비용 성분의 의존성을 무시함**  
  CBOT, 환율, 베이시스, 운임은 공통 거시·공급 충격에 의해 상관될 수 있다. 근거 없이 독립으로 20,000회 합성하면 특히 꼬리 비용 폭을 과소 또는 과대 추정할 수 있으며, 고정 시드는 이 구조적 오류를 해결하지 않는다.

- **[높음] 분위수 단순합을 “완전 순위의존 스트레스 상계”로 부르는 것도 일반적으로 성립하지 않음**  
  동일 분위수의 단순합은 comonotonic 결합에서의 분위수일 뿐, 가능한 모든 의존구조에 대한 VaR 상계가 아니다. 예를 들어 각각 4% 확률의 손실은 개별 95% 분위수가 0이지만 손실 사건이 서로 배타적이면 합계 손실 확률이 8%가 되어 합계 95% 분위수가 0보다 커질 수 있다.

- **[높음] 과거 60거래일 분포로 2·4주 기대손익을 산출하는 문제는 여전히 미해결임**  
  과거의 무조건부 60일 분포는 2주·4주 조건부 예측분포나 기대값이 아니다. “분포 폭 참고치”로 명칭을 바꿔도 기존 값이 기대손익 열에 남아 있다면 수치의 의미가 바뀌지 않는다. 동월 CBOT 차감 역시 리드타임 가격변동과 계약조건을 포함하므로 순수 베이시스·물류비 분해로 사용할 수 없다.

## ② 시계열 누수 위험

- **[치명] 2010~2025 백필에 대한 point-in-time 데이터 빈티지 규칙이 없음**  
  `latest ≤ now` 가드는 2026년 실행 시점의 미래값만 막을 뿐, 과거 기준일에 아직 발표되지 않았거나 이후 개정된 WASDE·PSD·NASS·FAOSTAT·관세청 값을 차단하지 못한다. 최신 개정본으로 과거 G1 산출물이나 검증 표본을 만들면 과거 시점에 알 수 없던 정보가 유입된다.

- **[높음] horizon별 목표값 엠바고가 명시되지 않음**  
  `h=60거래일` 모델은 보고서 기준일에서 최소 60거래일 이전까지의 학습 행만 완전한 목표값을 가진다. 문서의 as-of 검증과 5영업일 신선도 게이트에는 `t+h≤as_of` 조건이 없어, 최근 행의 미래 목표를 잘못 포함하거나 백필 시 전체 표본 목표를 사용하는 위험이 있다.

- **[높음] 레짐·구조적 단절·2σ 임계값의 단방향 산출 여부가 없음**  
  레짐을 전체 2010~2025 표본의 사후 구조변화점, 전체 표본 평균·표준편차 또는 양방향 필터로 정의하면 과거 상태가 미래 관측에 의해 결정된다. 스케일링·결측 대체·SHAP 배경표본도 각 as-of 이전 자료로만 적합한다는 규칙이 없다.

- **[높음] “전주 대비 순위”의 과거 순위를 어떻게 취득하는지 불명확함**  
  전주 스냅샷을 당시 모델과 데이터로 저장하지 않고 현재 모델로 전주 값을 재계산하면, 전주 순위에 이번 주 정보가 들어간다. 주별판 보존 정책은 있지만 비교 시 저장 스냅샷을 사용한다는 명세가 없다.

## ③ 상관을 인과로 과장한 해석

- **[치명] Elastic Net·SHAP·Granger를 “인과 근거/인과 순위/validated 인과 엣지”로 해석함**  
  Elastic Net 계수와 SHAP은 예측모형 내부의 연관·기여도이고, Granger 검정은 선택된 시차와 조건변수 아래의 선행 예측력이다. 교란 통제, 식별 가정, 개입·자연실험, 도구변수 또는 반사실 검증 없이 실제 조달비용의 원인 효과를 식별하지 못한다. 따라서 주간 브리프의 “인과 근거”나 온톨로지 엣지의 `validated` 승격은 방법론이 뒷받침하지 않는다.

- **[높음] WASDE가 “최대 정보 이벤트”이고 순위가 월별 발표 때만 움직인다는 주장이 검증되지 않음**  
  이벤트 전후의 초과변동, 예측오차 감소, 순위 회전율 또는 다른 발표와의 비교 실험이 없다. 발표 시점과 순위 변화가 같이 나타나더라도 동시 시장정보·가격 충격을 배제하지 않았으므로 WASDE가 변화를 일으켰다고 볼 수 없다.

## ④ 재현 불가능한 주장

- **[높음] 중요도 순위 산식이 정의되지 않음**  
  Elastic Net 계수, SHAP 값, Granger 통계량을 어떤 방향·스케일로 정규화하고 어떻게 하나의 순위로 결합하는지 없다. 레짐 정의, 시차 선택, 유의수준, 다중검정 보정, SHAP 배경표본과 동률 처리도 없어 동일 데이터로 동일 순위를 재현할 수 없다.

- **[높음] “하루치로는 순위가 사실상 변하지 않는다”는 핵심 근거가 재현되지 않음**  
  제시된 것은 표본 비율과 M-006의 사용자 불만뿐이다. 일별 top-k 교체율, 순위상관, 부트스트랩 신뢰구간, 임계값 근접 변수 목록 또는 분석 코드·산출물이 없어 안정성 주장을 검증할 수 없다. 작은 표본 비중은 순위 안정성을 수학적으로 보장하지 않는다.

- **[높음] 발행 게이트와 경보 트리거를 동일하게 재실행할 수 없음**  
  “필수 5종”의 검사명·버전·허용치가 이 문서에 없고, WARNING의 보조/필수 판별 기준도 명시되지 않았다. `2σ 등 기존 로직`, 영업일 달력, 타임존, 경계시각의 포함 여부도 확정되지 않아 실행 환경별 발행 결과가 달라질 수 있다.

- **[높음] 교차검증 결과에 실행 스냅샷이 부족함**  
  run ID와 90일 아티팩트명만 있고 저장소/커밋 SHA, 입력 데이터 해시, 프롬프트, 모델·API 버전, 샘플링 파라미터, 출력 해시가 없다. `GPT-5.6-Sol xhigh` 표기만으로는 2건의 판정과 코드 변경을 재현할 수 없으며, 아티팩트 만료 후에는 원문 검증도 불가능하다.

- **[높음] “월간 아카이브”와 실제 보존 정책이 충돌함**  
  Blob 이관 완료 시점과 경로가 확정되지 않은 상태에서 월별판도 90일 후 삭제된다. 따라서 Blob 이관이 지연되면 월간 기준본과 검증 증거가 영구 소실되며, 장기 백테스트나 감사 재현에 사용할 아카이브가 존재하지 않는다.

- **[참고] 비용 및 작업량 판단의 근거가 없음**  
  일별 발행 비용, 토큰 수, 호출 횟수, 단가, 실패·재시도율이 없고 “약 반일” 추정의 작업 분해도 없다. 특히 미구현된 M-009를 제외한 반일 추정이므로 승인 대안 간 비용 비교를 재현할 수 없다.
