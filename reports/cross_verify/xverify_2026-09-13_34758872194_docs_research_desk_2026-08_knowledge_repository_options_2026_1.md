# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/knowledge_repository_options_2026_08_25.md
- 런: 34758872194

---

## ① 수치·단위·논리 오류

- **[높음] 문서 수와 인덱스 행 수가 불일치한다.**  
  §0의 요약은 2,175건인데 인덱스는 2,227행으로 **52행(약 2.4%)** 더 많다. 중복·원문 없는 행·요약 없는 행 여부를 밝히지 않은 채 “원문↔요약이 한 테이블로 조인 가능”, “RAG의 절반 완성”이라고 결론냈다. 후자의 50%도 산정 기준이 없다.

- **[높음] “문서 ~5천”이라는 규모가 권장안의 실제 색인 범위와 맞지 않는다.**  
  구조에 명시된 입력은 `2,175 + 300+ + 66 + 152 = 2,693+`개이며, 152개는 문서가 아니라 용어다. 원문 PDF 2,175개까지 별도 문서로 더해야 약 4,868개가 되지만, 정규화 입력에는 PDF가 포함되어 있지 않다. 따라서 벡터 DB가 과잉이라는 판단에 사용된 규모의 분모가 불명확하다.

- **[높음] 제안 스키마로 EvidenceSpan 계약을 준수할 수 없다.**  
  제안 필드는 문서 ID·유형·날짜·태그·엔티티·본문·출처경로뿐이며, 계약상 필수라는 **원문 페이지, 원문 인용문, span offset 또는 원문 해시**가 없다. 출력도 “경로·발췌·날짜·근거 등급”만 제시한다. 특히 MEMORY·용어·research_desk에는 PDF 페이지가 없을 수 있어 “page+원문 인용 100% 일치 강제”, “새로 설계할 것이 없다”는 결론이 구조와 모순된다.

- **[높음] FAISS·Chroma·LanceDB를 모두 “별도 인프라”로 간주한 것은 사실·논리 오류다.**  
  FAISS는 프로세스 내 라이브러리이고, Chroma와 LanceDB도 로컬·임베디드 구성이 가능하다. 승인 라이브러리가 아니라는 사유와 별개로, 이들을 SaaS형 신규 인프라와 동일한 D-025 유형으로 배제한 근거는 성립하지 않는다.

- **[높음] “비용 0”, “이식성 100%”, “잔존자산 100%”는 단위와 분모가 없고 서로 모순된다.**  
  로드맵 자체가 Phase별 반나절의 인건비, 로컬 계산·저장 비용, HF 다운로드를 요구하므로 총비용은 0이 아니다. 또한 parquet 데이터가 이동 가능하더라도 TF-IDF 어휘·IDF, 임베딩 모델, 전처리 설정, CLI 환경과 DuckDB 질의 의미까지 Snowflake로 “그대로” 승계되는 것은 아니다.

- **[높음] “Phase 1→2는 같은 스키마라 재작업 없음”이 설계 내용과 모순된다.**  
  Phase 2는 임베딩 컬럼과 최소한 모델 ID·revision·차원·dtype·생성시각을 추가해야 한다. 문서 단위에서 chunk 단위 검색으로 바꾸면 행의 단위와 식별키도 변경된다. 스키마 변경이나 재색인 없이 전환된다는 주장은 성립하지 않는다.

- **[참고] “438KB 문서는 전문 로드 불가”라는 판정은 단위가 불충분하다.**  
  바이트 크기만으로 컨텍스트 적재 가능 여부를 결정할 수 없다. 파일별 토큰 수, 사용 모델의 컨텍스트 한도, 시스템 프롬프트·출력 예약량이 제시되지 않았다.

- **[높음] 외부 SaaS의 “잔존자산 0·승계 불가”는 검증 없이 절대화됐다.**  
  Notion·Airtable 모두 데이터 내보내기 경로가 존재할 수 있으므로, 실제 스키마·첨부파일·이력의 export/import 시험 없이 승계 가능 자산을 0으로 둘 수 없다.

## ② 시계열 누수 위험

- **[치명] 예측 기준시점(`as_of`) 제약이 전혀 없다.**  
  CLI는 질문만 받고, 스키마에는 `available_at`이나 필수 기준시점 필터가 없다. 이 저장소를 과거 대두유 가격 예측이나 백테스트에 사용하면 예측일 이후의 GAIN·FAO 보고서, MEMORY 결정, 정오표, 사후 분석이 검색되어 결과를 무효화할 수 있다.

- **[높음] 단일 “날짜” 필드가 발행일·입수일·요약 생성일·개정일을 혼합한다.**  
  원문 발행일로 요약 레코드를 소급 기록하면 나중에 작성된 요약·감성·태그·엔티티가 과거 시점에 이미 존재했던 것처럼 보인다. 최소한 `source_published_at`, `first_available_at`, `processed_at`, `revised_at`의 분리가 필요하나 설계에 없다.

- **[높음] 현재 상태의 단일 parquet에는 문서 버전과 유효기간이 없다.**  
  research_desk의 정오표나 MEMORY 결정이 수정되면 과거 버전이 사라진다. `revision_id`, `valid_from/valid_to` 또는 vintage snapshot이 없어 과거 예측 재현 시 당시 이용 불가능했던 수정본을 조회할 위험이 있다.

- **[높음] 전체 코퍼스로 학습한 TF-IDF의 IDF 자체가 미래 정보를 사용한다.**  
  미래 문서를 결과에서 필터링하더라도 vectorizer를 전체 mart에 한 번 fit하면 미래 문서의 단어 빈도가 과거 문서 순위를 바꾼다. 기준시점별 코퍼스로 fit하거나 당시 고정된 vectorizer vintage를 사용한다는 규칙이 없다.

- **[참고] 최신 용어 사전·signal mapping을 과거 질의에 적용하는 것도 누수원이 될 수 있다.**  
  사후에 추가된 동의어·정책명·인과 용어가 과거 시점의 검색 대상을 바꿀 수 있으나, 시맨틱 레이어의 버전·유효시점 정책이 없다.

## ③ 상관·연관을 인과로 과장한 해석

- **[높음] 검색층과 생성층의 분리가 환각을 “구조적으로 피한다”는 주장은 과장이다.**  
  분리는 근거 검색을 개선할 수 있을 뿐, 생성기가 검색 문서를 잘못 종합하거나 존재하지 않는 인과·수치·인용을 만드는 것을 방지하지 않는다. 무근거 문장률·인용 정확도 등의 비교 실험 없이 환각 회피 효과를 단정했다.

- **[참고] “파일에 잠김 → 세션마다 같은 조사 반복”이라는 비용 인과가 입증되지 않았다.**  
  반복 조사 횟수, 소요시간, 검색 실패 로그 또는 현행 방식과의 비교가 없다. 교차 저장소 검색 부재와 반복 비용의 동시 존재만으로 전자가 후자를 발생시켰다고 단정할 수 없다.

- **[참고] “단일 parquet+단일 CLI → 인수인계 내성”도 검증되지 않은 인과 주장이다.**  
  신규 담당자의 검색 성공률·교육시간·환경 구축 실패율을 측정하지 않았다. 단일 명령은 인터페이스 단순성을 보여줄 뿐, 인수인계 성공을 보장하지 않는다.

## ④ 재현 불가능한 주장

- **[높음] 자산 “실측”과 검색 코드 0건을 재현할 정보가 없다.**  
  Git commit, 기준 브랜치, glob, 제외 디렉터리, 중복 판정법, 파일 크기 계산 방식, 코드 검색 명령과 확장자 범위가 없다. 특히 2,175건과 2,227행의 차이를 설명할 manifest도 없다.

- **[높음] TF-IDF 검색 결과를 재현할 사양이 없다.**  
  chunk 단위, tokenizer, 한·영 정규화, stopword, n-gram, `min_df/max_df`, sublinear TF, 질의 확장 순서, 메타 필터 적용 시점, 동점 정렬키, vectorizer 저장 여부 및 scikit-learn 버전이 누락됐다. 동일 질의의 상위 N건을 재현할 수 없다.

- **[높음] Phase 2 임베딩 결과를 재현할 모델 식별자가 없다.**  
  “sentence-transformers”, “HF egress 2종”만 있고 정확한 두 모델명, Hugging Face revision/commit hash, pooling, 최대 길이, 정규화, 차원, dtype가 없다. 모델 저장소의 최신 revision이 바뀌면 같은 문서의 벡터와 순위가 달라진다.

- **[높음] TF-IDF의 적합성 및 벡터 DB의 과잉 판단을 뒷받침하는 벤치마크가 없다.**  
  대표 질의 세트, relevance label, Recall@K/MRR/nDCG, 지연시간, 메모리 사용량, 실패 기준이 제시되지 않았다. 따라서 “규모상 과잉”, “용어 확장으로 보완”, “반나절 구현”을 독립적으로 검증할 수 없다.

- **[참고] 제품·정책 관련 결정적 전제가 버전 고정된 근거 없이 제시됐다.**  
  NotebookLM의 API 부재, 승인 라이브러리 목록, Actions 종속성, egress 신청 완료가 내부 ID나 서술로만 인용되며 정확한 파일 경로·revision·제품 tier·확인일이 없다. 시점에 따라 바뀌는 사실이라 보고서만으로 재현 검증할 수 없다.
