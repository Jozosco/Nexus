# GPT 교차검증 — 2026-09-14 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 34797094475

---

- **[치명][① 수치·단위·논리 · ④ 재현성] 미매핑 지표의 데이터 출처를 임의로 “무료·CONFIRMED”로 생성한다.**  
  `_source_of()`가 `None`을 반환하면 실제 원장 근거 없이 `"무료 공개 소스(USDA·FRED·NOAA·공개 API)"`, `"cost": "무료"`, `"label": "CONFIRMED"`를 부여한다. 출처가 확인되지 않은 지표가 오히려 확인된 무료 데이터처럼 표시되어 데이터 계보와 비용 판단을 왜곡한다. 또한 prefix는 첫 일치 항목을 사용하므로 짧은 prefix가 긴 prefix를 가리면 YAML 배열 순서에 따라 잘못된 원천이 배정될 수 있다.

- **[높음][① 수치·단위·논리] 실제 outcome을 버리고 모든 사슬을 동일한 두 가격으로 강제 수렴시킨다.**  
  `_edges()`에는 실제 `outcome.node`가 들어가지만 렌더링은 `e.chain.slice(0,-1)`로 outcome을 제거한 뒤 모든 행을 `"대두유 국제가 · 수입 CIF"` 상자에 연결한다. 실제 outcome이 둘 중 하나가 아니어도 검증하지 않으며, 국제가와 한국 CIF라는 서로 다른 가격 기준을 동시에 결과로 보이게 한다. 따라서 “모든 사슬은 … 수렴한다”는 문구는 입력 원장으로 보증되지 않는다.

- **[높음][① 수치·단위·논리] DAG 순환 수치 `0`이 계산값이 아니라 하드코딩되어 있다.**  
  메타 패널은 항상 `<span class="n">0</span><span class="l">DAG 순환</span>`을 출력한다. 이 스크립트에는 순환 검사도 없고 외부 검증기 호출도 없다. 검증되지 않은 ontology를 직접 빌드해도 순환 0으로 표시된다.

- **[높음][① 수치·단위·논리] 방향과 상태의 결측·미지원 값을 반대 의미로 강제 변환한다.**  
  방향이 없으면 `_edges()`가 자동으로 `"UP"`을 부여하고, UI는 `"UP"`이 아닌 모든 값을 `"DOWN"`으로 표시한다. 따라서 `UNKNOWN`, `MIXED`, `CONDITIONAL` 같은 값도 하락으로 오인된다. 상태 역시 정확히 `"validated"`가 아닌 모든 값을 후보로 집계하므로 폐기·반증 상태도 후보로 표시될 수 있다.

- **[높음][① 수치·단위·논리 · ④ 재현성] 입력에 없는 리드타임 `40~50일`을 임의로 삽입한다.**  
  `lead_time_days`가 없으면 `"40~50일(실측 역산 갱신 예정)"`을 출력하지만 이 수치의 출처·산식·기준일·달력일/영업일 구분이 없다. 결측치를 숫자 범위로 대체해 실제 공급망 리드타임처럼 노출한다.

- **[높음][① 수치·단위·논리] 검증하지 않은 품질 상태를 확정적으로 표시한다.**  
  `"좌표 전량 확인"`은 tier 목록 개수만 센 뒤 출력하며 좌표 존재 여부·범위·중복을 검사하지 않는다. `"신뢰도 지표(2단계 검증)"`도 `historical`, `realtime` 목록 길이만 계산할 뿐 검증 수행 여부나 결과를 확인하지 않는다.

- **[높음][① 수치·단위·논리] 메타 지표의 명칭과 실제 집계 기준이 다르다.**  
  `n_ev`는 각 edge의 `evidence` 항목 수를 단순 합산하므로 같은 문헌이 여러 사슬에 중복되면 반복 집계되고, evidence가 반드시 문헌이라는 보장도 없다. 그런데 화면에는 `"근거 문헌 N건"`으로 표시한다. 또한 `"무료 대안 없음"`은 `free_status`나 `free_alternative`가 아니라 `label === "DATA GAP"` 개수를 세며, `"유료 사용"`도 실제 수집 로그가 아니라 `cost` 문자열이 `"유료(사용)"`으로 시작하는 항목 수다.

- **[높음][③ 상관의 인과 과장] `status` 메타데이터만으로 인과관계를 “검증됨”으로 제시한다.**  
  시간적 선후, 교란 통제, 자연실험·개입, 강건성 검정 등 인과 식별 조건을 확인하지 않고 `status === "validated"`이면 실선과 `"검증됨"` 라벨을 부여한다. `evidence`의 유형도 검사하지 않으므로 관측 상관이나 전문가 판단만 있는 관계도 검증된 인과 사슬로 표현될 위험이 있다.

- **[높음][④ 재현 불가능한 주장] HTML에서 근거를 확인할 수 있다는 설명과 달리 인용 정보를 폐기한다.**  
  `_edges()`는 `evidence` 원문·문헌 ID·URL을 보존하지 않고 길이만 `ev`로 저장한다. 클릭 상세에도 `"근거 문헌 N건"`만 나오므로 사용자는 어떤 문헌이 어떤 사슬을 지지하는지 재검증할 수 없다.

- **[높음][④ 재현 불가능한 주장] “결정적 생성” 및 “원장 하나”라는 설명이 구현과 모순된다.**  
  출력에 로컬 날짜인 `date.today()`가 포함되어 같은 입력도 실행일에 따라 다른 HTML이 생성된다. 용어 수는 별도 파일 `entities.yaml`에 의존하므로 `ontology.yaml` 하나만으로 생성되지 않는다. 렌더링도 Google Fonts 외부 서비스에 의존해 오프라인 여부나 원격 폰트 변경에 따라 결과가 달라질 수 있다.

- **[참고][① 수치·단위·논리] 버전 테스트가 구현 상수와 동일한 값을 사용해 독립적인 검증 기준을 잃었다.**  
  테스트가 명시적인 기대값 `v1.1`/`v1.0` 대신 `SUMMARY_VERSION_NEW`/`SUMMARY_VERSION_OLD`를 그대로 참조한다. 상수가 잘못 변경되어도 출력과 테스트가 함께 같은 잘못된 값을 사용하므로 요구 버전이 실제로 `v1.2`인지 검출하지 못한다.
