# GPT 교차검증 — 2026-08-28 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 33208016138

---

## ① 수치·단위·논리 오류

- **치명 — 실패한 품질 게이트도 ‘서명된 검사’로 발행될 수 있음.**  
  `g1-analysis`의 `if`는 `needs.model-readiness.result`만 검사하고 `c08-dq-validation.result`는 검사하지 않는다. `!cancelled()`가 있으므로 c08이 실패해도 취소만 아니면 실행될 수 있다. 이때 보고서는 게이트가 `FAIL` 또는 `미확인`이어도 상단에 항상 `✔ 서명된 검사`를 표시한다. 실제 서명자·서명값·검증 해시도 없다.

- **높음 — 경고·미수집을 ‘서명된 무소식’으로 오분류함.**  
  무경보 조건은 `🚨`가 없다는 것뿐이다. `⚠️`·`❓`는 `watch`에 남아 있어도 `alerts_html`은 “감시 변인 전체가 임계 범위 내”, “검사를 통과한 결과”라고 출력한다. `alerts=[]`인 경우조차 검사 항목 0건을 정상 통과처럼 표시한다.

- **높음 — 브리프 생성 실패 후에도 발행 완료로 기록됨.**  
  `build_daily_brief()` 또는 파일 쓰기 예외를 모두 삼킨 뒤, alert 모드에서 breach가 없으면 “일별 브리프 … 발행 완료”를 출력하고 반환한다. 실제 HTML이 없는데도 잡은 성공할 수 있고, 업로드 단계도 기존 G1 보고서만 있으면 전체 성공으로 끝날 수 있다.

- **높음 — P10/P90을 최소·최대로 잘못 표기함.**  
  `band_p10`, `band_p90` 및 경험분포 10·90분위수를 UI에서 각각 `최소`, `최대`로 표시한다. 분위수 밖에 각각 10%의 관측이 존재하므로 극단 위험을 과소표현한다.

- **높음 — 60거래일 팬 차트의 시간축과 경로가 수학적으로 일치하지 않음.**  
  전망 60거래일을 `fwd=20`개 점으로 축약하면서 `x()`는 이 점들을 과거 일별 관측과 같은 간격으로 배치한다. 따라서 과거 90거래일 대비 미래 60거래일 구간이 실제 비율보다 크게 압축된다. 또한 최종 전망점은 `n+fwd-2`인데 축 종료와 날짜 라벨은 `total=n+fwd-1`에 놓여 한 칸 어긋난다. 중간 P10/P90 경로도 `sqrt(t)`, P50은 `t` 보간으로 임의 생성되어 해당 시점의 분위수 산출값이 아니다.

- **높음 — ‘90일 z’가 90일이 아니라 90개 행임.**  
  `_z90()`은 `vals.tail(90)`을 사용하며 최소 30행만 있어도 값을 낸다. 특히 월별 ENSO ONI는 약 90개월의 z-score가 `90일 z` 열에 표시된다. 다른 지표의 일간·주간 변화도 실제 날짜 간격이 아니라 직전 행·6번째 전 행 기준이어서 결측 구간이 있으면 단위가 틀어진다.

- **높음 — 관측 없는 항로를 정상으로 위장함.**  
  `_route_status()`는 전용 지표가 없다고 주석으로 인정하면서 파나마와 말라카를 무조건 `"ok"`로 설정한다. 모듈의 “미수집 정직 표기” 원칙과 반대이며, 데이터 부재가 녹색 정상 상태로 표시된다.

- **높음 — 발주 시한이 리드타임을 실제로 역산하지 않음.**  
  `order_deadline = next_wasde + timedelta(days=6)`이며 `LEADTIME_DAYS=50`은 설명 문자열에만 사용된다. 도착 예정일이나 선적일에서 50일을 빼는 계산이 없으므로 “CIF 한국 리드타임 50일 역산 기준”이라는 근거가 성립하지 않는다.

- **높음 — 가격 스냅샷의 단위가 누락됨.**  
  CBOT ZL, CPO 등 서로 다른 단위의 값이 단위 없이 출력된다. 예컨대 CBOT는 USc/lb이고 CPO 계열은 통상 MYR/MT 등 별도 단위를 사용하므로 숫자만으로는 검증하거나 비교할 수 없다. `src`도 실제 입력 행의 출처를 확인하지 않고 CME·TE/Bursa 등으로 고정한다.

- **참고 — 60거래일 종료일이 CME 거래일 기준이 아님.**  
  `np.busday_offset`은 주말만 제외하고 CME 휴장일을 반영하지 않으므로 차트 종료 날짜가 실제 60번째 거래일과 달라질 수 있다.

- **참고 — ‘20거래일 지평’이 입력과 무관하게 고정됨.**  
  `build_daily_brief()`는 지평 인자를 받지 않지만 중요도 블록에는 항상 `20거래일 지평`을 출력한다. `target_label`이나 실행 설정이 다른 지평이면 보고서 설명과 분석 타깃이 불일치한다.

## ② 시계열 누수 위험

- **치명 — `run_ts`/`available_at` 기준의 as-of 필터가 전혀 없음.**  
  `_dated_series()`는 전체 프레임에서 가장 마지막 `price_date`를 선택하며 `price_date <= run_ts` 또는 `available_at <= run_ts`를 검사하지 않는다. `_reference_range_usclb()`도 전체 종가 이력을 사용한다. 특히 historical backfill에서 프레임에 후일 관측이나 최신 수정치가 포함되면 미래 정보가 KPI·범위·경보에 직접 유입된다. 그런데 footer는 검증 없이 “모든 입력은 available_at ≤ 발행 시점”, “장 마감 후 지표는 1일 지연 적용”이라고 단정한다.

- **치명 — 백필 보고서에 실행일 기준의 최신 비정형 신호와 도착가가 혼입됨.**  
  `_load_signals()`는 `run_ts`가 아니라 `date.today()`를 사용하고 하한만 적용해 미래 날짜 행도 배제하지 않는다. `_landed_band()` 역시 as-of 날짜 없이 현재 데이터를 로드한다. 따라서 과거 기준 보고서를 재생성하면 당시 알 수 없던 최신 기사·관세청 실적·가격층이 섞인다.

- **높음 — 동일 `price_date` 수정본의 공개 시점을 무시함.**  
  중복 행은 `available_at`이나 수정 시각이 아니라 단순 행 순서로 `keep="last"` 처리한다. 현재 시점에 개정된 CPI·수급 자료 등이 과거 백테스트에 들어갈 수 있고, 프레임 결합 순서가 바뀌면 선택값도 달라질 수 있다.

## ③ 상관을 인과로 과장한 해석

- **높음 — 피어슨 상관 부호를 가격의 상·하방 ‘방향’으로 표시함.**  
  핵심 변인의 `상방 ▲/하방 ▼`는 `피어슨_r` 부호만으로 결정되며, 요약에서는 이를 “요인”이라고 부른다. 상관은 인과 방향을 증명하지 않으며, 다중공선성 때문에 Elastic Net 계수 부호와 피어슨 부호가 반대일 수도 있다. 현재 구현은 막대 강도는 LASSO 계수, 방향 화살표는 상관계수로 만들어 해석 기준도 일관되지 않는다.

- **높음 — 키워드 기사에 정적 인과 사슬과 ‘검증됨’을 부여함.**  
  `_ONTOLOGY_CHAINS`는 기사 → 정책/위험 → 수요·공급 → 가격 상·하방을 하드코딩한다. 현재 기사의 구체 내용이나 조건을 검증하지 않고 `CE-003/013/014/022 (검증됨)`을 표시하며, 실제 `ontology.yaml`도 로드·검증하지 않는다. 이는 기사 키워드 연관성을 현재 가격 효과의 인과 근거로 과장한다.

## ④ 재현 불가능한 주장

- **높음 — 산출 방법 설명이 실제 실행 메타데이터와 결선되지 않음.**  
  보고서는 “최근 12개월”, “벌크 ≥100 MT”, “몬테카를로 20,000표본·시드 고정”, “Elastic Net + SHAP + Granger”를 고정 문구로 출력한다. 하지만 렌더러가 받는 것은 분위수 세 값과 중요도 표뿐이며 표본 수·시드·잔차 기간·모델 버전·SHAP/Granger 결과를 전달받거나 확인하지 않는다. 구현이나 설정이 바뀌어도 설명은 그대로 남는다.

- **높음 — 같은 `run_ts`로 재실행해도 결과가 달라짐.**  
  신호 컷오프, D-day 및 일정 선택이 `run_ts`가 아닌 `date.today()`에 의존한다. 또한 GitHub Ubuntu 러너는 통상 UTC인데 보고서는 `KST 05:30` 체계를 고정 표기한다. KST 05:30 실행 시 UTC 날짜는 전날이므로 보고서 날짜·최근 3일 신호·D-day가 하루 어긋날 수 있다.

- **높음 — 정책·일정의 근거 원문과 버전이 없음.**  
  WASDE 날짜, `Decree 423/2026`, 아르헨티나 수출세 `24%→15%`, 홍해 우회 `+12~15일`, 한국 리드타임 `40~50일`이 코드 상수·문구로만 존재한다. 출처 URL, 공표일, 문서 버전 또는 데이터 스냅샷이 없어 해당 런에서 사용한 사실관계를 재검증할 수 없다.
