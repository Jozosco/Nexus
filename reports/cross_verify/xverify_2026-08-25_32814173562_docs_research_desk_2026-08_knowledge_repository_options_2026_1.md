# GPT 교차검증 — 2026-08-25 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/knowledge_repository_options_2026_08_25.md
- 런: 32814173562

---

## ① 수치·단위·논리 오류

| 심각도 | 지적 | 구체 근거 |
|---|---|---|
| **높음** | 인덱싱 대상 규모의 분모가 일관되지 않는다. | 제안 구조가 실제로 적재하는 레코드는 요약 2,175 + 결정 300+ + research 문서 66 + 용어 152 = **최소 2,693건**이다. 그런데 벡터 DB 판단에는 “문서 ~5천”을 사용한다. 약 5천은 원문 PDF 2,175건까지 중복 계산해야 근접하지만, 구조도에는 원문 PDF가 적재 대상에 없다. 규모를 근거로 한 “전용 엔진 과잉” 판단의 기준이 불명확하다. |
| **높음** | “원문 PDF와 1:1” 및 “한 테이블로 조인 가능”이라는 완전성 주장이 수치로 입증되지 않는다. | 요약은 2,175건, 인덱스는 2,227행으로 **52행 차이**가 난다. 추가 52행의 유형, 키 중복, 원문·요약 경로 null, orphan 비율이 제시되지 않았다. 따라서 조인 가능성과 전체 커버리지는 별개의 문제이며, 이를 “RAG의 절반이 완성”으로 수치화할 근거도 없다. |
| **높음** | 제시된 스키마로는 EvidenceSpan 계약을 일괄 준수할 수 없다. | 저장 컬럼은 `문서id·유형·날짜·태그·엔티티·본문·출처경로`뿐으로 `page`, 원문 인용문, 문자 오프셋, 원문 해시가 없다. 또한 MEMORY 결정·시맨틱 용어·일반 Markdown에는 PDF 페이지 자체가 없을 수 있다. 그럼에도 모든 결과에 `page+원문 인용 100% 일치`를 강제한다는 설명은 구조와 모순된다. |
| **높음** | 문서 단위 TF-IDF만으로 “정확한 발췌”를 반환한다는 논리가 빠져 있다. | 청킹·문단 ID·시작/종료 오프셋·페이지 매핑이 없으므로 TF-IDF는 최대 438KB 문서가 관련 있다는 것만 랭킹할 뿐, 어느 문장이 근거인지 결정하지 못한다. 이는 “전문 로드 불가” 문제와 EvidenceSpan 인용 문제를 해결하지 않는다. |
| **높음** | “비용 0”, “재작업 없음”, “잔존자산 100%”는 서로 다른 범위를 혼용한 절대 주장이다. | 반나절 인건비와 실행 컴퓨팅 비용이 명시돼 있으므로 총비용은 0이 아니며, 성립 가능한 표현은 신규 라이선스·구독료 0 정도다. Phase 2는 임베딩 컬럼 추가와 TF-IDF→밀집벡터 랭킹 코드 변경이 필요하므로 “같은 스키마라 재작업 없음”도 사실상 성립하지 않는다. Parquet 데이터의 이동 가능성이 모델 파일·벡터라이저 설정·런타임·Snowflake 질의 동작까지 100% 승계된다는 뜻도 아니다. |
| **높음** | Notion·Airtable의 “이관 시 승계 불가·잔존자산 0”은 서비스 계정과 데이터 자산을 혼동한다. | 두 서비스 모두 CSV 등 데이터 내보내기 수단이 있으므로 계정 또는 워크플로우를 그대로 승계하지 못하더라도 데이터 자산이 0이 되는 것은 아니다. 사내 정책상 사용 불가라는 결론과 기술적으로 이관 불가능하다는 결론은 구분해야 한다. |
| **참고** | 외부 제품을 절대적으로 분류한 문구가 부정확하다. | “NotebookLM은 API도 없음”은 소비자용 제품과 Google Cloud/Enterprise 계열 API를 구분하지 않는다. Azure AI Search도 무료 제한 SKU와 유료 운영 SKU를 구분하지 않고 전부 “유료”로 적었다. FAISS는 별도 서버형 DB가 아니라 인프로세스 라이브러리이며 Chroma·LanceDB도 임베디드 구성이 가능하므로 셋 모두를 “별도 인프라”로 단정할 수 없다. 승인 라이브러리 미등재는 별도의 배제 근거다. |
| **참고** | “438KB 문서는 전문 로드 불가”는 단위와 실행 조건이 빠진 절대 표현이다. | 파일 바이트 크기는 모델 토큰 수와 같지 않다. 인코딩, 실제 토큰 수, 사용할 모델의 컨텍스트 한도와 시스템 프롬프트 예산이 없으므로 438KB라는 수치만으로 로드 불가를 판정할 수 없다. |
| **높음** | 검색과 생성을 분리하면 환각을 “구조적으로 피한다”는 결론은 성립하지 않는다. | 보고서 자체가 종합 서술을 기존 에이전트가 수행한다고 명시한다. 검색 결과에 없는 내용을 에이전트가 생성하거나 인용을 잘못 연결할 위험은 그대로이며, constrained generation·인용 검증기·근거 없는 문장 차단 규칙이 제시되지 않았다. |

## ② 시계열 누수 위험

| 심각도 | 지적 | 구체 근거 |
|---|---|---|
| **치명** | 과거 시점 질의·백테스트를 위한 point-in-time 경계가 없다. | 스키마에는 의미가 불명확한 `날짜` 하나만 있고, CLI에도 `as_of` 인자가 없다. 문서의 사건일·발행일·최초 입수일·요약 생성일·수정일을 구분하지 않으므로 과거 예측 시점에 이후 작성된 research 문서, MEMORY 결정, 정오표 또는 개정본을 검색할 수 있다. 결과에 날짜를 표시하는 것은 미래 문서의 검색 자체를 차단하지 않는다. |
| **높음** | 전체 최신 코퍼스로 적합한 TF-IDF와 질의 확장 레이어가 미래 정보를 간접 사용한다. | 현재 전체 문서로 IDF를 계산하면 미래 문서의 단어 빈도가 과거 시점 랭킹 가중치에 반영된다. 152개 용어·signal mapping·인과엣지도 생성·유효 시점이 없어서 이후 축적된 개념을 과거 질의에 사용할 수 있다. 결과 문서만 사후 날짜 필터링해도 이 표현·랭킹 누수는 제거되지 않는다. |

## ④ 재현 불가능한 주장

| 심각도 | 지적 | 구체 근거 |
|---|---|---|
| **높음** | “조사 실측” 자산 수와 검색 코드 0건을 재현할 정보가 없다. | 기준 commit/branch, 탐색 루트, glob, 제외 규칙, 중복 처리, 실행 명령과 출력 로그가 없다. 따라서 2,175건·2,227행·66개·300+건·152개 및 “의미 검색 코드 0건”이 같은 저장소 스냅샷에서 나온 결과인지 재실행할 수 없다. |
| **높음** | “정확한 근거를 빨리 찾는다”는 성능 주장이 검증되지 않았다. | 평가 질의 세트, 정답 근거, relevance 판정자, Recall@k/MRR/nDCG, 인용 정확도, 지연시간 및 grep 기준선이 전혀 없다. 예시 질의 한 건과 자산 규모만으로 TF-IDF가 현재 방식보다 정확하거나 빠르다고 재현할 수 없다. |
| **높음** | Phase 2 임베딩을 재생성할 사양이 없다. | `sentence-transformers`라는 패키지만 있고 모델 ID·revision/commit, tokenizer, pooling, 정규화 여부, 벡터 차원·dtype, 유사도 함수 및 패키지 버전이 없다. 동일 문서에서도 모델 revision과 설정에 따라 벡터와 순위가 달라진다. “HF egress 2종”도 대상 도메인·모델·승인 티켓이 특정되지 않았다. |
| **참고** | Phase별 “~반나절” 및 반복 조사 비용 주장은 산출 근거가 없다. | 구현 없음이라고 명시했지만 작업 분해, 담당자 기준, 하드웨어, 데이터 정제·예외 처리·테스트 시간, 과거 작업 로그가 없다. 특히 EvidenceSpan 페이지 매핑과 2,227행 조인 검증이 일정에 포함됐는지도 알 수 없다. |
