# GPT 교차검증 — 2026-09-02 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/g1_publication_schedule_panel_2026_08_14.md
- 런: 33604346354

---

## ① 수치·단위·논리 오류

- **[높음] 분석 기간과 표본 비율이 서로 맞지 않음** — `2010-01~2025-12`는 180개월이 아니라 192개월, 즉 **16개년**이다. 약 4,000거래일의 1건인 `0.025%`도 오히려 16개년 일별 표본에 가깝다. 월별 자료는 1건이 약 0.52%(1/192), 연별 자료는 약 6.25%(1/16)이므로 혼합주기 자료 전체에 `0.025%`를 일괄 적용할 수 없다. 또한 15년 롤링창이면 신규 1건 추가와 동시에 과거 1건이 제거되므로 “하루치 1건만의 영향”이라는 논리도 성립하지 않는다.

- **[높음] 2026년 현재 데이터의 포함 여부가 불명확함** — 작성일은 2026-08-14인데 명시된 분석창은 2025-12에서 끝난다. 2026년 YTD가 별도 증분으로 결합되지 않았다면 목표변수 신선도 5영업일 게이트와 충돌하고, 결합됐다면 “2010-01~2025-12 추정창” 및 표본 비율 설명이 틀리다.

- **[높음] “KST 05:30에 전일 확정치 가용”이라는 일괄 전제가 사실과 맞지 않음** — ERA5-Land 확정 자료는 통상 D-1 즉시 제공되지 않고, near-real-time 계열도 수일 지연된다. VIX 미국 종가는 미 동부 표준시 기준 KST 약 06:00~06:15 이후이므로 겨울철 05:30에는 확정되지 않는다. 따라서 금요일 05:30 실행이 표에 열거된 모든 “미국 목요일 마감분”을 포함한다는 보장이 없다.

- **[높음] “시장 개장 KST 07:00”이 CBOT 기준으로 잘못됨** — CBOT 대두유 전자거래 저녁 세션의 19:00 CT 개장은 한국시간으로 미국 서머타임 중 약 09:00, 표준시 중 약 10:00이다. 주간장 08:30 CT 역시 KST 22:30/23:30이다. 다른 시장을 뜻한다면 시장명이 없어 운영 근거를 재현할 수 없다.

- **[높음] 발행 게이트 규칙이 상충함** — §2.4·§4는 “필수 5종 PASS”를 요구하지만 §3은 “REJECTED만 차단하고 WARNING은 발행”이라고 한다. 필수 항목의 WARNING이 차단인지 허용인지 결정할 수 없다. 또한 §3에서 발행 전제로 둔 `C-08 PASS → C-06 통과` 중 C-06 게이트가 §4 공통 게이트에서 누락됐다.

- **[높음] 목표변수 신선도 5영업일은 주별·일별 산출물의 설명과 충돌함** — 이 조건이면 금요일 정규판이 직전 금요일 수준의 CBOT 자료로도 발행될 수 있다. 따라서 “미국 목요일 마감분 포함”과 일별 경보의 시의성을 보장하지 못한다.

- **[높음] WASDE “정확히 1회” 발행 로직이 정확히 한 번을 보장하지 않음** — `available_at > 직전 스케줄 시각`만 사용하면 지연 수집을 놓친다. 예를 들어 발표시각이 화요일 02:00이고 05:30 실행에서 파일 수집에 실패한 뒤 06:00에 적재되면, 수요일 실행에서는 02:00이 직전 스케줄 05:30보다 이전이므로 영구 누락된다. 반대로 재실행·수동 실행·스케줄 지연 시 동일 구간을 다시 읽어 중복 발행할 수 있다. 처리 완료된 WASDE issue ID 또는 영속 워터마크가 명시되지 않았다.

- **[높음] 모든 parquet 갱신을 새 WASDE 월호로 오인할 수 있음** — `available_at` 신규 발생만으로는 신규 월호, 원천 정정, 재수집, 동일 자료 재적재를 구별하지 못한다. issue month/version 또는 내용 해시 기반 중복 제거가 없어 정정 적재 때 월별판이 추가 발행될 수 있다.

- **[높음] 평일 cron을 “익영업일” 구현으로 간주한 것이 잘못됨** — `cron 0-4`는 월~금만 구별하며 한국·미국 공휴일이나 CBOT 휴장일을 반영하지 않는다. 따라서 §7의 “차기 평일 실행으로 익영업일 충족”은 영업일 정의와 일치하지 않는다.

- **[높음] 금요일과 WASDE 트리거가 겹칠 때 모드 합성 규칙이 없음** — §5는 금요일을 `①+②`, WASDE 익영업일을 `①+③`으로 정의하지만 동일 실행에 두 조건이 참일 때 `①+②+③`인지 한 모드가 다른 모드를 덮어쓰는지 명시하지 않았다. 구현 방식에 따라 주별판 또는 월별판이 누락될 수 있다.

- **[높음] 07:00 이전 발행은 현재 설계로 보장되지 않음** — 05:30 시작 후 분석에 30~60분이 걸리며, §7에는 외부 API가 30분 타임아웃 난 전례도 있다. GitHub Actions 스케줄 자체의 큐 지연까지 있는데 마감 시각용 타임아웃·폴백·SLA 게이트가 없다.

- **[높음] 승인 대상인 월별 심층판과 실제 계산이 불일치함** — §4·§5는 월별 모드에서 horizon×레짐 전면 재산출과 Granger 재검정을 제공한다고 쓰지만, §7.1 #5는 해당 계산이 아직 구현되지 않았다고 인정한다. 후속 M-009 계획은 현재 권고안의 기능 불일치를 해소하지 않는다.

- **[참고] 문서 상태와 변경 이력이 상충함** — 상단은 “승인 대기·구현 착수 금지”인데 §7은 이미 “3계층 구현 diff” 검증을 수행했다고 기록한다. 제안용 diff인지 실제 코드 변경인지 구분되지 않아 승인 전 구현 금지 상태를 확인할 수 없다.

- **[높음] `2σ` 다변수 경보는 예외적 사건 경보가 되지 않을 수 있음** — 정규·양측 기준으로 변수 하나의 `|z|>2` 오경보 확률은 약 4.55%다. 독립 변수 \(m\)개를 동시에 감시하면 하루 하나 이상 경보 확률은 `1−0.9545^m`이며, 20개면 약 60.6%다. 다중검정 보정, 연속 초과 조건, 쿨다운이 없고 `2σ` 초과 자체도 구조적 단절 검정이 아니라 단순 이상치 판정이다.

- **[높음] G2의 “분위수 단순합=완전 순위의존 상계”는 일반적으로 거짓임** — 동일 분위수의 합은 공단조(comonotonic) 가정 아래의 값일 뿐 모든 의존구조에 대한 VaR 상계가 아니다. 예를 들어 각 손실이 4% 확률로 1이면 각 VaR95는 0이고 단순합도 0이지만, 두 손실 사건을 서로 겹치지 않게 배치하면 합계가 8% 확률로 1이 되어 VaR95가 1이 된다. 따라서 “상계”로 표시하면 위험을 과소표시할 수 있다.

- **[높음] 독립 몬테카를로 컨볼루션을 주 밴드로 둔 근거가 없음** — CBOT, basis, 운임, 환율은 공동 시장충격에 노출돼 상관될 수 있다. 고정 시드와 20,000표본은 동일 결과 재실행만 보장하며 독립성 가정의 타당성을 보장하지 않는다. 공분산을 제거한 밴드는 실제 landed cost 꼬리위험을 왜곡할 수 있다.

- **[높음] `SBO−CPO > 175`의 단위와 비교 기준이 없음** — CBOT 대두유는 원천상 cents/lb, CPO는 MYR/ton 또는 USD/ton 등으로 인용될 수 있다. 통화, 중량단위, 선물 월물, 인도조건, 지역 basis를 동일하게 정규화했다는 명세가 없어 `175`를 적용할 수 없다.

- **[높음] 과거 60거래일 분포로 2·4주 기대손익을 계산하는 결함이 미해결 상태임** — 과거 무조건부 분포는 10·20거래일 조건부 기대값이나 예측분포가 아니다. “분포 폭 참고치”로 재명명하는 것은 계산을 수정하지 않으므로, 기대손익·의사결정 델타에 계속 사용되면 수치 해석이 잘못된다.

## ② 시계열 누수 위험

- **[치명] 2010~2025 백필에 데이터 빈티지 관리가 명시되지 않음** — WASDE·PSD·NASS·FAOSTAT·관세청 확정치와 일부 거시계열은 과거 값이 개정된다. 현재 최신 파일로 과거 패널을 재구성하면 당시에는 알 수 없었던 개정값이 과거 학습·Granger·SHAP 계산에 들어간다. WASDE 트리거의 `available_at` 가드는 현재 발행시점만 통제할 뿐, 각 역사 행의 point-in-time vintage를 보장하지 않는다.

- **[치명] 실제 가용시각보다 늦게 확보된 자료를 과거 관측일에 정렬할 위험이 큼** — ERA5-Land D-1 값, 겨울철 05:30 이전 VIX 종가, 목요일 발표 USDM 등을 관측일 기준으로 백필하면 당시 실행 시점에 없던 정보가 들어간다. 각 지표별 `observation_at`과 `available_at`을 분리해 조인한다는 규칙이 없다.

- **[높음] ENSO ONI의 시간 정렬 규칙이 없음** — ONI는 중첩 3개월 평균으로 발표 지연과 개정이 있다. 명목상 중앙월에 바로 붙이면 미래 월 관측을 포함하게 된다. 발표 빈티지와 실제 공개일 기준 시프트가 명시되지 않았다.

- **[높음] 레짐별 중요도에 사후 레짐 누수 가능성이 있음** — 레짐이 전체 표본의 변곡점·군집·향후 수익률을 이용해 사후 라벨링되면 과거 시점의 horizon별 중요도에 미래 정보가 들어간다. 온라인 필터링, 시점별 재추정, 학습 종료시점 고정 규칙이 없다.

- **[높음] 경보 임계값과 SHAP가 과거 전체 표본으로 계산될 가능성을 배제하지 못함** — `2σ`의 평균·표준편차 창, 모델 재학습 종료시점, SHAP background 데이터가 명시되지 않았다. 2010~2025 전체로 임계값이나 모델을 만든 뒤 과거 경보를 백필하면 미래 분포와 미래 학습 데이터가 과거 판정에 유입된다.

## ③ 상관을 인과로 과장한 해석

- **[높음] Elastic Net·SHAP·Granger 결과를 “인과 근거”와 “인과 엣지”로 부르는 것은 과장임** — Elastic Net 계수는 조건부 연관·예측 관계이고, SHAP는 주어진 모델의 출력 배분이며, Granger는 선택된 변수·시차 아래의 선행 예측력이다. 누락변수, 동시성, 비정상성 및 공통충격을 제거한 구조적 인과효과를 식별하지 못한다. 따라서 주간 브리프의 “인과 근거”나 CE 엣지 `validated` 승격 근거로는 부족하다.

- **[높음] “순위가 실제로 움직이는 계기는 월별 발표·레짐 전환·신규 소스뿐”이라는 결론에 인과 근거가 없음** — 제시된 M-006 사례는 “구조적 단절 상태가 반복됐다”는 사용자 불만일 뿐, WASDE 발표가 중요도 순위 변화를 일으켰다는 이벤트 연구가 아니다. 일별 관측 비중이 작다는 사실만으로 특정 이벤트가 순위 변화의 원인이라는 결론을 낼 수 없다.

- **[높음] “WASDE가 대두유 수급의 최대 정보 이벤트”라는 비교 우위가 입증되지 않음** — USDA 주간 수출·재고, NOPA crush, 바이오연료 정책, MPOB 팜유, 환율·원유 충격 등과 비교한 발표일 이벤트 연구, 정보증분, 예측오차 감소치가 없다. 단정적으로 월별 앵커를 정한 근거를 재현할 수 없다.

## ④ 재현 불가능한 주장

- **[높음] 중요도 순위와 안정성 주장을 재계산할 방법이 없음** — Elastic Net의 목표·정규화·시차·교차검증, SHAP 모델과 background, Granger 정상성 처리·최대 시차·다중검정 보정, 세 방법의 순위 통합 가중치, 레짐 정의가 없다. 따라서 “사실상 변하지 않음”, “상위 변수 순위”를 동일하게 재현할 수 없다.

- **[높음] `2σ 등 기존 로직`은 발행 트리거 명세가 아님** — 대상 통계량, 단측/양측, 기준창 길이, 결측 처리, 스케일 변환, 변수별 임계값, 중복 경보 억제 규칙이 없다. 동일 데이터로도 경보 발생 여부가 달라진다.

- **[높음] 교차검증 결과의 장기 재현성이 없음** — run ID와 90일 아티팩트만 있고 검증 대상 commit SHA, 데이터 해시, 전체 프롬프트, 모델 스냅샷, 도구 검색 결과가 없다. `GPT-5.6-Sol` 명칭과 reasoning effort만으로는 모델 갱신 후 동일 검증을 재실행할 수 없으며, 아티팩트 만료 후 근거 자체가 사라진다.

- **[높음] 아카이브 보존기간이 검증 지평보다 짧거나 지나치게 촉박함** — 주별판은 30일 후 삭제되므로 60거래일 의사결정 지평의 사후검증 전에 사라진다. 월별판 90일도 60거래일(통상 약 84~90일) 결과 확정과 거의 동시에 만료된다. Azure Blob 이관 시점·경로·불변성 정책이 확정되지 않아 이관 전 산출물은 장기 감사가 불가능하다.

- **[높음] 비용 절감 주장의 수치 근거가 없음** — 일별 발행 비용, 호출당 토큰·단가, 월별 예상 호출 수, CI-004 당시 잔여 크레딧과 소진 원인이 제시되지 않았다. “일별 과금 때문에 중단 전례”만으로 3계층안의 실제 비용 절감 규모를 재현하거나 비교할 수 없다.
