# GPT 교차검증 — 2026-09-14 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 34910111085

---

### ① 수치·단위·논리 오류

- **[높음] 영업일 결손 계산이 1일씩 과소 계산되고, 월요일 정산가 누락도 주말로 오인한다.**  
  `daily_brief.py`의 `np.busday_count(last_date + 1일, run_date)`가 이미 두 날짜 사이의 영업일 수를 반환하는데, 출력에서는 다시 `_bd - 1`을 사용한다. 예를 들어 직전 가격이 금요일이고 발행일이 화요일이면 `_bd=1`로 월요일 가격이 누락된 상태지만 `_bd <= 1` 때문에 `"주말 휴장"`으로 표시된다. 수요일 발행이면 월·화 2일이 비었는데 `"직전 1영업일 ... 미수집"`으로 과소 표시된다. 기본 `busday_count`는 CBOT 휴일도 반영하지 않는다.

- **[높음] 업로드 계열은 아무리 오래돼도 상단 상태가 녹색 `"주기 내 수집"`으로 표시될 수 있다.**  
  `_freshness_flag(..., upload=True)`는 허용일 초과를 `🚨`가 아닌 `📥`으로 바꾼다. 그런데 브리프의 정상 판정은 `fresh_over == 0 and fresh_missing == 0`만 검사하고 `fresh_upload`는 검사하지 않는다. 따라서 승인자 업로드 파일이 수개월 이상 낡아도 녹색 배지가 나오며, 하단 문구에만 갱신 필요 건수가 노출된다.

- **[높음] 월간 자료의 허용치를 70영업일로 둔 근거가 발행 주기와 맞지 않는다.**  
  WASDE와 ONI의 `45 → 70영업일` 변경은 약 14주, 즉 약 3.2개월이다. 월간 자료가 두 차례 이상 갱신되지 않아도 `"⏳ 주기 내"`로 분류될 수 있다. 주석의 “t월 값이 t+1월 발표”만으로는 70영업일 허용이 도출되지 않으며, 실제 발표일 캘린더나 업로드 SLA에 기반한 계산도 없다.

- **[높음] alert 모드의 과거 지표 기준시각이 실행할 때마다 최신인 것처럼 갱신된다.**  
  `g1_reliability.py`는 이전 JSON이 alert 결과인지 확인하지 않고 `historical`을 다시 이월하면서 `historical_asof = prev["generated_at"]`으로 설정한다. 정규판 D0 → alert D1 → alert D2 순서라면 H1~H5 값은 계속 D0 값인데 `historical_asof`는 D1, 이후 D2 직전 시각으로 전진한다. `prev["historical_asof"]`를 보존하지 않아 노후도가 위장되며, 이월 최대 기간도 없다.

- **[높음] 숫자 추출기의 날짜 차단과 범위·단위 검증이 불완전하다.**  
  `_extract_value()`는 키 세그먼트의 첫 숫자를 그대로 반환한다. `ACTUAL: September 11 revised to 3.2 MMT`에서는 캡처 값이 `11`인데, 날짜 정규식은 숫자 `"11"`만 `fullmatch`하므로 월 정보가 없어 날짜로 인식하지 못한다. 또한 `SURPRISE_SCORE: 11`도 요구 범위 `[-1, 1]` 검증 없이 11로 저장한다. 값 뒤 단위도 버리므로 동일 수량이 `0.032 billion tonnes`와 `32 MMT`처럼 표현되면 변환 없이 서로 다른 값이 된다.

### ② 시계열 누수 위험

- **[참고] 미래 `available_at` 검사가 관측시점과 무관하게 완화됐다.**  
  `test_pipeline_quality.py`는 `event_time` 또는 `price_date`와의 관계를 확인하지 않고, indicator의 `lag_days`만으로 `today + lag_days`까지 허용한다. 따라서 오래된 관측치라도 `available_at=내일`이고 lag가 1이면 통과한다. 이는 즉시 누수를 확정하지는 않지만, 해당 테스트가 “현재 시점에 사용 가능한 행만 존재한다”는 as-of 안전성을 더 이상 보증하지 못하며 downstream에서 `available_at <= cutoff` 필터를 빠뜨리면 미래 가용 행이 사용될 수 있다.

- **[참고] 예보 빈티지의 별도 누수 검증 없이 climate forecast 파일을 검증 경로에서 전부 제외했다.**  
  `data_readiness.py`와 `build_feature_mart.py`가 basename prefix만으로 `climate_forecast*`를 무조건 건너뛴다. 이 diff에는 별도 층에서 `발행일·lead·유효일` 관계를 검증하는 대체 게이트가 없다. 향후 별도 소비자가 예보 파일을 사용할 경우 미래 빈티지나 잘못된 lead 정렬이 readiness 및 마트 충돌 검사 모두를 우회한다.

### ④ 재현 불가능한 주장

- **[높음] WASDE surprise 숫자가 원자료로부터 결정적으로 산출되지 않는다.**  
  프롬프트가 LLM에게 `SURPRISE_SCORE`를 직접 부여하게 하고, 실제값과 컨센서스 차이로 점수를 계산하는 공식이 없다. 더구나 대상도 `"ending stocks or production"` 중 하나로 열어 두어 실행별로 다른 지표가 선택될 수 있다. 같은 WASDE 자료라도 모델 응답에 따라 점수와 대상이 달라질 수 있어 시계열 정의 및 재현성이 없다.

- **[높음] “이번 회차 신뢰도” 사용이 보장되지 않는다.**  
  reliability 스텝은 `continue-on-error: true`이고, 실패 시 기존 `data/processed/g1_reliability_latest.json`을 삭제하거나 실패 표식으로 교체하지 않는다. 이후 variable importance/브리프가 그대로 진행되므로 체크아웃된 직전 회차 JSON을 현재 회차 값으로 읽을 수 있다. 주석의 “브리프가 이번 회차 값을 읽게 함”은 실패 경로에서 성립하지 않는다.

- **[참고] 새 CI 의존성이 버전 고정 없이 설치된다.**  
  `openpyxl`, `pyyaml`을 버전 또는 lock file 없이 `pip install`한다. 실행 시점에 따라 설치 버전이 달라져 XLSX 파싱 및 YAML 검증 결과를 동일 커밋에서 재현할 수 없다.
