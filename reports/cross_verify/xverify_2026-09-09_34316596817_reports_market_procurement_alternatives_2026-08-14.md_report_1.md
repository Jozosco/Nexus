# GPT 교차검증 — 2026-09-09 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: reports/market/procurement_alternatives_2026-08-14.md, reports/market/procurement_alternatives_2026-08-19.md, reports/market/procurement_alternatives_2026-08-25.md
- 런: 34316596817

---

## ① 수치·단위·논리 오류

| 심각도 | 대상 | 지적 및 근거 |
|---|---|---|
| **치명** | 2026-08-14 | 현재 도착가 밴드의 상·하단 거리로 “하방 꼬리 우세→대기 여지”를 도출했다. 이 밴드는 2·4주 후 조건부 가격분포가 아니므로 대기 손익을 나타내지 않으며, P10/P50/P90만으로 기대값도 계산할 수 없다. 8/19·8/25 보고서가 동일 해석을 명시적으로 “대기 손익 방향 판단 불가”로 정정했다. |
| **높음** | 2026-08-14 | 커버리지 1개월 연장의 델타로 `1,564 $/MT`를 제시했지만 이는 현 P50 가격 수준일 뿐 연장 여부 간 비용 차이가 아니다. `+206 $/MT`도 P90−P50에 불과해 “기대 최대 보호액”이나 최대손실이 아니다. 금융비·보관비·1개월 후 기대 구매가가 누락됐다. 8/19 이후 보고서도 이를 오류로 인정했다. |
| **높음** | 2026-08-14 | 내재층 `P90−P50=155 $/MT` 전부를 급등 시 “운임 프리미엄”으로 해석했다. 원재료·환율·품질·공분산이 섞인 잔차를 운임으로 귀속할 수 없다. 또한 서로 호환되지 않을 수 있는 basis 최저가와 운임 최저가를 독립 결합하는 방향은 실행 불가능한 합성 최저가를 만든다. 두 문제 모두 8/19 이후 보고서에서 뒤늦게 정정됐다. |
| **높음** | 전체 | “현재 입력값 1.5개월”, spot, flat을 현재 조달상태처럼 사용하지만 입력표는 모두 **사내 실값이 아닌 기본값**이라고 명시한다. 따라서 `권장 하한까지 부족 0.0개월`과 Incoterms 조건은 실제 현황에 대한 결과가 아니라 가상 기본 시나리오 결과다. |
| **높음** | 2026-08-14 | 서로 다른 시장에서 별도로 표준화한 z값 차이의 음수를 “대체유가 SBO보다 약세”로 해석했다. z격차는 가격차나 USD/MT 경제성을 뜻하지 않는다. 8/19 이후 보고서가 이를 명시적으로 정정했다. |
| **높음** | 전체 | `SBO−CPO >175 $/MT`는 단순 FX 환산만으로 판단 가능한 전환 임계가 아니다. CBOT SBO와 CPO 원계열은 인도 장소·품질·계약월·Incoterm이 다르며, 실제 대체비용에는 운임·관세·정제 및 배합수율·품질조정 비용이 필요하다. 현재는 환산 스프레드조차 없는데 임계를 운영 입력값으로 유지하고 있다. |
| **높음** | 전체 | BDI `z=-0.24`만으로 “평시→커버리지 1~2개월”을 도출하는 것은 논리적으로 불충분하다. BDI는 주로 건화물 운임지수로 대두유의 실제 액체화물·탱커·컨테이너 및 특정 항로 운임과 일치하지 않는다. 재고, 수요, 보관·금융비, 실제 화물 견적 없이 최적 커버리지 범위를 정할 수 없다. |
| **높음** | 2026-08-25 | “2020년 이후 최대 +113% 다이버전스”만으로 공행성 붕괴와 평균회귀 가정 금지를 결론 냈다. 단일 최대 편차는 구조적 단절이나 비정상성을 증명하지 않으며, 단위근·공적분·구조변화 또는 레짐 검정 결과가 필요하다. |
| **참고** | 전체 | 표시값 기준 하방 거리는 `1,564−1,290=274 $/MT`인데 세 보고서 모두 `273 $/MT`로 적었다. 따라서 8/14의 비대칭도 표시값 기준 `206−274=-68`이지 `-67`이 아니다. 내부 비반올림 값 사용 여부도 표시되지 않았다. |
| **참고** | 전체 | 서두는 “각 델타가 $/MT 정량값”이라고 선언하지만 커버리지 항목에는 `0.0개월`, 대체유 항목에는 무차원 z격차 또는 미산출 값이 포함된다. 특히 8/19·8/25는 연장 비용 델타가 산출 불가라고 적어 표의 단위 선언과 충돌한다. |
| **참고** | 2026-08-19, 2026-08-25 | 본문은 현재 밴드가 예측이 아니어서 대기 방향을 판단할 수 없다고 하면서, 신선도·한계에는 계속 “①시점 축은 무추세 가정”이라고 적었다. 예측 미제공과 미래 P50 유지 가정이 동시에 남아 있어 해석이 일관되지 않는다. |

## ② 시계열 누수(look-ahead) 위험

| 심각도 | 대상 | 지적 및 근거 |
|---|---|---|
| **높음** | 전체 | 데이터의 **관측일** 외에 실제 공개시각·수집시각·빈티지·의사결정 컷오프가 없다. 특히 관세청 CIF처럼 공표 지연·수정 가능성이 있는 자료와 당일 CBOT/BDI 종가를 역사적 보고서 날짜로 재생성하면, 해당 시점에 알 수 없던 수정치나 장 마감 후 값이 들어갈 수 있다. “동일 일자 보고서 참조”만으로 point-in-time 가용성이 보장되지 않는다. |
| **높음** | 전체 | 90일 z-score의 창 방향, 종료시점, `shift(1)` 여부, 거래일/달력일 처리, 결측치 정렬 및 선물 연속계약 구성법이 없다. 전구간 평균·표준편차나 centered window를 사용했는지 배제할 수 없어 미래값 혼입 여부를 감사할 수 없다. |
| **높음** | 전체 | 대체유 최종 관측은 2026-07-01인데 CBOT z값의 기준일이 표시되지 않았다. 보고서일의 CBOT와 7/1 대체유를 결합했는지, 모두 7/1 기준으로 동기화했는지 알 수 없다. 비동시점 조인은 신호 왜곡을 만들며, nearest/forward 조인 구현에 따라 미래 관측치가 붙는 누수도 발생할 수 있다. |
| **참고** | 전체 | `z>1`, `z>2`, `175 $/MT` 임계의 선정 기간과 학습/검증 컷오프가 없다. 평가기간을 본 뒤 임계를 조정했다면 후견편향 및 파라미터 누수가 되지만 현재 문서로는 이를 배제할 수 없다. |

## ③ 상관을 인과로 과장한 해석

| 심각도 | 대상 | 지적 및 근거 |
|---|---|---|
| **높음** | 전체 | CE-ID와 ontology 전파 경로를 “인과 근거”로 제시하지만, 대상 문서에는 식별전략·통제변수·시차검정·자연실험·효과크기가 없다. BDI, 환율, 정책, 유지 스프레드 간 서술적 연결이나 ontology edge는 현재 조달비용에 대한 인과효과의 증거가 아니다. |
| **높음** | 2026-08-14 | 원재료·FX·품질 등이 혼재된 잔차 분위 차이 `155 $/MT`를 운임 급등이 신규 CFR 호가에 가산하는 금액으로 귀속했다. 이는 관측되지 않은 혼합 변동을 운임 원인 하나에 배정한 인과 과장이다. |
| **높음** | 전체 | “2021~22 레짐 악화 시 커버리지 연장이 대응 1순위였음”이라는 과거 행동 기록을 현재 권고 근거로 사용한다. 당시 연장이 비용 또는 위험을 실제로 줄였다는 반사실 비교가 없으므로, 과거에 채택된 행동이라는 사실만으로 최적 대응임을 입증하지 못한다. |
| **참고** | 전체 | `CE-002(B40→스프레드 축소)` 등 방향성 문구는 정책과 스프레드의 동시변화를 인과효과처럼 표현하지만, 생산량·원유가·환율·수출정책 등 공통 원인 통제가 제시되지 않았다. |

## ④ 재현 불가능한 주장

| 심각도 | 대상 | 지적 및 근거 |
|---|---|---|
| **높음** | 전체 | `BDI z=-0.24`, `CBOT z=-0.70`, 대체유 z격차 `+1.05/+0.46/+1.00`의 원천 시계열 ID, 계약월/롤 규칙, 가격·수익률 선택, 표준편차 방식, 기준일이 없다. “90일”만으로는 같은 값을 재계산할 수 없다. |
| **높음** | 전체 | 도착가 `[1,290, 1,770]`, P50 `1,564`, 내재층 `P90−P50=155`의 계산식·구성항목·가중치·데이터 스냅숏 해시가 대상 문서에 없다. 특히 `155`가 전체 밴드 상단 거리 `206`과 다른 이유를 검증할 수 없다. |
| **높음** | 전체 | 8/14·8/19·8/25의 P50, 밴드, BDI z 및 모든 z격차가 동일하게 복제됐다. 대체유는 7/1 고정임이 표시되지만 P50과 BDI에는 as-of 시각이 없어 실제 날짜별 재수집 결과인지 단순 copy-forward인지 판별할 수 없다. |
| **높음** | 전체 | `1~2개월 권장`, BDI 레짐 임계 `z>1/z>2`, 전환 임계 `175 $/MT`의 목적함수, 추정 표본, 손실함수 및 검증 성과가 없다. 특히 175는 “통계 검증 대기”라고 적혀 있어 정량 운영 파라미터로서 근거가 재현되지 않는다. |
| **높음** | 전체 | “GPT 교차검증 부호 정정”은 모델명·버전·프롬프트·응답·검증 데이터가 없고, 통계적 교차검증도 아니다. 부호 정정의 절차와 정확성을 재현할 수 없다. |
| **높음** | 2026-08-25 | farmdoc의 “최대 +113%”는 정확한 문서·표·기간·분모·스프레드 산식이 없다. 가격비율인지 상대 스프레드인지조차 정의되지 않아 수치와 “공행성 붕괴” 결론을 재현할 수 없다. |
| **참고** | 전체 | “FX parquet 부재—CI 재실행 시 산출”이라고만 되어 있고 FX 공급자, 환율 방향, 기준시각, 휴일 정렬, 코드 경로·커밋·데이터 버전이 없다. CI 재실행이 동일 USD/MT 결과를 낸다는 주장을 검증할 수 없다. |
