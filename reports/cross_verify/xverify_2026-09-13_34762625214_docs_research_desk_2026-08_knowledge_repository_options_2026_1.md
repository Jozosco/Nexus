# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/knowledge_repository_options_2026_08_25.md
- 런: 34762625214

---

## ① 수치·단위·논리 오류

- **[높음] 문서 규모 `~5천`의 산정 단위가 일관되지 않는다.**  
  실제 적재 대상으로 제시한 항목은 요약 2,175건 + 결정 300여 건 + research_desk 66건 + 용어 152건으로 약 2,693개 레코드다. `~5천`은 원문 PDF까지 별도 문서로 중복 계산해야 근접하지만, 구조도에는 PDF 원문 적재가 없다. 인덱스 CSV 2,227행까지 문서 수로 더했다면 동일 자료를 중복 집계한 것이다.

- **[높음] `요약 2,175건이 원문과 1:1`이라는 주장과 인덱스 2,227행 사이의 52행 차이가 설명되지 않는다.**  
  중복 원문, 요약 누락, 다중 행 매핑 여부가 없어 “원문↔요약을 한 테이블로 조인 가능” 및 전체 커버리지를 보장할 수 없다.

- **[높음] 제안 스키마로는 EvidenceSpan의 `page+원문 인용 100% 일치`를 보장할 수 없다.**  
  스키마에는 문서 ID·날짜·본문·출처 경로만 있고 페이지, 원문 오프셋, 원문 해시, 청크 ID가 없다. PDF 원문도 적재 대상에서 빠져 있으며 MEMORY 결정과 용어 레코드는 애초에 페이지가 없을 수 있다. 성문화된 계약이 있다는 것과 검색 결과에서 자동 강제된다는 것은 별개다.

- **[높음] `Phase 1→2는 같은 스키마라 재작업 없음`은 구조도와 모순된다.**  
  Phase 2에는 임베딩 컬럼 추가가 필요하고, 최대 438KB 문서에서 유효한 발췌를 검색하려면 문서 단위가 아닌 청크 단위 스키마와 위치 정보가 필요하다. 문서 단위 TF-IDF에서 청크 단위 임베딩 검색으로 전환하면 색인·랭킹·인용 로직이 바뀐다.

- **[높음] FAISS·Chroma·LanceDB를 모두 `벡터 DB 신설/별도 인프라`로 분류한 것은 부정확하다.**  
  FAISS는 로컬 라이브러리이며 Chroma와 LanceDB도 임베디드 방식으로 운영할 수 있다. 승인 라이브러리 여부와 별개로, 이들을 외부 SaaS형 신규 인프라와 동일시한 비교 논리는 성립하지 않는다.

- **[높음] 외부 SaaS의 `잔존자산 0·이관 시 승계 불가`는 검증 없이 단정한 값이다.**  
  Notion·Airtable에는 내보내기 경로가 있으므로 0이라고 할 수 없다. 실제 승계율은 본문·첨부·관계·버전 이력별 export/import 시험으로 산정해야 한다.

- **[높음] `비용 0`, `이식성 100%`, `잔존자산 100%`는 범위가 정의되지 않은 절대값이다.**  
  반나절 인건비, 빌드·CI·저장 비용은 0이 아니다. 또한 parquet 파일의 이동 가능성과 Snowflake/Azure에서 동일한 TF-IDF·임베딩 검색 동작을 승계하는 것은 다르며, 변환·호환성 시험이 제시되지 않았다.

- **[높음] HF egress 상태가 서로 모순된다.**  
  결정 요청에는 `(기승인)`이라고 쓰면서 로드맵과 같은 문장에서 `방화벽 승인 후`라고 명시한다. 신청서 반영 완료, 조정자 승인, 방화벽 승인 중 어느 단계가 완료됐는지 구분되지 않는다.

- **[높음] `생성형 환각 리스크를 구조적으로 피한다`는 결론은 제시된 구조로 보장되지 않는다.**  
  종합 서술을 기존 에이전트가 수행하면 검색 결과를 왜곡하거나 근거 없이 보충할 위험은 그대로 남는다. 인용 강제 validator나 생성 결과의 entailment 검사가 구조에 없다.

- **[참고] `교차 도메인 질의의 수행 경로가 없다`는 표현은 과장이다.**  
  보고서 스스로 grep과 CSV 필터가 존재한다고 적었다. 없는 것은 통합 의미 검색·랭킹 경로이지, 교차 디렉터리 검색 경로 자체가 아니다.

## ② 시계열 누수 위험

- **[치명] 시점 정합 검색을 위한 `available_at`가 없다.**  
  제안 스키마의 단일 `날짜`로는 원문 발행일, 요약 작성일, 저장소 유입일, 수정일을 구분할 수 없다. 과거 조달 의사결정이나 백테스트에서 이후 작성된 요약·MEMORY 결정·research_desk 정오표를 검색하면 미래 정보가 유입된다.

- **[치명] TF-IDF 학습 범위와 메타 필터 적용 순서가 정의되지 않았다.**  
  전체 저장소로 vocabulary와 IDF를 산출한 뒤 날짜로 결과만 필터링하면, 미래 문서의 단어 빈도가 과거 시점 랭킹에 반영된다. 과거 시점별 동결 색인 또는 cutoff 이전 문서만으로 fit하는 규칙이 필요하다.

- **[치명] 시맨틱 용어·동의어·인과엣지에 버전 및 효력 시작일이 없다.**  
  나중에 추가된 용어와 관계를 과거 질의 확장에 사용하면 검색 문서가 과거 당시 이용 가능했던 지식과 달라진다. 임베딩 모델도 모델명·revision·출시일이 없어 과거 백테스트에서 당시 존재하지 않은 모델을 사용할 위험이 있다.

## ③ 상관을 인과로 과장한 해석

- **[높음] `인과엣지 21건`을 검증된 자산처럼 취급하지만 인과 판정 기준이 없다.**  
  시간적 선후, 교란 통제, 식별 전략, 근거 등급이 제시되지 않아 단순 동시 발생·서술상 연관을 인과로 명명했는지 감사할 수 없다. 이를 질의 확장에 사용하면 검증되지 않은 인과 프레임이 검색 결과에 주입될 수 있다.

- **[참고] 의미 검색 부재 때문에 “세션마다 같은 조사를 반복하는 비용이 이미 발생”한다고 결론 내릴 근거가 없다.**  
  검색 기능 부재와 조사 반복 사이의 인과는 세션 로그, 중복 질의율, 소요시간 비교 없이 입증되지 않았다. 반복 원인은 인수인계·절차·품질 문제일 수도 있다.

## ④ 재현 불가능한 주장

- **[높음] `조사 실측` 값의 재현 정보가 없다.**  
  저장소 commit/hash, 파일 glob, 제외 규칙, 중복 제거 기준, 실행 명령과 결과물이 없어 2,175건·2,227행·66건·`검색 코드 0건`을 같은 방식으로 재산출할 수 없다.

- **[높음] 검색 방법의 핵심 파라미터가 정의되지 않았다.**  
  TF-IDF의 토크나이저, 언어 정규화, n-gram, min/max_df, stopword, 문서/청크 단위, 동점 처리와 top-N이 없다. Phase 2도 sentence-transformers 모델명, revision/hash, 차원, pooling·정규화 방식이 없어 검색 결과를 재현할 수 없다.

- **[높음] 권장안의 품질 우위를 뒷받침하는 평가가 없다.**  
  고정 질의 세트, 정답 문서, Recall@k·MRR·nDCG, 인용 정확도, 지연시간·메모리 측정 없이 TF-IDF가 충분하고 벡터 엔진은 과잉이라고 판정했다. `RAG의 절반 완성`, 동의어 확장으로 약점을 보완한다는 표현도 측정 기준이 없다.

- **[높음] `NotebookLM은 API도 없음`은 제품 범위와 확인 시점이 없는 절대 주장이다.**  
  일반판·기업판, 관리 API·질의 API 중 무엇을 뜻하는지 구분하지 않았고 외부 확인 자료와 확인일도 없다. 변경 가능한 제품 사양을 내부 ID `LLM-004`만으로 확정 배제 근거로 사용할 수 없다.

- **[참고] Phase별 `~반나절` 추정은 재현 가능한 산정치가 아니다.**  
  담당 인력, 하드웨어, 청크 수, 색인 시간, 테스트·문서화·오류 처리 범위 및 완료 조건이 없다. 특히 Phase 2는 모델 다운로드와 전 문서 임베딩 비용조차 산정되지 않았다.
