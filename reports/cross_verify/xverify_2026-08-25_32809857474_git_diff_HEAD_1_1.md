# GPT 교차검증 — 2026-08-25 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 32809857474

---

1. **높음 — 시계열 누수 위험**  
   후보에 `event_date`만 기록하고 `published_at`, `retrieved_at`, `extracted_at`, `reviewed_at` 같은 가용 시점이 없다. 이후 수동 검토로 `review_status`가 변경된 최신 파일을 백테스트가 읽으면, 사후 검토 결과가 과거 `event_date`부터 알려졌던 것처럼 사용될 수 있다. `extracted`로 최초 저장한다는 제약만으로는 point-in-time 누수를 막지 못한다.

2. **높음 — 수치·단위 검증 부재**  
   `_CANDIDATE_RULES`는 `2`, `60`, `200`을 단위·범위·스케일 버전 없이 직접 비교한다. 특히 `US_CHINA_TARIFF_STATUS` 같은 상태 코드를 `>= 2`로 비교하려면 코드가 순서형이라는 계약이 필요하지만 제시되지 않았다. 출력 JSON에도 `unit`이나 `scale`이 없어 동일한 값이 지수·백분율·등급 중 무엇인지 재현할 수 없다. `"스킬 정합"`이라는 주석 역시 기준 출처나 버전을 제공하지 않는다.

3. **높음 — 증거를 독립적으로 재현·검증할 수 없음**  
   `document_id`는 `daily_digest_날짜`로 합성되고, 실제 원문 URL·파일 경로·콘텐츠 해시·수집 시각이 없다. `page: 1`도 이 변경에서 해당 문서나 페이지 산출물을 생성한다는 근거 없이 고정된다. `exact_quote`는 원문이 아니라 `note`의 앞 300자일 수 있어, 후보를 원출처와 대조 검증할 수 없다.

4. **높음 — 동일 날짜 재실행 시 후보 및 검토 결과 덮어쓰기**  
   `write_text()`가 `candidates_{date}.json`을 병합 없이 전면 덮어쓴다. 같은 날 워크플로 재실행 시 이전 후보가 사라질 수 있고, 해당 파일에 수동 검토 상태나 증거가 추가돼 있었다면 다시 `review_status: extracted`로 덮어써진다. 또한 `event_id` 기준 중복 제거도 없어 동일 날짜·지표 행이 중복되면 같은 ID가 여러 번 생성된다.

5. **높음 — C12가 주장한 ‘아카이브 정합성’을 실제로 검사하지 않음**  
   `check_daily_codes()`는 `data/processed/unstructured_daily_signals.csv`를 읽지 않고 현재 코드의 `DAILY_UNSTRUCTURED` 상수만 비교한다. 따라서 아카이브에만 남은 과거 지표, 런타임에 실제 산출됐지만 상수에 없는 지표, 코드 변경으로 제거된 지표를 검출하지 못한다. 함수 설명의 “일별 아카이브 지표 정합”과 구현이 불일치한다.

6. **높음 — 환경에 따라 C12 검사가 생략돼도 게이트가 통과 가능**  
   `daily_unstructured_digest` 또는 그 의존 모듈 임포트가 실패하면 위반이 아니라 `warning`만 남기고 교차 검사를 전부 생략한다. 동일 커밋도 설치된 의존성에 따라 C12 위반 검출 여부가 달라져 검증 결과가 재현되지 않으며, 잘못된 `daily_codes`가 통과할 수 있다.

7. **참고 — 비유한수 입력이 잘못된 후보·비표준 JSON을 만들 수 있음**  
   `float()` 후 `math.isfinite()` 검사가 없다. 예를 들어 `"Infinity"`는 변환에 성공하고 모든 `>=` 임계를 통과한다. 이후 `json.dumps()`는 기본 설정에서 `Infinity`를 기록하는데, 이는 표준 JSON 값이 아니다. 반대로 단위가 붙은 `"200%"` 등은 경고 없이 후보에서 누락돼 파싱 실패와 임계 미달을 구분할 수 없다.

8. **참고 — 임계 ‘초과’ 설명과 경계 조건 불일치**  
   주석과 로그는 “임계를 넘으면/임계 초과”라고 하지만 실제 판정은 모두 `>=`다. 값이 정확히 `2`, `60`, `200`인 경우 설명상 초과하지 않았는데도 후보가 생성된다.

9. **참고 — 후보 파일 스테이징 실패가 조용히 은폐됨**  
   `git add ... "data/semantic/events" 2>/dev/null || git add ...csv`는 이벤트 디렉터리 추가가 어떤 이유로든 실패하면 오류를 숨기고 CSV만 스테이징한 뒤 성공 처리한다. 이 경우 스크립트는 후보 생성 완료를 출력하지만 후보 파일은 커밋·보존되지 않아 실행 결과를 재현할 수 없다.
