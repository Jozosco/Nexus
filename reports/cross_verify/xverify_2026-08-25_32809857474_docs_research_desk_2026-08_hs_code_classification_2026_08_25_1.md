# GPT 교차검증 — 2026-08-25 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/hs_code_classification_2026_08_25.md
- 런: 32809857474

---

## ① 수치·단위·논리 오류

- **[높음] HSK 연도 버전과 누적기간이 불일치한다.**  
  코드 근거는 HSK2021~2024 양허표인데 물량은 2010~2026 누적이다. HSK 10단위는 연도별 신설·폐지·품명 변경 가능성이 있으므로, 연도별 세번표와 코드 변환표 없이 동일 코드의 17년 물량을 합산하면 서로 다른 품목 범위를 이어 붙일 수 있다. 현재 자료로는 “확정 6코드”나 “6단위=10단위”가 전 기간에 성립한다고 볼 수 없다.

- **[높음] 데이터 존재만으로 코드의 공식 유효성을 입증했다는 논리가 성립하지 않는다.**  
  `1507901090`·`1507909000` 명칭의 저장 데이터가 있다는 사실은 파일 라벨, 과거 세번 또는 API 집계 결과가 존재한다는 뜻일 뿐, 해당 코드가 적용 연도의 공식 HSK였고 API가 정확히 그 10단위를 필터링했다는 증거는 아니다. A-028을 무효화하려면 연도별 공식 세번표와 원 API 응답이 필요하다.

- **[높음] API의 필드 길이와 헬스체크 성공을 ‘정확한 10단위 조회 지원’의 증거로 사용했다.**  
  `hsSgn` 크기 10과 10자리 샘플은 입력 형식을 보여줄 뿐, 서버가 10자리를 그대로 필터링하는지, 6자리로 절단·집계하는지까지 입증하지 않는다. `1507901010` 호출 성공도 HTTP 성공인지 해당 세번만의 결과인지 불명확하다. 인접 10단위 코드별 결과와 6단위 합계의 일치 검증이 없다.

- **[높음] 1개 연도 API 프로빙으로 코드 실존과 ‘무역 부재’를 판정할 수 없다.**  
  유효한 코드라도 해당 연도 거래가 없으면 응답이 없을 수 있고, 반대로 폐지·오입력 코드가 과거 매핑이나 상위단위 집계로 응답될 수도 있다. 따라서 “코드별 1년 샘플”은 코드 유효성, 0거래, 미보고를 구분하지 못한다.

- **[높음] 4개국 합계 차액을 베트남·말레이시아 물량으로 귀속한 것은 근거가 없다.**  
  통합본과 US·AR·BR·CN 합계의 차이는, 집계 조건이 같다는 전제에서 ‘그 밖의 모든 국가’ 물량만 뜻한다. 이를 “베트남·말레이시아 등에 귀속”할 수는 없다. 특히 베트남 27.5%는 MY24/25 전체 대두유 자료인데, 차액은 2010~2026 누적 및 특정 10단위 코드이므로 기간과 품목 범위도 다르다.

- **[높음] HS6 Comtrade 합계로 특정 HSK10 코드의 물량·국가 구성을 입증했다.**  
  `1511.90`에는 `-1000`, `-2000`, `-9000`이 모두 포함되고, `1512.19`와 `1514.19`도 복수의 10단위로 갈린다. 따라서 HS6 기준 62.5만t·4.4만t·1.3만t만으로 `1511.90-1000/-9000`, `1512.19-1010`, `1514.19-1000` 각각의 상위 원산국을 확정할 수 없다. 국가별 HSK10 원자료를 제시하지 않았다.

- **[높음] `1511.90` 수집으로 “SBO−CPO 스프레드”를 실측한다는 설명은 비교 대상이 다르다.**  
  CPO는 조유 코드 `1511.10`이고, `1511.90`은 팜 올레인·스테아린·RBD 등 비조유 제품이다. `1511.90` 수입단가에는 정제·분획·운임·제품 믹스가 포함되므로 이를 CPO 가격으로 간주하면 스프레드가 잘못 정의된다.

- **[높음] `1514.11-0000` 조유를 “압착용”이라고 한 것은 공정 방향이 반대다.**  
  유채씨를 압착해 조유를 생산한다. 이미 조유인 `1514.11`은 통상 정제 공정의 투입물이지 압착 공정의 투입물이 아니다. “CJ 겸용 설비”가 압착 설비인지 정제 설비인지도 근거가 없다.

- **[참고] 반올림된 수치로 물량 100%를 단정했다.**  
  말레이시아 39.6만t과 인도네시아 22.9만t은 표시 정밀도상 합계 62.5만t이지만, 반올림 때문에 소량의 제3국 물량이 존재할 수 있다. 원시 수량 없이 “100%” 및 “10개국 일괄은 낭비”를 단정할 수 없다.

- **[참고] 21kg을 ‘무역 부재’로 표현했다.**  
  21kg은 비록 경제적으로 미미해도 수치상 0이 아니다. 최소 유의물량 기준을 정의하지 않은 상태에서 무역 부재로 분류하면 문서의 “0 보존 vs 미보고 구분” 원칙과도 충돌한다.

## ② 시계열 누수 위험

- **[높음] 코드·국가 선택 자체가 미래 전체기간 정보를 사용한다.**  
  필수 코드와 수집국 우선순위를 2010~2026 누적량, 2024 Comtrade, 2026-03 FAS 순위로 선정했다. 이 목록을 2024년 이전 예측 백테스트에도 고정 적용하면, 당시 알 수 없었던 향후 공급국 순위와 품목 중요도를 이용한 사후적 변수·유니버스 선택 누수가 발생한다. 예측 기준일별 당시 이용 가능 자료로 목록을 재구성하거나, 해당 선택을 백테스트 성과와 분리해야 한다.

## ③ 상관을 인과로 과장한 해석

- **[높음] 무역량·원산국 순위만으로 경제적 전달경로를 확정했다.**  
  `1512.19-1010`의 “흑해 대체 경로”, `1514.19-1000`의 “EU 바이오디젤 연동”, `1201.90-1000`의 “국내 crush 직결”은 코드 분류와 수입량만으로 입증되지 않는다. 대체 전후 가격·물량의 시차 분석, 정책 충격, 용도별 실제 투입자료가 없으므로 현재 근거로는 가설 수준이다.

## ④ 재현 불가능한 주장

- **[높음] 핵심 수치의 산출 경로가 누락되어 있다.**  
  “저장소 GW xlsx 직접 판독”만 있고 파일 경로·커밋 해시·시트·셀 범위·중복 처리·결측 처리·국가코드 기준·2026년 최종 포함 월이 없다. 특히 2026년은 부분연도일 가능성이 큰데 컷오프가 명시되지 않아 2010~2026 누적량과 점유율을 동일하게 재계산할 수 없다.

- **[높음] 외부 출처가 코드별·수치별로 연결되어 있지 않다.**  
  §7은 여러 연도의 FTA 양허표를 일괄 나열할 뿐 문서명·버전·표·페이지·행을 제시하지 않는다. Comtrade도 reporter, flow, partner, classification, commodity level, net weight/quantity 필드, API URL과 조회일이 없고, FAS도 보고서 식별자와 원표 위치가 없다. 따라서 “전 코드 출처”, 베트남 27.5%, 팜유 62.5만t, 해바라기유 4.4만t 등의 주장을 독립 재현할 수 없다.
