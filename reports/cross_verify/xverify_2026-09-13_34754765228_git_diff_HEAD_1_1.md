# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 34754765228

---

- **높음 — 논리 오류·재현성:** 다중 검색어 폴백이 실제로 모두 실행되지 않는데 보고서에는 모두 시도한 것으로 기록된다. `discover_symbols()`는 `found`가 생기면 즉시 `break`하지만, `run()`은 `search_terms_of()`가 반환한 전체 검색어를 `tried_terms`로 저장한다. 예를 들어 첫 검색어 `ttf`가 스냅샷이 비어 있는 기존 `TTF:COM`만 반환해도 나머지 `eu natural gas`, `dutch ttf`는 호출되지 않지만 보고서에는 세 검색어 모두 “시도”한 것으로 표시된다. 운영자가 폴백을 모두 소진했다고 오판할 수 있다.

- **높음 — 논리 오류:** 검색 결과의 `:COM/:IND` 필터링보다 `if found: break`가 먼저 실행된다. 첫 검색어가 이름 조건에는 맞지만 `:NL` 같은 제외 대상 심볼만 반환하면 이후 검색어와 `/markets/commodities` 폴백을 시도하지 않고, 루프 종료 후 해당 결과를 전부 제거한다. 추가 테스트는 두 번째 검색어에 유효한 `STG:COM`이 함께 있는 경우만 검증하여 이 실패 경로를 포착하지 못한다.

- **높음 — 품목·단위 혼입 위험:** `kw_hit or term_hit` 때문에 넓은 폴백 검색어가 원래 품목 키워드를 우회한다. 특히 Drewry WCI의 `"container"`는 이름에 container만 있으면 통과하므로 별도 등록된 `Containerized Freight Index`의 `CFI:IND`도 WCI 후보가 될 수 있다. Drewry WCI와 일반 컨테이너 운임지수는 서로 다른 지표이며, 통상 USD/40ft 컨테이너와 지수 포인트처럼 단위·산식도 다를 수 있다. `"phosphate"` 역시 DAP가 아닌 다른 인산염 품목을 허용한다. 응답의 품목 식별자·단위·통화를 검증하지 않아 잘못된 계열이 XLSX에 조용히 적재될 수 있다.

- **참고 — 재현 불가능한 주장:** 주석의 “2026-09-13에 `TTF:COM`, `WCI:IND`, `DAP:COM`이 빈 스냅샷을 반환했고 단일 검색 결과가 필터 후 0건이었다”는 주장에 당시 요청 URL, 계정/플랜, 응답 코드·본문 등의 증거가 없다. 추가 테스트도 임의의 `Test Gas`와 완전히 모킹된 응답만 사용하므로 실제 Trading Economics 동작을 입증하지 못한다. 라이브 검색 결과는 변경될 수 있어 현재 diff만으로 해당 장애 원인을 재현할 수 없다.
