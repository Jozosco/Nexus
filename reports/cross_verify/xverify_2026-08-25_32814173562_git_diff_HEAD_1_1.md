# GPT 교차검증 — 2026-08-25 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 32814173562

---

- **[높음 | 시계열 누수]** `farmdoc 2025`에서 얻은 2020년 이후의 다이버전스 판단이 `cfg`의 기준일(as-of date) 확인 없이 정적 `guidance`로 항상 반환된다. 따라서 2025년 이전 시점의 백테스트·과거 리포트에도 미래에 발표된 “공행성 붕괴/평균회귀 금지” 정보가 노출될 수 있다. 출처 발행일 이후에만 문구를 활성화하거나 시점별 근거를 버전 관리해야 한다.

- **[높음 | 수치·논리 오류]** “공행성 붕괴”와 “스프레드 평균회귀 가정 금지”는 “2020 이후 다이버전스가 장기화되고 최대 +113%였다”는 사실만으로 도출되지 않는다. 큰 스프레드나 장기 괴리는 공적분 관계의 구조적 붕괴를 입증하지 않으며, 단위근·공적분 및 구조변화 검정과 표본 외 확인이 필요하다. 현재 근거로는 레짐 변화가 아닌 일시적 괴리일 가능성을 배제할 수 없다.

- **[높음 | 재현 불가능한 주장]** `최대 +113%`의 계산 정의가 없다. 분모가 팜유인지 SBO인지, 가격 수준·수익률·비율 중 무엇인지, 현물/선물·만기·통화·단위 환산, 관측 주기, 표본 종료일과 최대 발생일이 모두 누락됐다. 특히 “최대”는 표본 구간에 종속되므로 `farmdoc 2025·A-201`이라는 축약 인용만으로 동일 수치를 재현하거나 검증할 수 없다.

- **[참고 | 수치·단위 오류 위험]** SBO와 팜유는 원자료의 대표 호가 단위가 서로 다를 수 있으므로(예: cents/lb 대 USD/MT) `+113%` 산출 전에 동일 통화·질량·시점 기준으로 정규화했는지 명시해야 한다. 현 문구에는 환산식이나 사용 시계열이 없어 단위 불일치 여부를 판별할 수 없다.
