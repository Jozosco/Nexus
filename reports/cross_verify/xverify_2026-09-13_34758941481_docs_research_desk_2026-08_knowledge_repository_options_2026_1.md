# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/knowledge_repository_options_2026_08_25.md
- 런: 34758941481

---

## ① 수치·단위·논리 오류

- **[높음] 인덱싱 규모가 서로 맞지 않는다.** 권장 구조에 명시된 단위는 `2,175 + 300+ + 66 + 152 = 2,693+`건인데, 벡터 DB 배제 근거에서는 “문서 ~5천”이라고 한다. 약 5천은 원문 PDF 2,175건까지 중복 합산해야 나오지만, 정규화 입력 구조에는 원문 PDF가 포함되지 않는다. 규모 산정의 분모가 일관되지 않는다.

- **[높음] 인덱스와 요약 간 52건 차이가 해명되지 않았다.** 인덱스는 2,227행, 요약은 2,175건으로 52행(인덱스의 약 2.3%) 차이가 난다. 중복·고아 행·비요약 자료 여부와 키 유일성 검사가 없으므로 “원문↔요약이 한 테이블로 조인 가능”하다는 전체 커버리지 주장은 성립하지 않는다.

- **[참고] “RAG의 절반이 이미 완성”은 측정 불가능한 수치다.** RAG 완료율의 분모나 작업분해 기준이 없다. 청킹, passage 검색, 인덱스 빌드, 평가셋, 재랭킹, 인용 검증 등이 빠져 있어 메타데이터 테이블만으로 50%라고 산정할 근거가 없다.

- **[높음] 제시된 스키마로는 EvidenceSpan 계약을 집행할 수 없다.** 저장 필드는 문서 ID·유형·날짜·태그·엔티티·본문·경로뿐이며 `page`, 원문 인용문, 문자 오프셋, `chunk_id`, 원문 해시가 없다. 특히 최대 438KB 문서를 문서 단위 TF-IDF로 검색하면서 페이지별 원문 인용의 “100% 일치”를 검증하거나 정확한 발췌를 생성할 수 없다. 계약 문서의 존재는 자동 준수나 검증을 의미하지 않는다.

- **[높음] FAISS·Chroma·LanceDB를 모두 “별도 인프라”로 분류한 논리가 잘못됐다.** FAISS는 프로세스 내부에서 로컬 인덱스 파일로 사용할 수 있고, Chroma와 LanceDB도 임베디드 배치가 가능하다. 승인 라이브러리 목록 미등재와 별도 서버·인프라 필요 여부는 별개의 판단이다. 규모만으로 “과잉”이라고 결론 내릴 지연시간·메모리·검색 품질 비교도 없다.

- **[높음] “비용 0”은 자체 소요 추정과 모순된다.** Phase 1에 약 반나절의 인건비가 명시되어 있고, 빌드·저장·테스트·유지보수용 컴퓨팅 비용도 존재한다. 입증 가능한 표현은 “신규 라이선스·구독 비용 0” 정도이며 총비용 0이나 “무료”는 아니다.

- **[높음] Parquet의 이식성을 잔존자산·승계율 100%로 등치할 수 없다.** Parquet 데이터가 이동 가능해도 TF-IDF 어휘집·IDF 행렬, 질의 확장 규칙, 모델 파일, CLI, ACL, 경로 규칙 및 임베딩 모델은 Snowflake로 자동 승계되지 않는다. “100%”의 분모와 승계 시험 결과도 없다.

- **[높음] “Phase 1→2는 같은 스키마라 재작업 없음”은 본문과 모순된다.** Phase 2에서 임베딩 컬럼이 추가되며, 재현 가능한 임베딩에는 모델 ID·리비전·차원·정규화 방식도 필요하다. EvidenceSpan을 위해 청킹까지 도입하면 행 단위와 키도 변경된다. 처음부터 해당 필드를 예약하지 않는 한 동일 스키마가 아니다.

- **[높음] 외부 SaaS의 “잔존자산 0”은 계정 비승계와 데이터 가치 부재를 혼동한다.** Notion·Airtable 자료의 CSV/Markdown/API 내보내기, 첨부파일·관계·수식 보존 여부를 시험하지 않았다. 계정이나 워크플로가 승계되지 않는다는 사실만으로 내보낸 데이터 자산까지 0이라고 할 수 없다.

- **[참고] 438KB를 근거로 “전문 로드 불가”라고 단정한 것은 단위가 부적절하다.** 모델 입력 한도는 바이트가 아니라 토큰으로 판정해야 하며, 인코딩·문서 언어·사용 모델의 컨텍스트 길이가 제시되지 않았다.

## ② 시계열 누수 위험

- **[치명] Knowledge Mart에 point-in-time 통제가 없다.** 스키마에는 의미가 불명확한 “날짜” 하나만 있고 `published_at`, `available_at`, `ingested_at`, `revised_at`, `valid_from/to`, 문서 버전 및 질의의 `as_of` 컷오프가 없다. 따라서 과거 시점 예측·백테스트에서 이후 발표된 GAIN/FAO 자료, 사후 정오표, MEMORY 결정 및 research_desk 분석이 검색될 수 있다. “Feature Mart의 시점 정합 격자”와 같다는 설명과 달리 시점 정합을 강제하는 구조가 없다.

- **[높음] 결과만 날짜 필터링해도 TF-IDF 자체에서 미래 정보가 유입될 수 있다.** 전체 문서로 TF-IDF를 적합하면 미래 문서의 문서빈도가 과거 문서의 IDF와 순위를 바꾼다. 현재의 152개 용어·동의어·인과 엣지를 과거 백테스트에 그대로 적용하는 것도 당시 존재하지 않던 지식 구조를 사용하는 누수다. 컷오프별 코퍼스/사전 스냅샷 또는 당시 동결된 인덱스가 필요하지만 설계에 없다.

## ③ 상관·관찰을 인과로 과장한 해석

- **[높음] 검색 코드 부재가 “세션마다 같은 조사 반복”의 원인이라는 주장이 입증되지 않았다.** 세션 로그, 중복 조사율, 소요시간 비교나 대조군이 없다. 반복 비용은 검색 부재 외에도 프롬프트, 인수인계 절차, 문서 품질 또는 포인터 사용 실패로 발생할 수 있다.

- **[높음] 검색과 생성을 분리하면 환각 위험을 “구조적으로 피한다”는 결론은 과장이다.** TF-IDF가 잘못된 문서를 상위 노출하거나 에이전트가 근거 밖 내용을 합성·오인용할 수 있다. 분리는 위험을 낮출 가능성이 있을 뿐, 인용 충실도·근거 정밀도 평가 없이 환각을 방지한다고 단정할 수 없다.

## ④ 재현 불가능한 주장

- **[높음] “조사 실측”과 “검색 코드 0건”을 재실행할 정보가 없다.** Git 커밋/스냅샷, 검색 루트, glob 및 제외 규칙, 파일 형식, 중복 판정, 실행 명령과 출력이 없다. 따라서 2,175건·2,227행·66건·152용어·0건 검색 결과를 동일하게 재현할 수 없다.

- **[높음] TF-IDF 검색 결과를 재현할 사양이 없다.** scikit-learn 버전, tokenizer/analyzer, 대소문자·악센트·숫자 처리, n-gram, stopword, `min_df/max_df`, 정규화, 질의 확장 순서, 어휘집/IDF 저장 방식 및 동점 정렬 규칙이 빠져 있다. 한국어 예시 질의와 문서 언어 분포 사이의 교차언어 처리 방법도 제시되지 않았다.

- **[높음] Phase 2 임베딩은 재생성할 수 없다.** `sentence-transformers`라는 라이브러리명만 있고 모델 ID·HF revision/commit, tokenizer, pooling, 벡터 정규화, 차원, 청크 규칙과 라이선스가 없다. “HF egress 2종”의 정확한 호스트도 없어 동일 벡터 생성 여부를 검증할 수 없다.

- **[높음] “정확한 근거를 빨리 찾는다”와 반나절 구현 추정에 검증 절차가 없다.** 골드 질의·정답 문서, Recall@K·MRR·인용 정확도·검색 지연시간, 하드웨어, 실패 문서 비율 및 작업분해가 없다. 따라서 검색 품질과 두 Phase 각각의 “~반나절” 주장을 독립적으로 재현하거나 검증할 수 없다.

- **[참고] 외부 제품 관련 절대 주장에 시점 고정 근거가 없다.** “NotebookLM은 API도 없음”, “Azure AI Search는 유료”는 제품판·지역·SKU·확인일과 공식 문서 스냅샷이 없다. 변동 가능한 외부 사실이므로 2026-08-25 당시 판정을 재검증할 수 없다.
