# GPT 교차검증 — 2026-08-29 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/g1_publication_schedule_panel_2026_08_14.md
- 런: 33245684148

---

### ① 수치·단위·논리 오류

- **[높음] 분석 기간 산술이 틀림.** `2010-01~2025-12`는 15년이 아니라 **16년·192개월**이다. 15년 창이라면 `2011-01~2025-12` 등 180개월이어야 한다. 따라서 §2.2의 표본 비중과 “15개년 추정창”에 근거한 안정성 해석도 실제 코드의 시작·종료일을 확인하기 전에는 성립하지 않는다.

- **[높음] “신규 1건은 표본의 약 0.025%”를 혼합주기 자료 전체에 일반화함.** 0.025%는 유효 일별 표본이 약 4,000건일 때만 맞는다. 월별 관측 1건은 192개월 기준 약 **0.52%**이며, 60일 선행목표·결측 정렬 후의 실제 표본 수도 제시되지 않았다. 이 비중만으로 순위가 변하지 않는다고 결론낼 수 없고, 고레버리지 충격 한 건은 Elastic Net 계수와 레짐을 크게 바꿀 수 있다.

- **[높음] KST 05:30에 모든 일별 지표의 전일 확정치가 가용하다는 주장이 성립하지 않음.** ERA5-Land 최종 자료는 통상 D+1 자료가 아니며, 준실시간 계열도 수일 지연된다. 또한 미국 표준시 기간 VIX 정규장 마감은 KST 약 06:00, 산출 종료는 약 06:15이므로 05:30 수집으로 미국 목요일 확정치를 포함할 수 없다. 계절별 DST와 공급자별 게시 지연을 반영하지 않은 시간표다.

- **[높음] “시장 개장 KST 07:00”의 대상 시장이 불명확하거나 CBOT 기준으로 틀림.** CBOT 대두유 전자거래 재개는 미국 서머타임 기준 대략 KST 09:00, 표준시에는 10:00이며 주간 세션은 더 늦다. 다른 시장을 의미한다면 시장명과 거래시간 근거가 없어 07:00 전 발행이라는 설계 근거를 검증할 수 없다.

- **[높음] 금요일과 WASDE 트리거가 겹칠 때 발행 모드가 충돌함.** §5.1은 금요일을 `①+②`, WASDE 익영업일을 `①+③`으로 정의하지만 §5.2의 `G1_PUBLISH_MODE`는 `alert/weekly/monthly` 중 하나인 단일 값이다. WASDE 심층판 날짜가 금요일이면 주별판과 월별판 중 어느 쪽이 누락되는지 우선순위나 복합 실행 규칙이 없다.

- **[높음] §5.3과 §7.1의 WASDE 감지 규칙이 서로 다름.** §5.3은 “최신 `available_at`이 당일이면 익영업일 발행”이고, §7.1은 “직전 스케줄 실행 이후 새 `available_at` 발생 시 차기 실행에서 발행”이다. 앞 규칙이 폐기됐다고 본문에 표시되지 않아 구현 기준이 둘이다.

- **[높음] `available_at` 시간창 비교만으로는 “정확히 1회”가 보장되지 않음.** 워크플로 재실행은 같은 이벤트를 중복 발행할 수 있고, 장애 후 늦게 적재된 데이터의 `available_at`이 이미 직전 실행보다 과거이면 아예 누락된다. WASDE 정정본은 반대로 월중 재발행을 일으킬 수 있다. 보고서 판/릴리스 ID를 저장한 영속적 멱등 키와 원자적 발행 상태가 필요하다.

- **[높음] “익영업일”이 구현되지 않음.** UTC 일~목 cron은 KST 월~금만 구분할 뿐 한국·미국 공휴일을 처리하지 않는다. 공휴일에도 발행될 수 있고, 어느 국가의 영업일인지도 정의되지 않았다.

- **[높음] 월별판의 약속된 내용과 현재 계산이 불일치함.** §4·§5는 월별 모드에서 horizon×레짐 전면 재산출을 한다고 명시하지만 §7.1 #5는 해당 기능이 아직 없다고 인정한다. M-009 완료 전 월별판 발행을 차단하는 의존성이나 게이트가 없어, 명칭만 “심층판”인 불완전 산출물이 발행될 수 있다.

- **[높음] WASDE 하나만으로 월별 전면 재산출을 트리거하면 다른 월별 자료가 최대 한 달 늦게 반영됨.** PSD·GATS·KOSIS·관세청·FAO AMIS·ONI 등이 WASDE 이후 갱신돼도 별도의 심층 재산출 트리거가 없다. “월별 발표가 순위를 움직인다”는 전제와 구현이 맞지 않는다. 하드코딩 캘린더 폴백은 parquet 미갱신 시 전월 WASDE로 심층판을 발행할 위험도 있다.

- **[높음] `2σ` 초과를 “구조적 단절”로 부르는 것은 통계적으로 부정확함.** 단순 z-score 초과는 이상치일 수 있을 뿐 모수나 분포의 구조적 변화 검정이 아니다. 정규 가정에서 양측 2σ 초과 확률은 변수당 약 4.55%이며, 독립 변수 20개를 매일 검사하면 하나 이상 초과할 확률은 약 **61%**다. 기준창·단측/양측·다중검정 보정·최소 지속기간이 없다.

- **[높음] 목표변수 신선도 5영업일 게이트가 일별·주별 발행 목적과 충돌함.** 금요일 보고서가 직전 금요일 수준의 `CBOT_BO_CLOSE`만 있어도 통과할 수 있어 “전주 대비 변동”과 현시점 SHAP 설명을 새 목표 관측 없이 발행할 수 있다.

- **[높음] G2 대체유 임계값 `SBO−CPO > 175`는 차원이 정의되지 않음.** CBOT SBO는 통상 센트/파운드, CPO는 MYR/톤 또는 USD/톤으로 인용된다. 통화·중량 환산, 현물/선물, 인도조건, 계약월을 명시하지 않으면 뺄셈과 `175` 임계값 자체가 재현 불가능하다.

- **[높음] “완전 순위의존 분위수 합은 스트레스 상계”라는 표현은 일반적으로 거짓임.** VaR/분위수는 비가법적이어서 comonotonic 합이 모든 의존구조에 대한 분위수 상계가 되지 않는다. 예를 들어 각각 4% 확률로 100의 손실을 갖는 두 변수를 완전동조시키면 95% 분위수 합은 0이지만, 손실 사건을 서로 겹치지 않게 배치하면 합계의 95% 분위수는 100이 될 수 있다.

- **[높음] 독립 몬테카를로 컨볼루션을 주 밴드로 쓰는 근거가 없음.** SBO 가격·베이시스·운임·환율은 독립이라고 보기 어렵고, 관세와 환율은 landed cost에 곱셈 형태로 들어갈 수 있다. 독립성과 단순 가법성이 검증되지 않으면 20,000회 시뮬레이션을 해도 합계 분위수는 잘못될 수 있다.

- **[높음] 과거 60거래일 무조건부 분포는 2·4주 “기대손익”을 산출하지 못함.** §7.1 #12에서 한계가 인정됐지만 모델 교체는 후속으로 남아 있다. 이를 “분포 폭 참고치”로 재명명해도 horizon별 조건부 기대값이나 예측 분위수가 되는 것은 아니다.

- **[참고] `CBOT ZL 정산가`와 목표변수 `CBOT_BO_CLOSE`가 혼용됨.** 공식 정산가와 마지막 거래가/종가는 다를 수 있다. 내부 변수명이 정산가를 뜻하는지 별도 데이터 사전이 없으므로 목표 정의가 모호하다.

### ② 시계열 누수 위험

- **[치명] 2010~2025 백필에 point-in-time 빈티지 사용이 명시되지 않음.** WASDE·PSD·NASS·FAOSTAT·관세청 확정치 등은 과거 수치를 수정한다. 현재 최신값을 과거 행에 덮어쓴 뒤 당시 예측에 사용하면 미래 개정정보가 유입되어 Elastic Net, SHAP, Granger 결과 전체가 오염된다. 라이브 시점의 `latest ≤ now` 검사는 각 역사적 행에서 당시 이용 가능했던 빈티지를 사용했다는 보장이 아니다.

- **[높음] `available_at`의 시간대와 정밀도가 정의되지 않아 미래 발표를 조기 인식할 수 있음.** WASDE의 미국 동부시간 12:00 발표는 서머타임 기준 다음 날 KST 01:00이다. `2026-08-12` 같은 날짜값이나 naive timestamp로 저장하면 KST 8월 12일 05:30 실행이 실제 발표보다 약 19시간 먼저 “가용”으로 판정할 수 있다.

- **[높음] 월별·연별 지표의 발표 지연 정렬 규칙이 없음.** ONI처럼 3개월 기간이 종료된 뒤 발표되는 지표나 ERA5-Land 지연 자료를 관측월·관측일에 바로 붙이면 발표 전 날짜에 미래정보가 들어간다. `available_at`이 WASDE 트리거에만 설명되어 있고, 전체 특징 행의 as-of 조인 규칙은 제시되지 않았다.

- **[높음] 1·5·20·60일 horizon 모델의 purge/embargo가 없음.** 60일 선행목표는 학습·검증 경계 직전 60개 행의 라벨이 검증기간 가격을 포함할 수 있다. 시계열 walk-forward 분할, 경계 purge, 스케일링·결측대치의 fold 내부 적합 규칙이 없으면 성능과 중요도가 누수된다.

- **[높음] 레짐 산출이 필터링 기준인지 사후 스무딩 기준인지 정의되지 않음.** 전체 표본으로 판정한 레짐이나 Markov smoothing 결과를 과거 행에 부여하면 미래 관측으로 과거 레짐을 결정하는 look-ahead가 발생한다. 월별 “레짐별 중요도”의 핵심 전제인데 관련 통제가 없다.

### ③ 상관을 인과로 과장한 해석

- **[높음] Elastic Net·SHAP·Granger 조합을 “인과 근거”와 `validated` 인과 엣지로 취급함.** Elastic Net과 SHAP은 예측모형 내부의 연관·기여도를 나타내며, Granger 검정도 조건부 예측 선행성일 뿐 교란 제거·구조적 식별을 제공하지 않는다. 정상성·공적분·시차 선택·동시성·다중검정 통제도 제시되지 않아 “인과”나 “검증된 엣지”로 승격할 근거가 없다.

- **[높음] “순위가 실제로 움직이는 계기는 월별 발표·레짐 전환뿐”이라는 서술이 인과적으로 과도함.** 일별 가격 충격, 변동성 급등, 환율 변화 또는 이상치도 정규화·Elastic Net 선택·SHAP 배경분포를 통해 순위를 바꿀 수 있다. 이벤트 전후 순위 변화량, 대조기간, 신뢰구간을 이용한 검증이 없다.

- **[높음] WASDE를 “대두유 수급의 최대 정보 이벤트”로 단정할 근거가 없음.** 바이오연료 정책, EPA 발표, NOPA crush, MPOB/CPO, 원유·환율 이벤트와 비교한 이벤트 스터디나 정보이득 측정이 제시되지 않았다. 전문가 의견만으로 최대 효과를 단정했다.

### ④ 재현 불가능한 주장

- **[높음] 중요도와 Granger 결과를 재현할 핵심 설정이 없음.** 실제 유효 표본 수, 빈도 통합 방식, 결측 처리, 표준화, Elastic Net의 `alpha/l1_ratio`, SHAP explainer·background, Granger 최대 시차·검정식·유의수준, 레짐 정의가 제공되지 않았다.

- **[높음] “일별로 순위가 사실상 변하지 않는다”는 실증 자료가 없음.** M-006의 사용자 불만은 안정성 검정이 아니다. 일별 rank correlation, top-k turnover, 계수 신뢰구간, 충격일 민감도 결과가 없어 주기 결정의 핵심 정량 주장을 재현할 수 없다.

- **[높음] 경보 및 발행 게이트를 재현할 수 없음.** “필수 5종”, `2σ 등 기존 로직`, WARNING 영향범위, as-of 검증의 구체 식·기준창·실패 조건이 이 문서에 없다. 같은 데이터로 어떤 날 발행되는지 독립적으로 판정할 수 없다.

- **[높음] GPT 교차검증은 장기 재현성이 없음.** 사용 프롬프트, 모델 스냅샷, 샘플링 설정, 입력 파일 커밋 해시가 없고 아티팩트도 90일 후 만료된다. `GPT-5.6-Sol`과 `xhigh` 표기 및 run ID만으로 동일 판정을 다시 생성할 수 없다.

- **[높음] 월별 기준본과 검증 증거의 보존기간이 재현 요구보다 짧음.** 월별판 90일, 주별판 30일 후 삭제되며 Azure Blob 이관은 미래 계획이다. 이관 전에는 월별 변화나 패널 결정 근거를 장기 감사할 불변 산출물이 없다.

- **[참고] 비용·소요시간 주장이 정량화되지 않음.** 일별 과금 부담, 분석 30~60분, 구현 반일에 대한 호출 수·토큰·단가·실행시간 분포가 없다. 특히 실제 30분 타임아웃 사례가 있어 “07:00 이전 발행 가능”을 보장하는 근거로 사용할 수 없다.
