# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 34762625214

---

- **[높음｜① 수치·논리 오류] Tier2 좌표 검증이 설정 파일을 자기 자신과 비교하는 순환 검증으로 약화됨.**  
  `test_minimal_parser_matches_yaml`은 동일한 `production_regions.yaml`을 두 파서로 읽어 비교할 뿐이며, Tier2 좌표를 독립적인 정본이나 `anchor`와 대조하지 않는다. `test_coordinates_within_physical_range`도 범위·반구·소수 첫째 자리만 검사한다. 예를 들어 브라질 지역에 `lat=-0.1, lon=-0.1`을 넣어도 통과하므로, “벨트 중심 검증 반영”을 보장하지 못한다. 동일 반구 내 지역 간 좌표 오기나 교환도 탐지할 수 없다.

- **[높음｜① 논리 오류] Tier2 좌표의 `INFERENCE` 제약을 제거하면서 `CONFIRMED` 판정 근거를 검증하지 않음.**  
  기존 `test_tier2_inference`가 삭제되고 모든 지역에 `{"CONFIRMED", "INFERENCE"}` 중 하나만 요구한다. 따라서 설정 파일에서 Tier2 상태를 `CONFIRMED`로 바꾸기만 해도 통과하며, 출처의 권위·좌표 일치·승인 기록 등 확정 판정 근거는 확인하지 않는다. 검증 수준을 나타내는 상태값이 자기 선언으로 전락한다.

- **[높음｜② 시계열 누수 위험] 2026-09-13 as-of 기준보다 미래의 검증 정보가 허용됨.**  
  `verified_on`은 비어 있지 않은지만 검사하므로 `2099-01-01`, `tomorrow` 같은 값도 통과한다. `verified_on <= 2026-09-13` 검사와 출처의 발행·유효 시점 검사가 없어, 컷오프 이후 확정된 생산 벨트나 좌표 정보를 과거 as-of 구성에 반영할 수 있다.

- **[높음｜④ 재현 불가능한 주장] 출처·검증일 필드 검사만으로는 “재대조 가능” 또는 “승인자 확정값”을 재현할 수 없음.**  
  `source_url`은 문자열에 `"http"`가 포함되기만 하면 되어 `"not-http-evidence"`나 `"http"`도 통과하고, `verified_on`과 `anchor` 역시 임의의 한 글자면 통과한다. URL 유효성, 문서 버전·내용 해시·보관본, 실제 좌표와 anchor의 일치 여부를 저장·검사하지 않아 링크 내용이 변경되거나 사라지면 당시 확정 근거를 재현할 수 없다.
