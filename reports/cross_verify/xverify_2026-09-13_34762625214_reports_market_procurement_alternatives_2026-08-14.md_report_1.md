# GPT 교차검증 — 2026-09-13 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: reports/market/procurement_alternatives_2026-08-14.md, reports/market/procurement_alternatives_2026-08-19.md, reports/market/procurement_alternatives_2026-08-25.md, reports/market/procurement_alternatives_2026-09-13.md
- 런: 34762625214

---

## ① 수치·단위·논리 오류

- **[치명] 8/14·8/19·8/25의 운임 관련 판단이 오류로 확인된 BDI 계열에 기반함.**  
  각 문서의 정오표가 BDI 값·z·레짐이 “재척도 전 수동본” 기준이라고 명시하지만, 본문에는 계속 `z=-0.24`, `평시`, 커버리지 1~2개월, FOB+직접운임 검토가 표시된다. 잘못된 입력이 ②커버리지와 ③Incoterms 판단을 직접 결정하므로 해당 세 보고서의 관련 결론은 유효하지 않다. 외부 정오표 링크만으로 본문 수치와 결론이 정정되지는 않는다.

- **[높음] 8/14는 현재 시점의 가격 밴드를 미래 대기 손익으로 잘못 해석함.**  
  `[1,290, 1,770]`은 2·4주 후 조건부 분포가 아닌데도 꼬리 비대칭을 근거로 “하방 꼬리 우세(대기 여지 있음)”라고 결론 냈다. 현재 가격 불확실성의 비대칭만으로 대기 후 기대비용 방향을 판단할 수 없다. 8/19 이후 문서가 스스로 이를 “대기 손익 방향 판단 불가”로 정정했지만 8/14 본문에는 남아 있다.

- **[높음] 8/14의 커버리지 ‘델타’가 가격 수준과 분위수 거리를 혼동함.**  
  `1개월 연장 시 … 1,564 $/MT`는 비교 델타가 아니라 현 P50 가격 수준이다. `상방 보호 기대 최대 +206 $/MT` 역시 현재 밴드의 상단−중앙값일 뿐 1개월 후 기대손실, 최대손실 또는 연장 효과가 아니다. 금융비·보관비·선매입 수량·미래 구매가격도 빠져 있다.

- **[참고] 8/14·8/19·8/25의 하방 거리 산술이 표시값과 불일치함.**  
  표시된 값으로는 `1,564−1,290=274 $/MT`인데 `273 $/MT`로 기재됐다. 따라서 8/14의 비대칭도 `206−274=-68 $/MT`이지 `-67 $/MT`가 아니다. 비표시 원시값으로 계산했다면 반올림 규칙을 제시해야 한다.

- **[높음] 네 문서 모두 기본값을 실제 ‘현재’ 값처럼 사용함.**  
  요약에는 `현재 입력값 1.5개월`이라고 쓰지만 입력표는 이를 명시적으로 “기본값(사내 실값 아님)”이라고 밝힌다. 따라서 9/13의 `권장 하한까지 부족 1.5개월`은 실제 커버리지 부족분이 아니라 가상 기본값 기준 결과다. `spot`, `flat`도 동일한 기본값이므로 현재 계약 상태에 대한 판단으로 사용할 수 없다.

- **[높음] 9/13의 “기존 CFR term 유지”는 제시된 계약 입력과 일관되지 않으며 CFR 효과도 과도하게 단정함.**  
  입력은 `spot` 기본값인데 결론은 존재 여부가 확인되지 않은 `기존 CFR term` 유지를 전제로 한다. 또한 CFR은 운임 지불 의무를 매도인에게 배분하지만, 운임 상승분의 가격 전가 가능성까지 자동으로 고정하지는 않는다. 실제 고정 여부는 계약가격·조정조항·재협상 조건에 달려 있다.

- **[높음] 8/14는 무차원 z격차를 USD/MT 경제성의 대체값으로 사용함.**  
  원계열이 MYR/MT, EUR/ton, INR/10kg로 다르고 FX 환산 스프레드도 없는데, 음의 z격차를 배합 전환 방향 신호로 해석했다. z격차는 각 시장의 표준화 위치 차이일 뿐 절감액이나 대체 경제성을 나타내지 않는다. 8/19 이후 문서에서 이 오류를 인정했지만 8/14 판단은 정정되지 않았다.

- **[참고] “각 델타는 $/MT”라는 단위 선언과 표 내용이 불일치함.**  
  ②축의 핵심 델타는 `0.0개월` 또는 `1.5개월`이고, ④축은 실제 USD/MT 델타 없이 임계값만 제시한다. 열 제목 `정량 델타 ($/MT)`가 서로 다른 차원의 값을 혼합한다.

## ② 시계열 누수(look-ahead) 위험

- **[높음] 과거 날짜 파일에 2026-09-13에만 알 수 있었던 정오표가 인플레이스 삽입됨.**  
  8/14·8/19·8/25 파일을 파일명 날짜 기준의 point-in-time 산출물로 읽는 백테스트나 학습 파이프라인은 9/13의 데이터 오류 발견 사실과 외부 정정 결과 링크를 과거 정보로 오인할 수 있다. 파일 버전 또는 `effective_from` 기준으로 9/13 추가분을 제외하지 않으면 명시적인 미래정보 누수다.

- **[높음] BDI 수정계열을 과거 시점에 소급 적용할 경우 데이터 빈티지 누수가 발생함.**  
  정정계열은 9/13에 이용 가능해졌는데 이를 이용해 8/14·8/19·8/25의 z·레짐을 재산출한 뒤 실시간 성능처럼 평가하면 당시 이용 불가능했던 수정치를 사용하게 된다. 원본 빈티지와 수정 빈티지를 분리하지 않으면 실시간 의사결정 백테스트가 성립하지 않는다.

- **[높음] FX parquet 부재 상태에서의 사후 `CI 재실행`은 시점 고정이 없으면 미래 FX로 과거 스프레드를 채울 위험이 있음.**  
  보고서 날짜별 FX 스냅샷·관측시각·환율 방향이 고정되어 있지 않다. 이후 CI가 최신 FX를 가져와 7/1 또는 과거 보고서의 환산값을 생성하면 look-ahead와 실행시점별 결과 변동이 동시에 발생한다.

## ③ 상관을 인과로 과장한 해석

- **[높음] BDI 레짐을 대두유 운임과 커버리지·Incoterms 결정의 인과 신호로 사용함.**  
  BDI는 건화물선 지수이며 대두유의 실제 운송은 액체 벌크선·탱커·컨테이너 등 계약별 구조가 다를 수 있다. 노선·선형·laycan별 운임 견적이나 BDI→대두유 CFR 전가계수가 없는 상태에서 `평시/급등 → 1~2/3~6개월`, `급등 → CFR 유지`로 연결한 것은 광범위한 운임 상관 프록시를 조달비용 인과효과로 확대한 것이다. “BCAA 실측 전 프록시”라는 고지만으로 이 연결이 검증되지는 않는다.

- **[높음] 8/14는 혼합 잔차의 `P90−P50=155 $/MT`를 운임 프리미엄으로 귀속함.**  
  원재료·FX·품질·공분산이 섞인 분위수 차이를 “급등 레짐 전환 시 신규 CFR 호가에 가산될 운임 프리미엄”으로 해석했다. 운임 성분 분해나 식별이 없으므로 성분 귀속이 불가능하다. 8/19 이후 문서가 이를 명시적으로 인정한다.

- **[높음] CE-ID와 ontology의 방향 화살표를 ‘인과 근거’로 취급함.**  
  `BDI→수출 채산성`, `B40→스프레드 축소`, `해협→운임→CIF` 등의 경로만 제시되고 식별전략, 선행시차, 교란통제, 효과크기 또는 반사실 검증이 없다. 메커니즘 가설이나 상관 경로는 인과효과의 증거가 아니며, 특히 `CE-015 — 통계 검증 대기` 상태에서 인과 근거로 표시한 것은 자체 고지와 충돌한다.

- **[높음] 2021~22의 대응 관행을 현재 커버리지 연장의 비용 우위로 일반화함.**  
  “커버리지 연장이 실수요자 대응 1순위였음”은 당시 행동에 대한 기술일 뿐, 금융·보관비를 포함한 반사실 비용이 더 낮았다는 증거가 아니다. 이를 현재 3~6개월 권장 범위의 정당화로 사용하는 것은 행동 상관을 최적 정책의 인과효과로 과장한다.

- **[높음] 8/25·9/13의 “공행성 붕괴”는 최대 다이버전스만으로 입증되지 않음.**  
  `최대 +113%`의 가격 괴리는 공적분 붕괴나 평균회귀 소멸을 뜻하지 않는다. 구조변화·공적분·레짐 검정 없이 장기 괴리 관측을 구조적 관계 붕괴로 단정했다.

## ④ 재현 불가능한 주장

- **[높음] `SBO−CPO > 175 $/MT` 임계값을 재현할 산출 근거가 없음.**  
  가격 기준이 선물·FOB·CIF 중 무엇인지, 조유/정제유 품질, 관세·운임·FX, 표본기간, 추정식, 비용함수 및 오차범위가 제시되지 않았다. 문서 자체가 `통계 검증 대기`라고 밝히면서 이를 입력 파라미터로 고정했다. “GPT 교차검증”도 모델·버전·프롬프트·실행 로그가 없어 재현 가능한 검증이 아니다.

- **[높음] z값과 z격차의 계산 계보가 부족함.**  
  `90일`이 거래일인지 달력일인지, 수준/수익률 중 무엇을 표준화했는지, 표준편차 자유도, 선물 롤·결측일 처리, 데이터 소스·시각이 없다. BDI z도 표준화 창과 재척도 결합 방식이 제시되지 않아 `-0.24`와 `+2.74`를 독립적으로 재산출할 수 없다.

- **[높음] CBOT와 대체유 시계열의 기준일 정렬 여부를 확인할 수 없음.**  
  8월 보고서의 대체유 최종 관측은 모두 7/1인데 CBOT 최종 관측일은 없다. CBOT가 보고서 당일 값이면 44~55일 어긋난 시점의 z를 차감한 것이고, CBOT도 7/1 값이면 “현재 신호”가 수주간 지연된 것이다. 네 보고서 모두 CBOT z가 정확히 `-0.70`으로 반복되는 점도 스냅샷 또는 캐시 계보 없이는 검증할 수 없다.

- **[높음] `2020 이후 최대 +113%`를 재현할 정의가 없음.**  
  분자·분모, 기준가격, 통화, 시작·종료일, 빈도, 최고점 관측일이 제시되지 않았고 `farmdoc 2025·A-201`도 정확한 표·페이지·계산식을 지정하지 않는다. 따라서 수치와 “공행성 붕괴” 해석 모두 재현할 수 없다.

- **[참고] 8/14·8/19·8/25의 시장 산출값이 완전히 동일하지만 사용 스냅샷이 고정되어 있지 않음.**  
  세 날짜 모두 P50 `1,564`, 밴드 `[1,290, 1,770]`, 내재층 차이 `155`, CBOT z `-0.70`이다. 원천 관측일·데이터 해시·실행 커밋이 본 문서에 없어 정상적인 동결 입력인지, 갱신 실패나 캐시 재사용인지 판별할 수 없다.
