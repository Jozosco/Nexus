# GPT 교차검증 — 2026-09-14 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 34795678963

---

## ① 수치·단위·논리 오류

- **[높음] `.github/workflows/cross_verify.yml` — “이번 push에서 바뀐 파일” 판정이 실제로는 마지막 커밋만 포함한다.**  
  `git diff --name-only HEAD~1 HEAD`는 여러 커밋을 한 번에 push한 경우 앞선 커밋에서 변경된 문서를 누락한다. 또한 `|| true`로 얕은 checkout 등으로 `HEAD~1` 해석이 실패해도 `changed`가 빈 값이 되어 모든 문서 검증을 조용히 생략한다.

- **[참고] 기후 예보 기간 정의가 서로 모순된다.**  
  `test_forecast_rows_shape`는 변수별 **15행, 리드 0~14일**을 전제로 하지만, 함수 설명은 “최대 16일”, 행 규약과 품질 검사는 `lead_days` **0~16일(포함 시 17개 리드)**을 허용한다. 현재 검증기는 잘못된 15·16일 리드도 통과시킬 수 있다.

- **[높음] BDI XLSX 폴백이 `MIN_ROWS` 및 일별 계열 무결성을 보장하지 않는다.**  
  폴백은 TE 또는 Stooq 결과가 완전히 비었을 때만 실행된다. 두 소스가 1~9행만 반환하거나 오래된 일부 행만 반환하면 XLSX 폴백 없이 `MIN_ROWS=10` 실패가 그대로 발생한다. 주석의 “BDI 일별 계열 무결성 보장”과 실행 조건이 일치하지 않는다.

- **[높음] 오래된 BDI 스냅샷을 최신 데이터로 오인할 수 있다.**  
  XLSX 행 전체에 `ingested_at=pd.Timestamp.utcnow()`를 부여하고 freshness 검사는 `ingested_at` 최댓값을 사용한다. 따라서 XLSX의 마지막 `price_date`가 수주~수개월 전이어도 방금 수집된 것으로 freshness를 통과할 수 있다. 새 테스트도 최종 관측일이 최근 영업일인지 확인하지 않는다. 또한 `sorted(files)[-1]`은 파일명 사전순 마지막일 뿐 최신 스냅샷임을 보장하지 않는다.

- **[높음] cron 변경의 개장 시각·지연 산술 전제가 앞뒤가 맞지 않는다.**  
  삭제된 설명은 05:30 KST가 개장 45분 전이므로 기존 개장을 **06:15 KST**로 전제한다. 새 설명은 근거 없이 개장을 **07:00 KST**로 변경한다. 기존 전제라면 새 nominal 04:17에 2시간 지연 시 06:17로 이미 개장 후이며, 관측된 최대 지연은 20:30→22:56인 **2시간 26분**이라 “2시간 지연을 감안해도 개장 전”이라는 결론이 성립하지 않는다.

## ② 시계열 누수 위험

- **[높음] 기후 예보의 가용 시점이 실제 수집 시각보다 앞당겨진다.**  
  `price_date`를 유효일에서 발행일의 자정으로 바꾸면서 `event_time <= ingested_at`이 되어, 정확한 수집 시각으로 캡하던 `event_time > ingested_at` 분기가 더 이상 발동하지 않는다. 새 테스트도 이를 명시적으로 고정하고 `available_at`이 같은 날짜이기만 하면 통과시킨다. 예를 들어 19:17 UTC에 처음 수집한 예보가 그날 00:00 이후부터 사용 가능했던 것처럼 보이면, 00:00~19:17 사이 intraday as-of 백테스트에 미래 정보가 들어간다. 현재 모델 패턴에서 제외됐더라도 향후 skill 검증·모델 편입 시 직접적인 누수 위험이다.

- **[높음] `issue_date`가 실제 원천 발행 시각이 아니라 수집일로 대체되어 예보 빈티지를 보장하지 못한다.**  
  `lead_days`도 원천 모델 초기화 시각이 아니라 `valid_date - date.today()`로 계산된다. 같은 날짜 안에서 나중 모델 사이클을 수집해도 동일한 발행일·리드로 기록되므로, 일중 시점 기준으로는 나중에 나온 예보가 앞선 빈티지처럼 취급될 수 있다.

## ③ 상관을 인과로 과장한 해석

- **[참고] GitHub 지연 원인을 정각·30분 슬롯 혼잡으로 단정할 근거가 부족하다.**  
  지연된 실행 시각만으로 “GitHub이 정각·30분 슬롯을 혼잡 지연시킨다”는 원인을 특정했다. 17분 슬롯과의 비교 실행, 동일 시점의 GitHub 큐 상태, 반복 표본이 제시되지 않았다. `INFERENCE`라고 표시했지만 해당 추론을 근거로 운영 cron을 변경했으므로 인과 결론은 아직 검증되지 않았다.

## ④ 재현 불가능한 주장

- **[높음] Open-Meteo 예보 빈티지를 재현할 메타데이터가 부족하다.**  
  소스는 동적으로 모델을 고르는 `best_match`인데 모델명·모델 초기화 시각·API 응답 생성 시각을 저장하지 않고 날짜만 `source_vintage`로 기록한다. 같은 날 재실행하면 같은 `climate_forecast_{date}.parquet`를 덮어쓰면서 값이 달라질 수 있어, 어떤 모델 사이클의 예보로 skill을 계산했는지 재현할 수 없다.

- **[높음] UTC 기준으로 재현 가능하다는 품질검사 설명과 커넥터 구현이 불일치한다.**  
  품질검사는 `_today_utc()`를 사용하지만 기후 예보 발행일과 BDI cutoff는 여전히 러너 로컬의 `date.today()`를 사용한다. 예를 들어 UTC+9 환경의 00:30에는 커넥터가 다음 로컬 날짜를 쓰고 검사는 이전 UTC 날짜를 써 동일 데이터가 환경에 따라 통과·실패한다. `lead_days`와 120일 필터 결과도 러너 시간대에 의존한다.

- **[높음] BDI 폴백 테스트가 구현 고장을 성공적인 skip으로 숨긴다.**  
  `fetch_bdi_te_xlsx()`는 파일 부재뿐 아니라 import 오류·파서 예외·스키마 오류도 모두 빈 DataFrame으로 반환하며, 테스트는 빈 결과를 전부 `pytest.skip` 처리한다. 따라서 폴백 코드가 완전히 깨져도 CI가 통과할 수 있다. 실제 저장소 파일과 현재 날짜에도 의존해 같은 커밋의 테스트 결과가 실행 시점에 따라 달라진다.

- **[참고] 테스트의 `openai` 전역 stub이 수집 순서에 따라 결과를 바꿀 수 있다.**  
  설치 여부가 아니라 `sys.modules` 등록 여부만 확인해 `openai`를 전역 stub으로 교체한다. 다른 테스트가 먼저 실제 모듈을 import했는지에 따라 shipping connector가 실제 모듈 또는 stub을 사용하므로 테스트 순서·환경에 종속된다.

- **[참고] “23개 전 지역 좌표 확인 완료”로 바꾼 근거가 이 변경에 없다.**  
  승인 기록, 확인자, 좌표 버전 또는 검증 산출물 없이 카탈로그 문구만 “11개 승인 대기”에서 “전 지역 확인 완료”로 변경됐다. 따라서 어떤 좌표가 어떤 근거로 확정됐는지 추적·재현할 수 없다.
