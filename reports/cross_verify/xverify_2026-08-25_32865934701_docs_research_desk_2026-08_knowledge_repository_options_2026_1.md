# GPT 교차검증 — 2026-08-25 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/knowledge_repository_options_2026_08_25.md
- 런: 32865934701

---

## ② 시계열 누수 위험

| 심각도 | 지적 | 구체 근거 |
|---|---|---|
| **치명** | 과거 시점 검색을 보장하는 point-in-time 필드와 차단 조건이 없다. | 제시된 스키마에는 일반적인 `날짜`만 있고 `published_at`, `available_at`, `ingested_at`, 개정 시각 및 `as_of` 조건이 없다. 따라서 과거 가격 예측·백테스트에서 이후 작성된 MEMORY 결정, 정오표, research_desk 문서가 검색될 수 있다. 문서 표기일은 실제 입수 가능일과 같지 않으므로 누수를 막지 못한다. |
| **높음** | 문서만 날짜로 필터링해도 TF-IDF와 질의 확장에서 미래 정보가 간접 누수될 수 있다. | 전체 누적 코퍼스로 TF-IDF의 IDF·어휘를 학습한 뒤 과거 문서만 반환하면 미래 문서의 단어 빈도가 과거 검색 순위에 반영된다. 152개 용어·동의어·인과엣지도 시점별 버전이 없어 이후 구축된 의미 체계가 과거 질의를 변경할 수 있다. 필터 선적용 및 시점별 인덱스 적합 절차가 명시되지 않았다. |

## ① 수치·단위·논리 오류

| 심각도 | 지적 | 구체 근거 |
|---|---|---|
| **높음** | “문서 ~5천”이라는 규모 산정이 제시된 자산 수와 맞지 않으며 중복 집계 가능성이 높다. | 실제 인덱싱 대상으로 적은 값은 요약 2,175 + 결정 300+ + research_desk 66 + 용어 152로, 용어와 결정까지 각각 독립 레코드로 쳐도 약 2,693+건이다. 약 5천 건은 인덱스 CSV 2,227행을 더해야 나오지만, 이 CSV는 동일 요약·원문에 대한 메타데이터 테이블이므로 별도 문서로 더하면 이중 계산이다. |
| **높음** | 요약 2,175건과 인덱스 2,227행 사이의 52행 차이가 설명되지 않았다. | “원문 PDF와 1:1” 및 “원문↔요약을 한 테이블로 조인 가능”이라고 했지만, 추가 52행이 중복·요약 누락·비PDF 자료 중 무엇인지 제시하지 않았다. 이 상태에서는 문서 단위 랭킹, 중복 결과 및 근거 추적 건수가 왜곡될 수 있다. |
| **높음** | 제시된 스키마만으로 EvidenceSpan 계약을 준수하거나 “원문 인용 100% 일치”를 강제할 수 없다. | 정규화 필드는 문서 ID·유형·날짜·태그·엔티티·본문·출처 경로뿐이다. 원문 버전/해시, 페이지, 원문 인용문, 문자 오프셋, 근거 등급 필드가 없다. 주 입력도 원문 PDF가 아니라 요약 md이므로 경로만 저장해서는 검색 발췌가 PDF 원문과 정확히 일치하는지 검증할 수 없다. 계약의 존재는 자동 집행 수단이 아니다. |
| **높음** | 438KB 문서를 포함하면서 청킹 없이 문서 단위로 저장·임베딩하는 구조는 장문 검색 요구와 모순된다. | 구조도에는 문서당 본문 한 행만 있고 `chunk_id`, 페이지·오프셋, 청크 중첩 규칙이 없다. sentence-transformers는 모델별 최대 토큰 이후를 절단하므로 438KB 문서의 후반 근거가 임베딩에서 사라질 수 있다. TF-IDF로 문서를 찾더라도 관련 “발췌”를 산출하는 알고리즘이 없다. 따라서 “Phase 1→2 같은 스키마라 재작업 없음”도 성립하지 않는다. |
| **높음** | FAISS·Chroma·LanceDB를 일괄적으로 “별도 인프라”라고 분류한 것은 기술적으로 부정확하다. | FAISS는 프로세스 내에서 실행하는 라이브러리이며, Chroma와 LanceDB도 로컬·임베디드 구성이 가능하다. 승인 라이브러리 미등재는 별도 배제 사유가 될 수 있지만, 외부 SaaS 이전 문제를 다룬 D-025 논리를 이들 모두에 적용한 비교는 왜곡돼 있다. |
| **높음** | “비용 0”은 보고서 자체의 투입 추정과 모순된다. | 로드맵에서 Phase 1·2 각각 약 반나절의 인력 투입을 명시했고, 로컬 계산·저장·유지보수 및 HF egress 절차도 필요하다. 정확한 표현은 라이선스·구독의 증분비용이 0일 수 있다는 것이며, 총비용 0은 아니다. |
| **높음** | “이식성·잔존자산 100%”는 Parquet 데이터 이동성과 전체 시스템 승계를 혼동한다. | Parquet는 테이블을 옮길 수 있을 뿐, 원문 PDF의 유효한 경로, TF-IDF 어휘·IDF 상태, 임베딩 모델 가중치, CLI 실행환경, ACL 및 검증 로직까지 승계하지 않는다. Snowflake에서도 DuckDB+scikit-learn CLI가 그대로 실행되는 것이 아니므로 마이그레이션 시험 없이 100%로 단정할 수 없다. |
| **참고** | “438KB 문서는 전문 로드 불가”라는 절대 표현에는 단위 변환과 모델 한도가 없다. | 파일 바이트 수만 제시했고 실제 토큰 수, 사용 에이전트의 컨텍스트 한도, 시스템 프롬프트·응답 예약량을 제시하지 않았다. 모델에 따라 전문 로드 가능 여부가 달라지므로 현재 수치만으로는 “불가”를 결론낼 수 없다. |
| **참고** | 검색·생성 계층을 분리하면 환각을 “구조적으로 피한다”는 결론이 과장됐다. | 외부 에이전트가 검색 결과를 누락·오독하거나, 관련 없는 문서를 종합하거나, 인용에 없는 결론을 생성할 수 있다. 계층 분리는 환각 위험을 낮출 수 있을 뿐 제거하지 않으며, 인용 충실도 검증 절차도 제시되지 않았다. |

## ③ 상관을 인과로 과장한 해석

| 심각도 | 지적 | 구체 근거 |
|---|---|---|
| **높음** | 의미 검색 부재가 반복 조사 비용을 발생시킨다는 인과 주장이 검증되지 않았다. | “지식이 파일에 잠김 → 세션마다 같은 조사를 반복”이라고 서술하지만, 세션 로그, 중복 질의율, 조사시간 비교 또는 실패 원인 분석이 없다. 반복 조사는 포인터 품질, 문서 최신성, 사용자 교육 등 다른 요인으로도 발생할 수 있어 현재 자료로는 검색 기능 부재의 효과를 분리할 수 없다. |

## ④ 재현 불가능한 주장

| 심각도 | 지적 | 구체 근거 |
|---|---|---|
| **높음** | “조사 실측” 수치와 “검색 코드 0건”을 재현할 절차가 없다. | 저장소 커밋 해시, 대상 경로·glob, 제외 규칙, 중복 처리, 파일 크기 단위, 실행 명령 및 검색 키워드가 없다. 특히 “의미 검색·벡터·랭킹 코드 0건”은 무엇을 코드로 판정했는지 기준이 없어 동일 결과를 재현할 수 없다. |
| **높음** | TF-IDF 검색 결과를 재현하는 데 필요한 설정과 산출물이 누락됐다. | 토큰화 방식, 한글 형태소 처리 여부, 정규화, n-gram, stopword, `min_df`/`max_df`, fit 코퍼스 순서, scikit-learn 버전, 어휘·IDF 행렬 저장 여부가 없다. “단일 parquet+단일 CLI”만으로 동일 순위를 보장할 수 없다. |
| **높음** | Phase 2 임베딩은 모델이 특정되지 않아 재현할 수 없다. | `sentence-transformers`는 라이브러리명일 뿐이다. HF 모델 ID·revision/commit, tokenizer, 최대 길이, pooling, 임베딩 차원, 정규화, 청킹 규칙 및 모델 파일 보존 방식이 없다. “HF egress 2종”도 구체 도메인·모델명이 없어 승인 범위와 실행 결과를 확인할 수 없다. |
| **높음** | “정확한 근거를 빨리 찾는다”는 핵심 효용을 뒷받침하는 검색 평가가 없다. | 테스트 질의, 정답 근거 집합, Recall@k/nDCG, 인용 정확도, 중복률, 지연시간 및 실행 하드웨어가 전혀 없다. TF-IDF·의미 확장·현행 grep 간 비교 실험 없이 정확성과 속도를 주장했다. |
| **참고** | Phase별 “약 반나절” 추정은 재현 가능한 산정치가 아니다. | 문서 파서 수, 예외 형식, 청킹·검증·테스트·CI 작업 범위, 담당자 숙련도 및 완료 기준이 없다. 특히 52행 불일치 해소와 EvidenceSpan 원문 검증이 포함되는지도 불명확하다. |
| **참고** | “NotebookLM은 API도 없음”은 시점·제품군에 민감하지만 검증 정보가 없다. | NotebookLM 일반판/기업판 구분, 확인한 API 범위, 공식 문서 URL·버전·확인일이 없고 내부 ID `LLM-004`만 제시됐다. 2026-08-25 현재의 외부 제품 상태를 독립적으로 재현할 수 없다. |
