# GPT 교차검증 — 2026-08-28 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: docs/research_desk/2026-08/g1_publication_schedule_panel_2026_08_14.md
- 런: 33170861953

---

## ① 수치·단위·논리 오류

- **[높음] 분석창 연수 계산 오류**
  - `2010-01~2025-12`는 양 끝을 포함하면 **192개월, 16개년**이다. 문서 전반의 “15개년 추정창”과 불일치한다. 실제 창이 15년이면 시작일은 2011-01이어야 하며, 실제 창이 16년이면 표본 비중·모형 설명을 수정해야 한다.

- **[높음] “신규 관측 0.025%이므로 순위가 변하지 않는다”는 논리가 성립하지 않음**
  - 관측치 비중은 계수·순위 변화의 상한이 아니다. 신규값이 고레버리지 충격이면 Elastic Net의 활성 변수 집합, Granger 유의확률, SHAP 순위가 크게 바뀔 수 있다. 특히 상관된 변수에서 Elastic Net 순위는 작은 데이터 변화에도 불연속적으로 교체될 수 있다.
  - 순위상관, 부트스트랩 신뢰구간, leave-one-out 민감도 같은 검증값이 없어 “사실상 변하지 않음”을 뒷받침하지 못한다.

- **[높음] KST 07:00 시장 개장 주장의 시간대가 맞지 않거나 시장이 불명확함**
  - 목표변수인 CBOT 대두유 시장을 뜻한다면 CME 야간 세션의 19:00 CT 개장은 미국 서머타임 기준 **KST 09:00**, 표준시 기준 **KST 10:00**이다. KST 07:00과 맞지 않는다. 다른 시장을 뜻한다면 시장명이 없어 CI-001의 설계 근거를 검증할 수 없다.

- **[높음] DQ 발행 게이트 규칙이 상충함**
  - §2.4·§4는 “필수 5종 PASS”를 요구하면서 WARNING 발행을 허용한다고 쓰고, §3의 C-08 의견은 “REJECTED만 차단”이라고 규정한다. 필수 5종 중 하나가 WARNING일 때 발행 여부가 서로 다르게 해석된다.
  - §3에서 C-06 통과도 발행 전제라고 했지만 §4의 공통 게이트와 §5 구현 계획에는 C-06 게이트가 빠져 있다.

- **[높음] `available_at` 시간창만으로 “정확히 1회” 발행을 보장할 수 없음**
  - “직전 스케줄 시각 이후 발생” 방식은 발행 이력이나 멱등 키가 없으면 재시도 시 같은 구간을 다시 평가해 중복 발행할 수 있다.
  - 반대로 금요일 실행이 실패하면 다음 월요일은 금요일 스케줄 이후만 조회하므로, 금요일 실행 전에 발생했던 WASDE 이벤트를 놓칠 수 있다. 따라서 §7.1 #2의 “정확히 1회 보장”은 과장이다.
  - 평일 cron은 주말만 배제할 뿐 미국·한국 휴일을 반영하지 않아 “익영업일” 구현과도 동일하지 않다.

- **[높음] “즉시 경보” 요구와 실제 고정시각 실행이 불일치함**
  - 일별 경보는 평일 KST 05:30에만 평가된다. 금요일 실행 직후 발생한 사건은 월요일까지 약 72시간 지연될 수 있으므로 P1-02의 “임계 초과 시 즉시 경보”를 충족하지 않는다.

- **[높음] 월별 심층판의 명칭과 실제 기능이 불일치함**
  - §4·§5는 월별 모드에서 horizon×레짐 전면 재산출을 제공한다고 명시하지만 §7.1 #5는 해당 계산이 아직 없고 M-009에서 구현될 예정이라고 인정한다. 현재 구현 diff의 `monthly` 결과를 “심층판”으로 발행하면 산출물 명칭이 실제 계산을 과장한다.
  - 또한 순위 변동 계기로 PSD 등 여러 월별 발표를 들었으나 트리거는 WASDE만이다. WASDE 이후 발표된 PSD·GATS·AMIS·관세청 자료는 다음 WASDE까지 전면 재산출이 지연될 수 있다.

- **[높음] 2σ 초과를 “구조적 단절”로 부르는 것은 통계적으로 부정확함**
  - 정상분포를 가정해도 변수 하나의 양측 2σ 초과 확률은 약 **4.55%**다. 다변수 동시 감시에서는 다중검정 보정이 없을 경우 경보가 빈번해진다. 예를 들어 독립 변수 20개라면 하루 한 개 이상 초과할 확률은 약 `1−0.9545^20 ≈ 61%`다.
  - 기준창, 단측·양측 여부, 변동성 추정법, 다중검정 보정이 정의되지 않아 단순 이상치를 구조적 단절로 오인할 위험이 크다.

- **[높음] 목표변수 신선도 5영업일 기준이 주간판의 데이터 포함 주장과 충돌함**
  - 금요일 보고서가 직전 금요일 가격만 보유해도 통상 5영업일 이내로 판정될 수 있다. 따라서 “미국 목요일 마감분 포함”이 실패해도 발행 게이트를 통과할 수 있다.

- **[치명] G2의 “완전 순위의존 분위수 합 = 스트레스 상계”는 일반적으로 거짓임**
  - 분위수/VaR는 일반적으로 부분가법적이지 않으므로 동일 분위수의 단순합이 합계 분위수의 상한을 보장하지 않는다.
  - 예를 들어 두 손실이 각각 4% 확률로 1, 그 외 0이면 각 95% 분위수는 0이다. 두 손실이 상호배타적으로 발생하면 합계의 95% 분위수는 1이 되어 `q95(X)+q95(Y)=0`을 초과한다.
  - 주 밴드의 독립 컨볼루션 역시 CBOT·베이시스·운임·환율 사이 의존성을 무시한다. 독립성 검증 없이 적용하면 조달비용 꼬리위험을 과소 또는 과대 추정할 수 있다.

- **[높음] `SBO−CPO > 175` 임계값의 단위와 가격 기준이 없음**
  - CBOT SBO는 통상 cents/lb, CPO는 USD/metric ton 등 서로 다른 단위·시장 기준으로 제공될 수 있다. `175`가 USD/t인지, 동일 통화·톤 단위로 환산된 FOB/CIF 가격 차이인지 명시되지 않아 계산을 재현할 수 없고 원시 가격을 직접 차감하면 단위 오류가 된다.

- **[높음] 과거 60거래일 분포는 2·4주 기대손익이 아님**
  - 과거 무조건부 변동 분포는 미래 2주·4주 조건부 기대값이나 해당 지평의 예측분포를 제공하지 않는다. “분포 폭 참고치”로 재명명해도 기존 기대손익 계산의 통계적 근거가 생기는 것은 아니며, §7.2에서 인정한 문제가 현재 산출물에는 그대로 남아 있다.

## ② 시계열 누수(look-ahead) 위험

- **[높음] WASDE 트리거의 `available_at ≤ now` 검사는 전체 피처의 point-in-time 정합성을 보장하지 않음**
  - 해당 가드는 WASDE 발행 트리거만 제한한다. PSD·NASS·FAOSTAT·관세청 확정치처럼 사후 개정되는 자료를 현재 최신값으로 과거 날짜에 결합하면 당시 알 수 없던 값을 역사 학습·검증에 사용하게 된다.
  - 각 관측치의 관측일, 최초 공개일, 개정 공개일, 사용 빈티지를 분리해 선택한다는 규칙이 문서에 없다. “as-of 검증”도 검사 항목이 정의되지 않았다.

- **[높음] ERA5-Land를 전일 확정치로 취급하면 가용시점 누수가 발생할 수 있음**
  - ERA5/ERA5-Land 근실시간 자료는 통상 수일 지연되며 확정·통합 자료는 더 늦게 개정된다. §2.1처럼 KST 05:30에 전일 확정치가 가용하다고 놓고 백필을 관측일 기준으로 정렬하면 실제 공개 전 기상정보를 사용하게 된다.
  - AIS·GeoIntel도 표준화된 “전일 확정 종가”가 없는 이벤트성 자료여서 관측시각과 수집·공개시각을 구분하지 않으면 같은 위험이 있다.

- **[높음] 1·5·20·60일 horizon 및 레짐별 재추정의 시간 분할 규칙이 없음**
  - walk-forward 분할, 60일 라벨 중첩에 대한 purge/embargo, 각 학습시점의 마지막 사용 가능 라벨 처리 방식이 명시되지 않았다. 일반 교차검증이나 무작위 분할이면 미래 목표값이 학습 폴드에 들어갈 수 있다.
  - 레짐을 전체 기간의 사후 변곡점이나 양방향 평활값으로 정하면 과거 시점의 레짐 라벨 자체가 미래정보를 포함한다. 온라인·단방향 레짐 판정 규칙이 제시되지 않았다.

## ③ 상관을 인과로 과장한 해석

- **[높음] Elastic Net·SHAP·Granger 결과를 “인과 순위/인과 근거/validated 인과 엣지”로 표현함**
  - Elastic Net 계수와 SHAP는 예측모형 내부의 조건부 연관성·기여도이며 인과효과가 아니다. Granger 검정도 시차 예측력 검정으로, 공통 원인·동시 정책충격·비정상성에 의해 유의해질 수 있다.
  - 개입 정의, 교란 통제 DAG, 도구변수·자연실험, 구조적 식별 가정이 없으므로 §2.3·§3·§4의 “인과 근거” 및 `validated` 승격은 방법이 지지하는 범위를 넘는다.

- **[높음] 경보판의 상위 SHAP 3개를 구조적 단절의 근거로 제시하는 것도 원인 귀속이 아님**
  - SHAP 상위값은 현재 예측에 대한 모형상 기여도일 뿐, 임계 초과나 가격 충격을 발생시킨 원인을 식별하지 않는다. 상관된 피처 사이에서는 배경분포와 조건부/주변 SHAP 방식에 따라 기여도가 재배분될 수도 있다.

## ④ 재현 불가능한 주장

- **[높음] 중요도 순위 산출 및 안정성 주장을 재현할 명세가 없음**
  - Elastic Net 계수 표준화, 세 방법의 순위 결합 규칙, Granger 지연차수·정상성 처리·다중검정, SHAP 배경집합, 레짐 정의, 학습 분할과 시드가 제시되지 않았다.
  - M-006의 한 차례 “상태 동일 반복”만으로 “순위는 월별 발표 때만 유효하게 변한다”는 일반화를 재현하거나 검증할 수 없다.

- **[높음] “WASDE가 대두유 수급의 최대 정보 이벤트”라는 주장에 정량 근거가 없음**
  - 발표 전후 수익률·변동성·예측오차 개선, 다른 이벤트와의 비교, 표본기간 및 이벤트창이 제시되지 않았다. P1-01의 의견만으로 월별 트리거를 단일 이벤트에 고정했다.

- **[높음] 실행 증거와 산출물의 보존기간이 짧아 장기 재현이 불가능함**
  - 교차검증 전문과 월별 산출물이 90일 후 삭제되고 주별판은 30일 후 삭제된다. Azure Blob 영구 보존은 미래 이관 계획일 뿐 현재 보존 경로가 아니다.
  - run ID만 있고 코드 커밋 SHA, 데이터 스냅샷·빈티지, 환경/패키지 버전, 프롬프트 전문이 영구 보존된다는 명세가 없어 만료 후 같은 결과를 재구성할 수 없다.

- **[참고] 2026년 데이터의 학습창 포함 여부가 불명확함**
  - 문서 작성일은 2026-08-14인데 명시된 분석창은 2025-12에 끝난다. 2026-01~08 관측을 별도로 추가하는지, 고정창인지 롤링창인지 쓰지 않아 실제 표본수와 “신규 하루치 0.025%” 계산을 재현할 수 없다.
