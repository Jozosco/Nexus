# GPT 교차검증 — 2026-08-19 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: reports/market/landed_cost_band_2026-08-14.md, reports/market/landed_cost_band_2026-08-19.md
- 런: 32251935391

---

## ① 수치·단위·논리 오류

- **[치명] 예측 분위로 해석할 수 없는 값을 P10/P50/P90 도착가 밴드로 제시 — 양 문서**
  - CBOT 층은 미래 가격의 조건부 예측이 아니라 최근 60거래일 **가격 수준**의 경험분포이며, 목표 도착일·조달계약일·예측 horizon이 없다.
  - 따라서 1,290/1,564/1,770은 과거 표본을 재조합한 분위일 뿐 미래 CIF의 확률적 커버리지를 갖지 않는다. 8/19 문서는 이를 “규모감 참고 범위”라고 인정했지만 값과 분위 표기는 그대로라 예측 밴드로 재사용될 위험이 남는다.

- **[치명] ‘4층 분해’가 아니라 CBOT와 오염된 잔차의 2항 합 — 양 문서**
  - 실제 계산은 `CBOT + (도착월 CIF − 동월 CBOT)`이다. 두 번째 항에는 보고서가 인정한 대로 리드타임 가격변동·계약조건·원산지 구성이 포함된다.
  - 그러므로 이를 `basis+운임+보험`이라는 물리적 성분으로 명명하거나 밴드 확대분을 운임·basis 불확실성으로 귀속할 수 없다. 별도 운임 성분이 없으므로 BDI 운임 레짐을 선택적으로 적용했다는 설명도 성립하지 않는다.

- **[높음] ‘스트레스 상계’는 통계적 상계가 아님 — 양 문서**
  - 값은 각각 `1,496−210=1,286`, `1,617−32=1,585`, `1,685+123=1,808`로 동일 분위의 단순합이다.
  - 완전 순위의존은 하나의 의존 시나리오일 뿐, 일반적으로 합계 VaR/분위의 점별 상계를 보장하지 않는다. 자체 표에서도 P10 스트레스값 1,286이 독립값 1,290보다 낮아 “상계”라는 명칭과 모순된다.

- **[높음] 운임 레짐 구간이 중첩됨 — 양 문서**
  - `경계 z>1`과 `급등 z>2`는 z>2에서 동시에 참이다. 구현 순서에 따라 급등이 전부 경계로 분류될 수 있다. 경계 구간은 최소한 `1<z≤2`처럼 배타적으로 정의해야 한다.

- **[높음] 독립 컨볼루션 가정이 잔차 정의와 구조적으로 충돌 — 양 문서**
  - 잔차 \(R=CIF-CBOT\)는 CBOT를 음의 부호로 직접 포함하므로 `Cov(CBOT,R)=Cov(CBOT,CIF)−Var(CBOT)`이다. 경제적 의존성이 없어도 음의 상관이 기계적으로 생길 수 있다.
  - 이 상태에서 CBOT와 잔차를 독립 복원추출하면 실제 공동분포를 보존하지 못한다. 8/19 문서가 상관 검정 부재를 인정했지만 계산은 수정하지 않았다.

- **[높음] 12개 관측치로 산출한 꼬리 분위에 과도한 정밀도를 부여 — 양 문서**
  - 제시된 12개 값은 2025-03부터 2026-06까지 **16개 달력월에 걸친 비결측 12개 관측치**이며, 연속된 12개월 창이 아니다.
  - 일반적인 선형 분위법이면 P10은 정렬된 2·3번째 값, P90은 10·11번째 값 사이 보간에 불과하다. 20,000회 MC는 이 작은 원표본의 정보량을 늘리지 않으므로 달러 단위 분위와 80% 밴드의 안정성을 뒷받침하지 못한다.

- **[높음] ‘평시 분위’가 실제 레짐 조건부 표본인지 확인 불가 — 양 문서**
  - 제시된 -210/-32/+123은 나열된 12개 잔차 전체에 일반적 선형 분위법을 적용한 값과 일치한다. 각 월의 BDI 레짐, 평시로 남은 표본 수, 필터 결과가 없다.
  - 특히 8/19에는 BDI z가 미확보인데도 동일 값을 “평시 분위”로 유지했으므로, 실측 레짐 조건부 분포가 아니라 평시를 가정한 무조건 분포일 가능성이 높다.

## ② 시계열 누수 위험

- **[치명] 계약시점 예측·백테스트에서 도착월 정보가 유입될 위험 — 양 문서**
  - 조달 계약이 도착 40~50일 전에 이뤄진다면 `도착월 CIF`와 `도착월 CBOT 월평균`은 계약시점에는 모두 미래 정보다.
  - 이 값으로 잔차를 만들고 계약시점 모델의 학습 타깃이나 특성으로 사용하면 직접적인 look-ahead가 된다. 또한 잔차에 포함된 계약~도착 CBOT 변동을 현재 CBOT 층과 다시 합산해 변동을 일부 이중계상한다.

- **[높음] 관세청 월자료의 기간 라벨과 실제 가용시점이 분리되지 않음 — 양 문서**
  - `2026-06-01`은 6월 통계의 기간 시작일이지만 보고서 설명상 실제 공표는 약 7월 15일이다.
  - 백테스트에서 6월 1일자로 조인하면 완성된 6월 수입자료를 약 44일 앞당겨 사용한다. `period_month`와 `available_at/release_date`를 별도로 관리했다는 근거가 없다.

## ③ 상관을 인과로 과장한 해석

- **[높음] 음의 잔차를 아르헨티나 수출세·미국 45Z의 결과로 단정 — 양 문서**
  - 최근 실측은 미국·중국·아르헨티나 각 1개 월 수준이고, 잔차는 리드타임 가격변동·계약조건·원산지 구성까지 혼합한다.
  - 통제군, 정책 전후 비교, 원산지별 패널 회귀 또는 계약시점 정렬 없이 “원인”이라고 할 수 없다. 특히 “basis가 CBOT 변동을 흡수”한다는 해석은 `CIF−CBOT` 정의 자체가 만드는 기계적 음의 상관과 구분되지 않았다.

- **[높음] BDI 임계값에 인과적 운임 레짐 의미를 부여 — 양 문서**
  - BDI는 건화물선 지수이고 대두유의 액체 탱커 운임과 시장·선복 구조가 다르다. 8/19 문서도 이를 인정한다.
  - 실제 탱커 운임과의 검증, 시차 추정, 임계값 보정 없이 `z=1/2`를 운임 경계·급등으로 두고 `causal_edges`를 인과 근거로 제시한 것은 방향성 상관 프록시를 인과 규칙으로 과장한 것이다.

## ④ 재현 불가능한 주장

- **[높음] MC 결과를 재현할 핵심 설정과 원자료가 없음 — 양 문서**
  - “시드 고정”이라고만 하고 실제 시드값, 분위 보간법, 반올림 규칙을 제시하지 않았다.
  - CBOT 60개 관측값, Databento dataset/schema·심볼·만기 롤 규칙·UTC 일봉 생성 방식도 없다. 따라서 1,290/1,564/1,770을 독립적으로 동일하게 재산출할 수 없다.

- **[높음] BDI 근거가 두 버전 사이에서 추적 불가능하게 변경됨**
  - 8/14에는 `90일 z=-0.24`, 마지막 관측 `2026-07-01`이 있으나 8/19에는 같은 과거 시점의 BDI가 “미확보”로 바뀌었다.
  - 데이터 빈티지, 소스, 계산 창, 결측 발생 사유가 없어 8/14의 z값과 두 보고서의 평시 판정을 검증할 수 없다.

- **[참고] 외부 실증·검증 수치의 식별정보 부족 — 양 문서**
  - “S&P 실증”, `CE-010·013·016`, `V-001 중앙값 괴리 0.10%`에 제목·발행일·표본기간·계산식·대상 계약이 없다.
  - 특히 0.10%가 절대/부호 포함 괴리인지, 어떤 만기와 정산가를 비교했는지 알 수 없어 해당 정확도 주장을 재현할 수 없다.
