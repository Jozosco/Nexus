# GPT 교차검증 — 2026-08-29 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 33245684148

---

- **[치명] ① 미수집 항목이 있어도 ‘서명된 무소식’으로 판정됨**
  - `if breach: ... else:` 구조라서 `breach == []`이면 `watch`의 존재와 무관하게 “감시 변인 전체가 기준 범위 안”, “서명된 무소식”을 출력한다.
  - 같은 분기에서 `watch`를 “미수집”으로 표기하므로, `breach=0, watch>0`이면 경보 불가능한 항목까지 정상 판정을 통과한 것처럼 표시된다. 극단적으로 전 항목 미수집이어도 무경보로 보일 수 있다.
  - 무소식 판정은 `not breach and not watch`일 때만 허용하고, 그 외에는 “판정 불가/미수집” 상태로 분리해야 한다.

- **[높음] ① `BDI_ZSCORE`의 단위가 원지수로 오표기됨**
  - `BDI_ZSCORE` 표시명이 `"BDI 해상운임 z"`에서 원자료와 동일한 `"BDI 해상운임지수"`로 바뀌었다.
  - 해당 값은 무차원 z-score인데 BDI 지수 포인트처럼 보이게 된다. 특히 경보 카드의 현재값·기준값을 잘못 해석하게 만든다.

- **[높음] ①·④ CBOT ‘종가’와 ‘정산가’를 혼용하고 교차검증을 근거 없이 주장함**
  - 데이터 키와 객체는 계속 `CBOT_BO_CLOSE`, `CloseKpi.close`, “종가”를 사용하지만, 출력만 `"CME 정산가"`, `"CME 정산가 기준"`, `"정산가 교차검증을 거친 종가 계열"`로 변경됐다.
  - 선물의 최종 체결 종가와 공식 정산가는 서로 다른 값일 수 있다. 이번 diff에는 정산가 소스 선택이나 종가와의 교차검증 코드·검증 결과가 없다.
  - 따라서 실제 수치 정의가 불명확하고 “교차검증” 주장도 재현할 수 없다.

- **[높음] ②·④ 시점 규율에서 `available_at`을 제거하고 ‘확정값’으로 바꾼 것은 동치가 아님**
  - 기존의 `available_at ≤ 발행 시점`은 시점 가용성을 검사할 수 있는 규칙이지만, 새 문구의 “발행 시점 이전에 확정된 값”은 잠정치·수정치·최종치를 구분하지 못한다.
  - FRED·관세청 자료처럼 사후 수정되는 계열을 최신 빈티지로 재실행하면, 과거 발행 시점에는 없던 수정값이 들어가는 look-ahead가 발생할 수 있다. 장 마감 자료의 하루 지연만으로는 이를 막지 못한다.
  - 발행 당시 빈티지와 `available_at` 조건을 보존·기록하지 않으면 동일 보고서를 재현할 수 없다.

- **[높음] ①·④ 모든 예외를 데이터 부재로 오진함**
  - `_analogue_block`은 모든 `Exception`을 잡아 `results=[]`로 만든 뒤, 사용자에게는 “분석 데이터 미가용 또는 대상 변수 부재”라고만 출력한다.
  - 계산 버그, 스키마 변경, 형 변환 오류도 데이터 부재로 위장되며, 이어지는 “CI 실행에서 생성됨” 주장도 보장되지 않는다. 실패 원인별 강등 또는 명시적인 산출 오류 상태가 필요하다.

- **[참고] ① P10~P90의 분위수 의미가 잘못 표현됨**
  - `ret_p10`과 `ret_p90`을 `"하위~상위 10%"`로 표시했지만, 두 값은 각각 10백분위와 90백분위 경계이며 그 사이 구간은 경험분포의 중앙 80%다.
  - 산출 근거의 `"하위 10%·중앙값·상위 10% 지점"`도 집단과 분위수 지점을 혼용한다. `P10·P50·P90` 또는 `10·50·90백분위`로 표기해야 한다.

- **[참고] ① 하락률에 음수 부호와 하락 표현을 중복 적용함**
  - 하락 분기에서 `f"한 주간 {kpi.wk_pct:+.1f}% 하락"`을 사용하므로 `wk_pct=-2.0`이면 “한 주간 -2.0% 하락”이 된다.
  - 방향어를 사용할 때는 `abs(kpi.wk_pct)`로 “2.0% 하락”이라 해야 수학적 이중 방향 해석을 피할 수 있다.

- **[참고] ①·④ 기간 단위와 z 산식 정보가 출력에서 소실됨**
  - `_H_LABEL`이 정확한 `5/20/60거래일`을 모두 “약 1주/1개월/3개월”로 바꿔 Markdown 결과만으로는 실제 계산 horizon을 알 수 없다.
  - 스냅샷 설명도 `"90일 롤링"`에서 `"90일 기준"`으로 바뀌어 롤링 여부와 90거래일·90관측치·90캘린더일 중 무엇인지 불명확하다. z 기반 유사 시기 선택을 동일하게 재현하기 어렵다.
