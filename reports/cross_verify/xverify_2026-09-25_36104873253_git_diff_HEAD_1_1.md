# GPT 교차검증 — 2026-09-25 [✅ 판정]

- 모델: gpt-5.6-sol (effort=xhigh)
- 대상: git diff HEAD~1
- 런: 36104873253

---

## ① 수치·단위·논리 오류

- **[높음] 복귀일의 ‘첫 5거래일’ 정의가 실제로는 최대 6거래일임**
  - 근거: `_recovery_days()`의 `path[:min(initial_window, path.size - 1) + 1]`은 `INITIAL_REACTION_DAYS=5`일 때 `k=0~5`, 즉 6개 거래일을 탐색한다.
  - 보고서는 “첫 5거래일 내 절대값 최대 이동”이라고 명시하므로, 초기 반응점과 복귀일·복귀 빈도가 잘못 산출될 수 있다.

- **[높음] 대형 충격 회귀계수가 ‘반응’으로 잘못 표기됨**
  - 근거: 대형 충격 사양은 `d_pos`, `d_neg`와 절편을 넣으므로 해당 계수는 정상일 대비 조건부 차이이지, +2σ/−2σ 충격일의 총수익률 또는 절대 반응이 아니다.
  - 그런데 보고서는 이를 `+2σ 초과일 반응`, `−2σ 미만일 반응`으로 표시한다.

- **[높음] 결측 충격을 정상일로 코딩하고, 표시된 극단일 수도 실제 회귀 표본과 다름**
  - 근거: `(shock > 2).astype(float)`와 `(shock < -2).astype(float)`는 `shock=NaN`도 0으로 만든다. 현재 충격값이 없는 날 일부가 정상일 기준군에 포함될 수 있다.
  - `n_pos`, `n_neg`는 `y`만 결측 제거해 세지만 회귀는 시차 통제변수까지 결측 제거한다. 따라서 표의 `[일수]`는 실제 추정에 사용된 극단일 수가 아니다.
  - 극단일이 0건 또는 극소수여도 추정을 막는 검사가 없어 0에 가까운 계수와 비정상적으로 좁은 구간이 출력될 수 있다.

- **[높음] M1 사건 연구가 불필요하게 TE 데이터 범위에 의해 잘림**
  - 근거: `build_panel()`은 `end=min(zl.index.max(), te.index.max())`로 패널을 자르고, M1도 이 패널의 ZL을 사용한다.
  - 동반 TE 계열이 일찍 끝나면 더 긴 ZL 가격 이력이 있어도 +60일·120일 복귀 관측이 잘리며, 최근 사건의 복귀 여부가 달라질 수 있다.

- **[높음] ‘비치명’ 실패 처리가 실제로는 후속 분석을 중단시킴**
  - 근거: 워크플로는 신뢰도 산출 실패를 `비치명`이라고 기록하지만 바로 `exit "$rc"`를 실행한다. `continue-on-error`도 보이지 않으므로 같은 잡의 후속 C-03 분석 단계가 실행되지 않는다.

- **[높음] G1 입력 기반 설명이 실제 입력과 일치하지 않을 수 있음**
  - 근거: `load_inputs_diag()`는 TE parquet가 없거나 26개보다 적어도 항상 `수동 업로드 26지표`라고 기록한다.
  - 또한 기존의 `Databento UTC 종가` 표기를 `이력 종가`로 바꿔, 실제로는 정산가가 아닌 UTC 일봉을 사용했다는 핵심 차이를 보고서 헤더에서 숨긴다.

- **[참고] 뉴스 중복 제거가 콘텐츠가 아니라 소스 순서에 따라 신호 분류를 바꿈**
  - 근거: `seen_titles`가 모든 RSS 지표에 전역으로 적용되고 지정학 레인이 상품 레인보다 먼저 순회된다.
  - 대두유·제재를 동시에 다룬 동일 기사는 `RSS_REUTERS_GEO`에 먼저 잡히면 상품 뉴스 지표에서는 제거된다. 따라서 지표별 기사 수와 분류가 `RSS_SOURCES` 선언 순서에 종속된다.

## ② 시계열 누수(look-ahead) 위험

- **[높음] 같은 런의 당일 기사를 투입하면서 시장 마감 시각 컷오프를 적용하지 않음**
  - 근거: 워크플로가 `daily-unstructured-digest`를 선행시키고 같은 런의 CSV를 C-03에 복원한다. 수집 코드는 2일 날짜 컷오프만 보이며 14:20 ET 등 보고서 기준시각 검사는 없다.
  - 같은 코드의 규칙 설명은 “마감 후 수집분은 +1일”이라고 되어 있으므로, 마감 후 기사가 당일 신호·브리프에 들어갈 수 있다.

- **[높음] 진단 입력 폴백이 발표시점·빈티지를 보존하지 않은 데이터를 역사 검증에 사용함**
  - 근거: `load_inputs_diag()`는 최신 TE parquet를 `price_date`로 피벗한 뒤 종가 달력에 단순 전방 채움한다. `available_at`, 최초 발표값, 개정 이력 또는 당일 발표시각을 검사하지 않는다.
  - 이 값을 미래 20일 수익률에 대한 H1/H2 검증에 사용하므로, 개정된 과거값이나 당일 종가 이후 확정값이 과거 시점에 존재했던 것처럼 들어갈 수 있다. 마트 실패 시 이 폴백이 정규 결과로 발행된다.

- **[높음] 사건 날짜에 시각·거래세션 컷오프가 없음**
  - 근거: `searchsorted(event_date)`는 사건이 거래일에 발생하면 발표 시각과 무관하게 그 날짜의 UTC 일봉을 `t0`로 사용한다.
  - 장 마감 후 발생한 사건은 실제 첫 반응 거래일이 다음 거래일이어야 하지만, 현재 구현은 사건 전 가격이 포함된 당일 바를 “당일 반응”으로 분류할 수 있다.

- **[높음] M2의 같은 날짜 충격과 ZL 수익률 사이 선후관계가 검증되지 않음**
  - 근거: Brent·BDI·난방유의 `price_date`만 ZL UTC 일봉과 맞추고 발표·마감 시각은 사용하지 않는다. 종속변수에는 충격일 당일 ZL 수익률이 포함된다.
  - 프록시 값이 ZL 바 종료 후 확정되는 경우 같은 날짜 값은 실시간 이용 불가능한 정보다.

- **[참고] 사건 백분위와 ±2σ 기준이 사건 이후 자료를 포함함**
  - 근거: 사건 백분위는 전체 패널의 무조건부 분포로 계산되고, σ도 2010~2025 전체 표본으로 고정된다.
  - 순수 사후 기술에는 가능하지만, 당시 시점의 이상도·검증값으로 재사용하면 미래 변동성 및 미래 관측치를 사용한 점수다.

## ③ 상관을 인과로 과장한 해석

- **[높음] 국소투영 결과를 ‘충격에 대한 반응’으로 명명하지만 외생 충격을 식별하지 않음**
  - 근거: 설명변수는 원자료 Brent·BDI·난방유의 동시적 일간 변화이며 도구변수, 고빈도 식별, 서사적 외생 충격 또는 공통 요인 통제가 없다.
  - 시차 통제만으로는 ZL과 프록시가 환율·거시·위험회피·수급 뉴스에 동시에 반응하는 경우를 분리할 수 없다. 한계 절의 면책과 별개로 제목·표·판독의 “반응계수”는 실제 추정량보다 강한 인과적 표현이다.

- **[높음] 아르헨티나 사례의 ‘완전 상쇄’와 ‘충격 크기를 결정’ 주장은 제시된 수치로 입증되지 않음**
  - 근거: 브라질 생산량 `1억 5,300만 톤`이라는 총량만으로는 아르헨티나 감소분을 상쇄했는지 판단할 수 없다. 브라질의 전년 대비 증가분, 세계 공급 변화, 재고·수요가 필요하다.
  - 단일 시기의 가격 움직임만으로 “대체 공급국 완충 여부가 충격 크기를 결정”했다고 결론 내리는 것도 동시기 요인을 통제하지 않은 인과 주장이다.

## ④ 재현 불가능한 주장

- **[높음] ‘사전 고정 사건 목록’이라는 주장을 검증할 기록이 없음**
  - 근거: 사건 목록과 분석 코드가 같은 변경에서 처음 추가됐고, 사전 등록 문서·커밋·선정 규칙·제외 기준이 없다.
  - 결과를 본 뒤 사건을 선택하지 않았다는 점을 재현할 수 없으며, 사건 11건의 양(+) 빈도 등은 목록 선택에 민감하다. 사건 사실·정확한 발생시각에 대한 출처도 없다.

- **[높음] 입력 파일 설명과 실제 선택 방식이 다르고 실행일도 고정값임**
  - 근거: 모듈 설명은 `ZL_ohlcv-1d_*.csv`를 데이터 기반으로 적지만 실제 코드는 `ZL_ohlcv-1d_2010-06-06_2026-08-12.csv` 한 파일만 읽는다.
  - `REPORT_DATE="2026-09-25"`도 실제 실행일과 무관하게 고정되어 있어 이후 재실행해도 동일 생성일자로 덮어쓴다. TE parquet의 버전·해시도 기록하지 않아 같은 커밋이라도 파일 갱신 시 결과가 바뀐다.

- **[높음] 같은 런의 비정형 입력 복원이 결정적이지 않음**
  - 근거: `find data/raw -name ... | head -n 1`로 첫 파일을 고르므로 여러 아티팩트 사본이 있으면 선택 결과가 파일 탐색 순서에 좌우된다.
  - 행 수만 기존 파일과 비교하므로 더 최신인지, 같은 런의 파일인지, 중복·손상 파일인지 검증하지 못한다. 원본 아티팩트도 90일 후 삭제되므로 이후 동일 보고서 입력을 복원하기 어렵다.

- **[높음] 도착가 입력 선택이 데이터 출처가 아니라 ‘가장 늦은 날짜’에 의존함**
  - 근거: 재귀 탐색한 모든 `cbot_session_close.parquet` 중 `index.max()`가 가장 큰 파일을 선택한다. 현재 런 아티팩트 여부, 전체 기간 길이, 단위, 중복일자, 미래 오기입을 확인하지 않는다.
  - 잔존한 부분 파일이나 잘못된 미래 날짜 파일 하나가 USc/lb로 간주되어 도착가 USD/톤 계산을 바꿀 수 있어, 작업공간 상태에 따라 결과가 달라진다.
