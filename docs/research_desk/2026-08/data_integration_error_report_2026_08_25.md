# Data Integration 오류 보고 (R-016 표준 형식) — 2026-08-25

**대상**: Data Integration 파이프라인 런 #76(8/23)·#77(8/25) — 2런 연속 동일 실패
**요약**: 커넥터 12종 전부 정상. 실패는 **게이트 2건(4개 원인)** — 전부 코드 수정으로
당일 해소(커밋 참조). 외부 API 발급·수작업이 필요한 항목 없음.

| 제목 | 내용 | 예상되는 원인 (가능성 높은) | 지원 필요 사항 |
|---|---|---|---|
| ①-a pytest — crop_data 미래 available_at 871건 | `AssertionError: [crop_data] 미래 available_at 871건` (2런 동일 — 신규 수집 아닌 스탬프 규칙 결함) | WASDE 커넥터의 마케팅연도 **전망** 행(2026/27, price_date=2026-10-01)에 same_month 규칙이 "기간의 달 12일"을 발표일로 파생 → 2026-10-12 미래 스탬프. 실제 발표는 8/12였음 | **해소(코드)** — asof.py에 전망 행(event_time>ingested_at) 캡 도입: available_at ≤ 수집 시점. 일별 관측 행(VIX 등 장중누수 방지 규칙)은 조건부라 불변 |
| ①-b pytest — production_data 동일 30건 | 동일 오류, NASS 2026 전망 행(전국 1+주 29) | ①-a와 동일 경로(기본 규칙 lag 1일이 미래 기간에 적용) | **해소(코드)** — 동일 수정으로 동시 해소 |
| ①-c pytest — 관세청 HS 검증 임포트 사망 | `ModuleNotFoundError: No module named 'httpx'` | 8/18 수정(A-183)이 HS 목록을 커넥터에서 import하도록 바꿨는데 pytest 잡 의존성에 httpx 누락 — **수정이 유발한 회귀** | **해소(코드 1줄)** — pytest 잡 pip에 httpx 추가 |
| ② Model Readiness — BDI 커버리지 56% 차단 | `④ BDI — 커버리지 56% (게이트 85% 미달)` → G1 skipped 지속 | **지표코드 선점**: 준비도 검사가 후보를 커버리지가 아닌 **순서**로 채택 — CI에서 API 수집 BDI(2017~, 56%)가 수동 9개년 TE_BDI(100%)를 가림. 8/18의 재구성 스텝은 정상 실행됐으나 참조되지 않음(당시 원인 오진) | **해소(코드)** — 피처는 커버리지 최대 후보 채택으로 변경(타깃은 계약 순서 유지). 로컬 검증: BDI가 차단 목록에서 소멸 |

### 참고 — 실패 원인이 아닌 관찰 항목

| 항목 | 상태 |
|---|---|
| 관세청 401 간헐(291/800 호출) | **기지 이슈**(키 상태) — 폴백으로 10,591건 저장, 잡 green. 조치는 여전히 조정자 수동(포털 활용신청 갱신 → `DATA_GO_KR_SERVICE_KEY` 교체) |
| 8/18 수정 3건(CBOT openpyxl·관세청 연.월 파싱·crop_data 녹색 실패) | **전부 해소 확인** — 최신 런 로그로 실증 |

### 판정 기준

다음 스케줄 런(KST 05:30)에서 ①pytest 게이트 green ②Readiness green ③**C-03 G1 변수
중요도 잡의 skipped 해제**가 확인되면 종결. G1 일별 경보판 가동의 단일 차단점이던
BDI 선점이 해소되므로 경보판 첫 가동이 기대됨.
