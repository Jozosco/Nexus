# Data Integration & Reporting Pipeline — 지난주 실패 분류·근거·조치 (2026-09-07)

**요청**: 승인자 — 지난 한 주 파이프라인이 한 번도 완전 성공하지 못함. G1 리포트 산출에 실질 영향을 주는 것과 아닌 것을 분류하고,
근거와 조치를 보고. **판정 근거**: Actions 런·잡 API 실측(6런·잡별 결론) + 코드 대조 + 잡 로그.

## §1 한 줄 결론
**G1 브리프는 6일 중 5일 실제 발행됐다.** 런 결론 'failure'는 G1 체인과 무관한 품질 테스트 잡(6일 전부)과 관세청 잡(2일) 실패가 만든
표시이고, **실제로 브리프가 안 나온 날은 9/7 하루** — USDA 장애가 필수 산출물 누락으로 판정돼 게이트가 체인을 막은 경우다.

## §2 런별 실측 (CONFIRMED — Actions 잡 API)

| 런 | KST 발화 | 실패 잡 | G1 분석 잡 | 브리프 | 판정 |
|---|---|---|---|---|---|
| #88 | 9/2 07:43 | 품질 테스트 | 성공 | 발행 | 비영향 |
| #89 | 9/3 07:45 | 품질 테스트 | 성공 | 발행 | 비영향 |
| #90 | 9/4 07:43 | 관세청 HS1507(38분)·품질 테스트 | 성공 | 발행 | 비영향(관세청은 필수 목록 밖) |
| #91 | 9/5 07:26 | 관세청·품질 테스트 | 성공 | 발행 | 비영향 |
| #92 | 9/6 07:08(일·임시 cron) | 품질 테스트 | 성공 | 발행 | 비영향 |
| **#93** | 9/7 07:12 | **WASDE(1h30m)** → 품질 게이트 REJECTED → 분석·준비도·G1 전부 skip | **skip** | **미발행** | **영향** |

## §3 분류·근본 원인·조치

### A. G1 산출에 영향 — 1건
| 실패 | 근본 원인 | 조치(적용 완료) |
|---|---|---|
| USDA FAS PSD 403→양 호스트 타임아웃→벌크 zip 타임아웃→ARMS 리다이렉트→0건→잡 실패→`crop_data` 필수 누락→REJECTED→G1 skip | ①외부 장애(USDA 양 호스트) ②**ARMS 302를 따라가지 않는 클라이언트 결함**(리다이렉트 본문을 JSON 해석) ③일별 런이 2010~현재 17년을 매일 호출해 장애 시 최대 50분 공회전 ④**G1이 쓰지 않는 산출물(crop_data)이 필수로 강제** — G1의 작황·수급 입력은 수동 WASDE·PSD parquet(준비도 잡이 재구성, 피처 계약 438개 중 crop_data 유래 0건) | 리다이렉트 추적 · 일별은 당해 연도만 호출 · 벌크 120초·잡 30분 상한 · **필수 목록에 선택 표식(`crop_data?`)** — 잡 실패는 여전히 빨간불로 표면화하되 G1 발행은 막지 않음 |

### B. G1 산출에 비영향 — 3건
| 실패 | 근본 원인 | 조치 |
|---|---|---|
| 품질 테스트 3건(해운·상품 '미래 price_date 19~21건', 통합 검사) — 6일 연속 | 건수가 9/5 19건→9/7 21건으로 **날마다 증가** = 수집일 기준 전방 창을 만드는 원천이 섞여 있음(진짜 미래 일자라면 감소해야 함). 두 커넥터 공통·동일 건수 → 공통 경로(Trading Economics historical) 유력(INFERENCE — 코드에 `price_date ≤ 수집일` 클램프 부재). 통합 검사는 같은 행을 다시 세는 중복 판정. **G1 잡은 이 테스트 잡에 의존하지 않음**(5일 실측) | 저장 직전 관측 계열 미래 행 제거 + 지표별 건수 로그(다음 런에서 원천 확정) · 테스트는 정당 경보였으므로 유지 |
| 관세청 HS1507 잡 38분 실패(2일) | data.go.kr 시간대 장애 패턴 — 필수 목록 밖·수동 업로드본이 히스토리 커버 | 변경 없음(관찰 유지) |
| 임시 주말 cron 잔존 | 8/31 리허설용이 제거되지 않아 주말 2회 실패가 '한 주 실패' 인상을 키움 | 제거 |

### C. 구조 관찰
런 결론이 "한 번도 성공 못 함"으로 보이는 이유는 비차단 잡의 실패도 런 전체를 failure로 만들기 때문이다. **발행 여부의 정본은 서명 스탬프
(E1)와 브리프 아티팩트**이며, 스탬프는 6일 전부 커밋됐다(9/7은 '게이트 REJECTED'로 정직 기록). 이후 판정은 오늘 밤 스케줄 런.

## §4 오류 보고 표 (R-016)
| 제목 | 내용 | 예상 원인 | 지원 필요 |
|---|---|---|---|
| USDA FAS API 장애(9/7) | PSD·ARMS·벌크 전 소스 실패 | 외부 서비스 장애(양 호스트) + 클라이언트 리다이렉트 결함 | 없음(코드 조치 완료) |
| 해운·상품 미래 일자 | 저장 직전 클램프로 차단, 원천은 다음 런 로그로 확정 | TE 응답 전방 행(추정) | 없음 |

## §5 조치 실효 판정 (2026-09-09 추가 — CONFIRMED, Actions 잡 API·로그)

| 런 | KST 발화 | 실패 잡 | G1 분석 잡 | 브리프 | 판정 |
|---|---|---|---|---|---|
| #94 | 9/8 07:57 | 관세청 HS1507 1건(ConnectTimeout 37분 — 시간 예산 1,800초 작동 후 정상 종료) | 성공 | 발행 | 비영향(관세청은 필수 목록 밖) |
| **#95** | 9/9 07:52 | **없음 — 20개 잡 전부 성공(정식 가동 이후 첫 전 초록 런)** | 성공 | 발행 | — |

- **미래 일자 원천 확정**: 저장 직전 차단 로그가 `shipping_indices: 수집일(2026-09-08) 이후 price_date 17건 제거 — BDI 17건`,
  `commodity_data: … CPO_USD_MT 17건`을 남김 → §3-B의 추정(Trading Economics historical 전방 창)이 **실측으로 확정**됨.
  품질 테스트는 두 런 모두 통과(30 passed).
- **WASDE 잡**: 1시간 30분 실패 → **2분 17초 성공**(당해 연도만 수집·리다이렉트 추적·잡 상한 30분) → 작황 산출물 정상 발행, 게이트 통과.
- **관세청 잡**: 9/8은 data.go.kr 연결 시간 초과가 37분 지속됐으나 예산 초과 시 정상 종료 규칙으로 체인을 막지 않음. 9/9는 3분 15초 완주.
- **서명 스탬프 원장**: 9/7·9/8 모두 `⚠️ 경보 1건·게이트 PASS` 기록 — 검사된 무소식과 침묵의 구분이 유지됨.
- 잔여 관찰: 마트 조립 단계가 일별 관세청 API 산출물(`customs_import_*.parquet`)을 "필수 컬럼 없음"으로 제외함 — 현재는 업로드본
  parquet이 2026-08까지 커버해 영향 없으나, 당년 증분은 마트에 반영되지 않음(후속 과제로 등재).

