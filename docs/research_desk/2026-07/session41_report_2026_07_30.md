# Session 41 — G1 정밀화 종합 보고 (수집 오류·미수집 원인·리포트 구조 개편)

**작성일**: 2026-07-30 · **참여**: C-01(종합) · C-03(주도) · C-02·04·06·08 · P1-01~06

---

## 1. 수집 실패·부분수집 — 원인·수정 (완료)

### 1.1 BOK ECOS ERROR-100 (실패)
- **원인 3중**: ①키 미(未)strip — 시크릿 끝 공백/개행이 URL 경로 세그먼트를 오염("필수값 누락"의
  전형적 원인) ②요청건수 5000 < 2010~2026 일별 ~6,000행 ③`run()`이 2010 기준선 미반영(2020 기본).
- **수정**: `.strip()` + 요청건수 100000 + `HISTORICAL_START_YEAR`(기본 2010) 반영.

### 1.2 KOSIS 부분수집 (22003=198건 원시 수신, 저장 0 / 22002=0건)
- **원인(확정)**: `outputFields`에 **`DT`(관측값 필드)가 빠져** 있었음 — 레코드 198건은 도착하나
  값 파싱이 전부 None → 저장 0건 → "수집 실패" 경고. 22002는 `objL1=""`(분류값 필수 테이블).
- **수정**: `DT+`·`ITM_ID+` 필드 추가(커넥터+헬스체크), 22002는 `objL1=ALL`(전체 분류).
- 참고: 198건 = 2010.01~2026.06 월 수와 정확히 일치 — **조회 범위 자체는 15개년 정상**이었음.

## 2. G1 '미수집' 카테고리 — 근본 원인·빠른 해법 (Req 1.2)

| 카테고리 | 근본 원인 | 빠른 해법 |
|---|---|---|
| 해운지수(BCAA/BDI) | API 게이트(설계상 스킵) + **TE_BDI가 이미 대체** | 중복 행 제거(완료) — BDI는 TE 15개년으로 분석 반영 중 |
| 작황(WASDE/PSD **API**) | API 게이트 — **수동 WASDE 101지표·PSD 27지표가 대체** | 중복 행 제거(완료) |
| 수입통계(관세청 HS1507 **API**) | API 게이트 + 포털 점검(7/29~8/2) | GW 확장 수집으로 대체(8/3) |
| 수입통계(관세청 GW 확장) | **8/3 이후 실행 예정**(점검 기간) + 신규 키 필요 | 8/3 Actions `connector=customs-gw` |
| 정책신호(GAIN PDF)·수급전망(FAO AMIS) | 잡은 실행되나 **FILE_PATTERNS 등록 시점 차이**로 리포트 미반영 가능 + gain_historical은 정성 메타 | 금회 등록 정비 — 차기 실행 시 반영. FAO/GAIN **요약 md 생성 착수**(§5) |
| 지정학(GPR/호르무즈) | GPR 잡은 비게이트(공개 xlsx) — 실행 로그 확인 필요(호르무즈는 실시간 전용이라 Historical 부재 정상) | 차기 실행 로그 확인 → openpyxl 등 의존성 실패면 즉시 수정 |
| 지정학 인텔리전스(GeoIntel) | **실시간 전용**(BACKFILL 스킵 — 설계) | Historical에선 부재 정상 — 행 제거(완료) |

**구조적 정비(완료)**: ①중복·설계상 부재 행이 "미수집"으로 표기되던 노이즈 제거 —
API 선택 8종은 부재 시 1줄 요약("include_api=true 시 수집")으로 통합(Req 1.1) ②데이터 범위
불일치는 2010.01 기준선 정합(§3)과 소스별 한계 명시로 처리.

## 3. 분석창 확정 — 2025-12 종료 (Req 1.3.1, 적용 완료)
- **결정**: 추정창 **2010-01~2025-12** · 2026 상반기(호르무즈)는 **이벤트 검증 창**으로 분리.
- 근거·기각 대안: `reports/market/analysis_window_rationale.md` (미완결 극단 이벤트의 계수 오염
  방지 + 결과 일관성 + out-of-sample 검증 가치).
- 구현: `ANALYSIS_END_DATE=2025-12-31`(env 재지정 가능) — 상관·중요도·Granger ①② 적용,
  실시간 경보는 최신 데이터 유지.

## 4. 리포트 구조 개편 (Req 1.4, 코드 반영 완료)
| # | 요청 | 반영 |
|---|---|---|
| 1.4.1 | 변수 출처를 부록으로 | ✅ 변수 카탈로그 → **부록**(맨 뒤), 본문 간결화 |
| 1.4.2 | Granger 사전조건 분류(12~51p) 삭제 | ✅ 본문 제거 |
| 1.4.3 | 연도 2017 시작 오류 + 3단 분석 | ✅ 2010 기준선 수정 + **①전체(2010~25)→②5년 구간→③이벤트 연도(2012·2018·2020·2022·2025)** — C-03 설계 근거는 analysis_window_rationale.md |
| 1.4.4 | 인과관계 전체 → src/semantic | ✅ `src/semantic/causal_chains.md`(18체인 전체) — 본문은 핵심 6체인만 |
| 1.4.5 | 방법론·임계근거 → 별도 파일 | ✅ `reports/market/methodology_feature_selection.md` · `threshold_rationale.md` — 본문 제거, 푸터에 참조 경로 |

## 5. 비정형 처리 실동작 검증 (Req 3, 실행)
- `scripts/summarize_pdfs.py` 신규 — **각 PDF 옆 동일 폴더에 `{원본명}.md` 요약** 생성.
  구성 = C-04(판독 메타·식용유 섹션 발췌) + P1-05(신호 태그 8종·방향성 힌트) +
  P1-06(entities.yaml 사전 기반 정규 엔티티 후보) + provenance(원문 발췌).
- **FAO AMIS 137건**: 전량 실행(결과는 각 연도 폴더에 저장·커밋).
- **GAIN 1,234건**: 로컬 전량은 과중 — Actions `gain-pdf` 잡에서 요약 단계 실행 권장(다음 실행).
- 참고: 발췌는 원문 그대로(provenance) — LLM 해석 요약(P1-05 Phase B)은 FinBERT/LLM 도입 시 고도화.

## 6. _reference 2010~ 기준선 (Req 2)
- 전면 재작성 대신 **부록 방식** 채택: `addendum_2010_2016_extension_plan.md` —
  2010~11 라니냐·식량위기 / 2012 미 대가뭄 / 2014~16 공급과잉 3개 사례 보충(⚠️ 초안, 15개년
  백필 후 수치 재검증) + 데이터 부족 구간 대응 계획. 원문 수치 갱신은 백필 리포트 확정 후 일괄.

## 7. 다음 실행 (조정자)
1. dev→main 병합 후 **Historical Analysis Pipeline** 재실행 — 위 수정 전부 반영된 리포트 생성.
2. 8/3: `connector=customs-gw` 실행(관세청 확장 수집).
3. 헬스체크 재실행 → BOK·KOSIS 15개년 수집 확인.
