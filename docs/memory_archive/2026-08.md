# MEMORY Archive — 2026-08
> 2026-08 세션 학습 아카이브. 월별 갱신 원칙(조정자 지시).

| Date | ID | Category | Issue | Resolution |
|---|---|---|---|---|
| 2026-08-10 | A-080 | Bug | 데이터 API 4종 오류 일괄 수정 | ①USDA PSD 500 → api.fas.usda.gov 폴백 체인(A-076 유지) ②ARMS 404 = 리포트 조합 미제공(서비스 정상) → 경고 후 빈 결과(치명 오류 처리 제거) ③CDS 308 = URL 후행 슬래시 → 제거 ④관세청 JSONDecodeError = **API가 XML 응답**인데 r.json() 호출한 구조적 오류 → resultCode 00 검사·item 카운트로 교체(응답 자체는 정상이었음) |
| 2026-08-10 | A-081 | Feature | Databento CBOT ZL(BO=F) 15개년 수집 구축 | 조정자 가입·키 등록 완료(DATABENTO_API_KEY). scripts/ingest_databento_bo.py: GLBX.MDP3·ZL.c.0(연속)·ohlcv-1d, 가격 1e-9 스케일 변환, 출력 data/raw/Databento/GLBX.MDP3/{csv,xlsx}+parquet. historical_backfill에 databento 잡 추가(connector=databento) |
| 2026-08-10 | A-082 | Feature | NASS 수집 범위 2017→2010 + 비정형 summary 폴더 체계 | NASS year__GE를 HISTORICAL_START_YEAR(기본 2010) 기반으로 변경. summarize_pdfs.py: 요약 저장을 카테고리 루트 summary/{연}/{월}/ 로 이동(조정자 지정) + summary_index.csv 코퍼스 인덱스(신호·엔티티·방향성) 생성 — C-03 시계열화 투입 기반 |
| 2026-08-10 | R-010 | Report | WBS_0810.xlsm — 금주 작업범위 시트 신설 | Schedule 21행(금주 Databento·관세청GW·API수정·GATS 수기·GAIN 요약), 데이터_유무료(Databento 확보 반영), 지연위험 6건(R3 외부 API 신규), 금주_작업범위 8건. 팀장 브리핑용 |
| 2026-08-11 | A-083 | Bug | Databento 128·gain-pdf 0건·헬스체크 타임아웃 근본원인 일괄 | ①Databento: 수집 성공 후 push 403 = historical_backfill.yml **permissions 블록 부재**(GITHUB_TOKEN 읽기전용) → contents:write 추가 ②gain-pdf: 연/월 재정리 후 비재귀 glob → rglob(Summary 제외) ③한국 3기관 ConnectTimeout: 미국 러너↔한국 호스트 간헐 지연 + 단발호출 → 60s+3회 재시도 헬퍼 ④CDS 308/404: v2 API 폐기 + 실수집원은 Open-Meteo(CDS 미사용) → 체크 교체 ⑤ARMS: api.ers 후보 체인+404 비치명 ⑥PSD 헬스체크에도 api.fas 폴백 |
| 2026-08-11 | A-084 | Feature | 비정형 요약 v2 — 템플릿 정합·Summary 스킴·삭제 정리 | '비정형데이터 요약본 Template.md' 자동화 서브셋(메타·판단표·국가·신호·근거발췌 + 서술섹션 '미확인' 원칙4). 저장: FAO {Y}/Summary/·GAIN {Y}/{M}/Summary/. 인덱스 data/processed/unstructured_index_*.csv. 구 산출물 316파일 삭제. FAO 137건 v2 재생성(실패 0) |
| 2026-08-11 | C-012 | Architecture | LLM 전면 교체 — Gemini 배제·OpenAI 교차검증 상시 | 조정자 지시(성능 급락): P1-05/06 secondary=gpt-5.6-sol. config/llm_cross_validation.json 신설 — 전 분석(정형+비정형) 교차검증 gpt-5.6-sol/luna·Reasoning Pro·effort high/xhigh 상시. GATS Oilseeds 폴더 트리(1507.10.0000·1517.90.4035·1507.90/.4050·.4020) 생성, 루트 PDF 138건·GATS csv 이동 단계 추가 |
| 2026-08-11 | PM-035 | PM Session | Session 43: permissions 근본수정(A-083), gain rglob, 헬스체크 재시도·폴백 4종, WASDE 2010 기본, 요약 v2+템플릿(A-084), LLM 교체(C-012), GW 공백=빈 템플릿 규명 | 다음: 병합 후 databento·gain-pdf·customs-gw 재실행 → 헬스체크 검증 → 통합 관계분석(8/14~) |
| 2026-08-11 | A-085 | Bug | 워크플로우 4종 오류 근본원인 | ①GAIN "No /Root object" = **72건이 `<DOCUMENT SAFER>` DRM 래퍼**(WASDE A-051 동일) — 파서 결함 아님. 매직바이트 사전판별로 스킵·리포트 ②customs reindex ValueError = 동일(연,월)이 복수 HS 행으로 중복 → 월 단위 sum 선행 ③Databento 422 dataset_unavailable_range = 라이선스 미포함 실시간 근접 구간 → 종료일 T-1 클램프 + 응답 권장일 파싱 자동 축소 재시도 ④KOSIS·관세청 ConnectTimeout 재발 = 러너 IPv6 경로 블랙홀 추정 → local_address=0.0.0.0 IPv4 강제(헬스체크+커넥터) ⑤ARMS는 api.ers 신규 스키마{status,data,errors} → 0건 단정 제거 |
| 2026-08-11 | A-086 | Data | GATS 재수출 결측 = 대부분 '무역 부재'(정상 0) | verify_gats_reexport_gaps.py: 1507.10.0000(2010·2016·2018)은 **전 국가 0 = 실제 무역 부재** → 0으로 보존(대체 금지, D4). 1507.90.4020·1517.90.4035 결측연도는 '미보고'(행 없음) → NaN 유지. HTS≠HS 구분(4020 1회정제·4050 완전정제·1517.90.4035 완전경화) README 반영 |
| 2026-08-11 | A-087 | Feature | 핵심 용어 사전집 → 시맨틱 레이어 v2 변환 (ERD §8) | '비정형 분석용_핵심 용어 사전집_v1.0.xlsx'(152 용어·관계 23종) → entities.yaml v2(8 분류, 표준명EN/KO·동의어·해석규칙·가격방향·경보기준·status·owner_agent) + ontology.yaml v2(관계 23종 domain/range·DAG 규칙·금지관계). yaml.safe_load 검증 통과 |
| 2026-08-11 | A-088 | Feature | 시맨틱 레이어 6종 체제 완성 — metrics/query_templates v2 + 신규 2종 | metrics.yaml v2: INDICATOR_DEFINITION(방향의미·집계규칙·권장출처) + UNIT_DEFINITION(QUDT-lite 12종). query_templates.yaml v2: 필수슬롯·출력스키마(P1-05 §3)·근거반환 규칙. 신규: provenance.yaml(SourceDocument·EvidenceSpan — page·exact_quote·추출버전 필수) + event_schema.json(MarketEvent·CausalClaim·TradeFlow·Forecast·KoreaImpact, draft 2020-12). C-04/P1-05/P1-06 .md에 ERD §8·§9 평가경계·§11 CQ 반영 |
| 2026-08-11 | A-089 | Data | GAIN DRM 차단 130건 확정 목록화 — 재업로드 필요 | reports/market/drm_blocked_documents.md: Biofuels 58 + Oilseeds 72 = 130건(연도별 2010:72·2020:16·2021:42). FAO·WASDE는 전량 정상. `<DOCUMENT SAFER>` 래퍼 — 사내 DRM 클라이언트로 해제 후 재업로드해야 판독 가능(WASDE A-055 전례) |
| 2026-08-11 | A-090 | Architecture | 비정형 분석 워크플로우 분리 + 신호 시계열화 (WBS 1.1.53) | unstructured_analysis.yml 신규(C-01 결정 — 실행 프로파일·재실행 주기·쓰기권한 격리·Phase B 비용 게이트 4근거): fao/gain 요약 v2 + build_unstructured_timeseries.py(UNSTR_GAIN/FAO_{태그} 월별 문서수 + UNSTR_*_TONE 톤 평균) → main 커밋. FAO 실검증 969행·9지표(2012-09~2026-07). G1 FILE_PATTERNS에 unstructured_signals_historical 등록 → 정형+비정형 통합 분석. historical_backfill gain-pdf 잡은 판독 parquet 전용으로 축소, ingest_gain_pdf.py 구 gain_summaries 이중 생성 제거 |
| 2026-08-11 | R-011 | Report | 라이브러리·프로그램 인벤토리 (조정자 Req) | docs/research_desk/2026-08/library_program_inventory_2026_08_11.md: ①Actions 자율(무료 OSS 20여종) ②API 키만 수동(무료 12종/유료 5종 — TE·Databento·Perplexity·OpenAI·Anthropic) ③완전 수동(DRM 클라이언트·Azure ML·Snowflake·Secrets·XLGantt — 유무료 구분). GATS README 재수출 검증 결과 반영(A-086 판정 표) |
| 2026-08-11 | PM-036 | PM Session | Session 44(계속): 사전집→시맨틱 v2(A-087/088), DRM 130건 목록(A-089), 비정형 워크플로우 분리+시계열화(A-090), 라이브러리 인벤토리(R-011), GATS 검증 리포트 재생성(무역부재 판정 정정) | 다음(조정자): ①DRM 130건 해제 재업로드 ②병합 후 Unstructured Pipeline 실행(target=all) ③databento·customs-gw 재실행 ④8/14~ 통합 관계분석 |
| 2026-08-11 | A-091 | Data | GAIN DRM 130건 해소 완료 — 전수 재검증 통과 | 조정자가 DRM 해제 후 재업로드(dev 브랜치, 29커밋·130파일 바이너리 교체). 검증: ①매직바이트 전수 — GAIN 2,087건·FAO 137건 전량 `%PDF`, 잔여 DRM **0건** ②실판독 — 재업로드 130건 pdfplumber 첫 5쪽 추출 **성공 130/실패 0**, 평균 8,303자. drm_blocked_documents.md를 '해소 완료' 상태로 전면 개정. GAIN 판독 코퍼스 2010~2026 전기간 확보(Biofuels 511·Oilseeds 1,576) |
| 2026-08-11 | A-092 | Data | GAIN 전량 판독 완료 + 파일명 US형식 1건 정정 | DRM 해소 후 전량 재판독: **총 2,090건 → 판독 정상 2,089 · 실패 0 · DRM 0**. 2010~2026 전 연도 공백 없음(국가 99개). 건너뜀 1건 = `12.28_Brazil_Oilseeds and Products Update.pdf` — 원문 `Date: 12/28/2012` 확인 결과 파일명이 **US 형식 MM.DD**로 잘못 부여됨(YY.MM 규칙 위반, 재정리 시 `2012/28/` 폴더 생성). `12.12_...`로 개명 후 `2012/12/`로 이동 → 판독 정상(3쪽·4,872자·압착/재고). 파서에 월>12 경고 메시지 추가(추정 변환 금지 — 규칙 위반은 노출) |
| 2026-08-11 | A-093 | Data | 비정형 신호 시계열 15개년 전 구간 확보 | build_unstructured_timeseries.py 재실행: **2,156행 · 16개 지표 · 2010-01~2026-07**(직전 FAO 단독 969행·9지표 → GAIN 합류로 2배 확대). GAIN 신호 분포(2,089건 기준): 재고 55.3% · 압착 54.0% · 바이오연료수요 40.4% · 기상이변 31.6% · 수입관세 19.7% · 생산변동 18.8% · 수출규제 15.3%. 국가 상위: 인도 222 · 중국 138 · 브라질 117 · 인도네시아 108 · 아르헨티나 103. UNSTR_GAIN_TONE은 요약 인덱스(summarize_pdfs) 필요 → Actions 실행분에서 생성 |
| 2026-08-12 | A-094 | Bug | customs-gw 6시간 초과 — 데이터 크기 아닌 **연결 실패 대기 누적** | 구조: 9품목×10국가×17연도=1,530회, 실패 1건당 최악 254초(4×60s+백오프14s) → 실패율 10%만 돼도 11.3시간. 추가로 호출마다 httpx.Client 신규 생성(핸드셰이크 1,530회 반복·소켓 미해제). 해결: ①API의 strtYymm~endYymm 다월 범위 활용 → (HS·국가)당 1회 = **1,530→90회(17배 감소)**, 실패 시에만 연도 분할 폴백 ②타임아웃 분리(connect 10s/read 30s) ③공유 클라이언트(풀 8) ④CUSTOMS_TIME_BUDGET_S=3h 초과 시 수집분 저장 후 정상 종료 + 잡 timeout-minutes 240 ⑤페이싱 1s. 예상 10분 이내 |
| 2026-08-12 | A-095 | Bug | KOSIS·관세청 ConnectTimeout — 2회 완화 실패로 가설 재수립 | A-083(재시도)·A-085(IPv4 강제) 모두 재발 → 기존 가설 오류 가능성. httpx.ConnectTimeout은 DNS·TCP·TLS를 뭉뚱그려 계층 특정 불가. **유력 가설: A-094의 1,530회 6시간 연속 호출로 data.go.kr이 러너 IP 일시 차단(응답코드 아닌 패킷 드롭=연결 타임아웃)** — 헬스체크가 그 직후 실행됐고 간헐성과도 정합. 해결: scripts/diagnose_kr_hosts.py 신규(DNS/TCP 80·443/TLS/HTTP http·https 분리 측정 + 판정 규칙) → customs-gw 선행 스텝 배치(continue-on-error). 호출량 17배 감축으로 차단 유발 요인 자체 제거 |
| 2026-08-12 | A-096 | Bug | Databento "Response ended prematurely" — 단일 스트림의 전량 유실 구조 | 16년치를 단일 get_range로 요청 → 스트림 중단 시 수신분 전량 폐기. '(degraded)'는 원천 품질 안내이지 차단 사유 아님. T-1 클램프도 추정치라 422 재발 여지. 해결: ①metadata.get_dataset_range()로 실제 가용 종료일 확정(T-1 추정 폐기) ②연 단위 청크 17회 분할 ③청크별 백오프 재시도(5→10→20s)+**부분 성공 저장** ④get_dataset_condition()으로 available/degraded/pending 일수 사전 로깅. data/raw/Databento 미생성 경고는 수집 실패의 결과 — 부분 수집만 돼도 자동 해소 |
| 2026-08-12 | A-097 | Bug | FAO FontBBox 경고 = 무해(추출 손실 0 실증) | pdfminer.six가 임베디드 폰트의 FontBBox(글리프 경계상자) 누락 시 내는 경고. 경계상자는 배치 보정용이라 문자 추출과 무관. 실증: FAO 137건 **전량 판독 성공(실패 0)**, 평균 50,893자(최소 4,655·최대 193,916). 해결: logging.getLogger("pdfminer").setLevel(ERROR)로 노이즈만 제거 — 판독 로직 무변경 |
| 2026-08-12 | A-098 | Bug | USDA PSD 500 진짜 원인 = 헬스체크에 폴백 미적용 | A-076에서 api.fas 우선 폴백을 **커넥터에만** 적용, 헬스체크 스텝은 apps.fas 직접 호출+raise_for_status 유지 → 500 즉시 예외. 즉 서버 문제이자 '이미 아는 이관을 반영 안 한' 문제. 해결: 호스트×인증헤더 조합 순회(api.fas+X-Api-Key → api.fas+API_KEY → apps.fas+API_KEY, ESR 전례 A-019) + 전 후보 실패 시 비치명(수동 업로드 psd_historical 27지표로 커버) |
| 2026-08-12 | A-099 | Bug | USDA ARMS 0건 — 오류 본문을 숨기고 assert 중단 | api.ers 신규 스키마 {status,info,message,data,detail,errors}에서 data가 비면 키 목록만 출력 후 assert로 잡 중단. 정작 원인이 담긴 message·errors를 미출력. 해결: message/errors/detail/info/status 그대로 노출(파라미터 즉시 교정 가능) + 비치명화(대두 생산비용은 G1 핵심 8변수 미포함 보조 지표) |
| 2026-08-12 | A-100 | Bug | CDS "200인데 데이터 없음" = 점검 스텝의 3중 결함 | 조사 결과 해당 스텝은 **수집이 아닌 연결 점검**이라 저장물이 없는 게 정상. 다만 ①스텝명은 ECMWF CDS인데 실제로는 open-meteo 호출 ②키 불요 소스에 CDS 인증 헤더 부착 + ECMWF_API_KEY 미등록 시 점검 스킵 ③200·403·404를 전부 '연결 정상' 출력(본문 미검증) — ③이 혼선의 직접 원인(연결 성공과 데이터 수신 미구분). 해결: 스텝명 'Open-Meteo ERA5 Archive(기후 실수집원 — CDS 아님)'로 정정, CDS 키·헤더 제거, 응답 일자 수·실측 기온 개수 검증해 '200+값있음'과 '200+값없음' 구분 출력. 기후 실수집은 climate_connector(Open-Meteo ERA5-Land 12산지) 담당 |
| 2026-08-12 | C-013 | Architecture | Gemini 헬스체크 스텝 제거 (C-012 이행 완료) | 조정자 지시(성능 급락)로 Gemini 전면 배제했으나 llm_health_check.yml에 점검 스텝과 google-genai 의존성이 잔존. 스텝 삭제 + pip install에서 google-genai 제거. 교차검증은 OpenAI gpt-5.6-sol/luna(config/llm_cross_validation.json) 전담 |
| 2026-08-12 | M-009 | Modeling | 모델 전략 전면 개정 — Champion–Challenger 전환 (조사 패키지 반영) | docs/research_desk/2026-08/model_strategy_2026_08_12/ 5종 반영. **G1**: LASSO→**Elastic Net**+LightGBM/XGBoost+SHAP·Permutation+Granger·**국소투영**, 중요도를 horizon(1·5·20·60일)×레짐×원산지별 산출. **G2**: VMD-LSTM/TFT→**SARIMAX+Quantile LightGBM+EGARCH-X→EnCQR**, 출력을 P10~P90·구간·상승확률·임계초과확률로 계약 변경. **G3**: 레짐 하드라벨→**확률**+G2 분포 결합, D-021로 실제 P&L 대신 market-cost proxy·regret. 딥러닝(GRU/LSTM·N-BEATSx/N-HiTS·TFT·PatchTST·Chronos)은 **Challenger로 강등** — 사전등록 승격규칙 통과 시에만 승격 |
| 2026-08-12 | M-010 | Modeling | **VMD/EMD 기본 구성 제외** + IQR 캡핑 범위 축소 + 24개월 기준 폐기 | ①VMD: 전체 시계열 일괄 분해가 미래 정보를 과거 fold로 유입 → 기본 제외(사용 시 fold 내 one-sided/rolling 재적합 필수). libraries.md vmdpy 주석 처리 ②IQR 캡핑: '적합 전 항상'→**데이터 오류·정상 노이즈에만**. 2020·2022 검증된 충격을 캡핑하면 G2/G3가 꼬리위험을 학습 못함 — 원본·robust·캡핑 3버전 실험 후 사건 검증으로 선택 ③"최소 24개월"(M-004)은 통계 fallback 기준일 뿐 딥러닝 충분조건 아님 → 유효 시계열 길이·독립 충격 수·레짐별 사례 수로 대체 |
| 2026-08-12 | D-023 | Architecture | as-of 시점 정합성을 최우선 하드 제약으로 승격 | `모델의 t일 입력 = available_at ≤ t인 최근값`. 전 피처에 event_time·release_time·available_at·source_vintage 필수 — 없으면 투입 금지. WASDE·PSD는 대상기간이 아닌 **실제 발표일 이후**에만 가시화, 정책은 발표일/시행일 분리, 개정치는 vintage별 적재(덮어쓰기 금지). CLAUDE.md §1·README §4.3·modeling.md 반영. **미충족 상태: data/schemas 8종에 해당 필드 부재 → 최우선 보강 대상** |
| 2026-08-12 | C-014 | Architecture | C-03 스펙 전면 재작성 — G1 전담에서 G1/G2/G3 정량 계층 총괄로 확대 | 구 스펙의 LASSO·Bayesian Inference·TCN-XGBoost·GPR 절대값 0.022·Snowflake 내부데이터를 모두 폐기. 신설: §0 First Principles(데이터 계약 우선), §4 Challenger 승격 규칙(4 horizon 중 3개 개선·coverage 5%p·stress slice·DM 검정), §6 모델 진입 게이트 9종, §7 실험 카드, §9 금지 해석 6종(SHAP≠인과·전망문장 학습 금지·개정치 소급 금지 등). 소유 디렉터리 src/forecasting·src/risk·**src/evaluation** |
| 2026-08-12 | R-012 | Report | 도입 라이브러리·프로그램 통합 가이드(단계별 절차) | docs/research_desk/2026-08/integration_setup_guide_2026_08_12.md. 3등급 분류 — [A]에이전트 자율(lightgbm·arch·mapie·**duckdb**·great_expectations·darts·neuralforecast, 전부 무료 OSS) / [B]키·계정만 수동(**Supabase** 무료티어·**Neo4j AuraDB** 무료티어 — 각 5~6단계 절차 명시) / [C]완전 수동(Azure ML 유료·XLGantt 유료·Baltic 유료견적·DBpia 유료). 도입 순서 판단: **DuckDB(ASOF JOIN) 우선**, Supabase 2~3주, Neo4j는 validated 인과 엣지 100건 이상 축적 후. 미도입 결정: Snowflake 보류(D-021로 목적 소멸)·GNN 보류·Airflow 불필요 |
| 2026-08-12 | R-013 | Report | 문서 정합성 전수 감사 (Req 3) | docs/research_desk/2026-08/doc_consistency_audit_2026_08_12.md. 갱신 13종(CLAUDE·README·modeling·libraries·c03 agent/skill·phase1 스킬 5종·INDEX·헬스체크). 모델 표기 Opus 4.7 잔여 **0건**(L-012·M-006 정합). 중복 판정: inventory↔integration_guide=역할 분담이라 중복 아님, gain_summaries 이중생성만 제거. 사용자 업로드 원문 3종(Enterprise Semantic Architecture·Dual-Agent Guide·C-04 Role Framework)은 **편집 금지·원본 보존**. 남은 리스크 4건 중 **data/schemas as-of 필드 보강이 최우선**(모든 모델의 선행 조건) |
| 2026-08-12 | PM-037 | PM Session | Session 45: 장애 7건 근본원인 해결(A-094~A-100)·Gemini 잔재 제거(C-013)·모델 전략 전면 개정(M-009/M-010/D-023/C-014)·통합 가이드(R-012)·문서 감사(R-013) | 다음: ①data/schemas as-of 필드 보강(최우선) ②customs-gw·databento 재실행 검증 ③G1에 국소투영·permutation·horizon×레짐 분해 구현 ④Supabase 계정(조정자) |
| 2026-08-12 | A-101 | Bug | 관세청 다년 범위 호출 불가 확정 — A-094 최적화 무효 정정 | 실행 결과 `resultCode=99 시작과 종료의 조회기간은 1년이내 기간만 가능합니다`. A-094의 "다년 범위 지원 → 1,530→90회(17배)" 전제가 **틀렸음**(문서 미명시, 실행으로만 확인 가능). 게다가 조용히 실패 — _request()가 resultCode!=00에 []를 반환해 상위가 '데이터 없음'으로 오탐(로그의 'U.S.A: 데이터 없음'이 실제로는 요청 거부). 해결: 연 단위 복귀({YYYY}01~{YYYY}12 고정) + **ThreadPoolExecutor 병렬 5**로 벽시계 흡수(커넥션 풀 12/8). 호출량은 못 줄이므로 동시성이 유일한 수단. 예상 15~25분. **연도 범위 축소는 해결책 아님**(데이터 축소일 뿐 — 2010~2026 유지) |
| 2026-08-12 | A-102 | Bug | Databento price_date 전량 1970-01-01 — 로컬 100% 재현 | 원인: `pd.concat(parts, ignore_index=True)`가 각 청크의 **ts_event DatetimeIndex를 폐기** → 이후 reset_index()가 RangeIndex(0,1,2..)를 'index' 컬럼으로 만들고 pd.to_datetime이 이를 **에폭 나노초**로 해석 → 1970-01-01+n ns. 증상 정합: min·max 모두 1970-01-01인데 nunique=4,051(행 수와 동일), 20,255행=4,051×5지표. **데이터는 정상 수신, 날짜만 파괴**(4,051일은 2010~2026 거래일 4,032과 부합 — 실제 누락 아님). 해결: ①_to_price_date() — concat 이전 청크별 시각 승격, DatetimeIndex 우선·무명도 처리·후보 컬럼 부재 시 즉시 예외(임의 추정 금지) ②_assert_date_range() 범위 검증 실패 시 저장 중단 ③yyyy-mm-dd 저장(조정자 요청) ④연도별 커버리지+200일 미만 경고. 로컬 검증: 1970 오염 0건 |
| 2026-08-12 | A-103 | Bug | PSD TypeError — 세션45가 만든 회귀 | `_get_retry(url, params, timeout, tries)`에 headers 인자가 없는데 A-098이 headers=_h로 호출. 헬퍼가 워크플로우 heredoc에 **9벌 복제**돼 한 곳 수정이 전파되지 않는 구조가 원인. 9곳 전부 headers 파라미터 추가 + c.get에 전달. 부수: 라벨의 url.split('/api')[0]이 '//api'에 먼저 매칭돼 'https:/'로 표기(진단 무영향) |
| 2026-08-12 | A-104 | Bug | BOK ECOS ConnectTimeout 신규 — 한국 호스트 계열 취약성 | KOSIS·관세청 해소 후 BOK에서 동일 증상 발생 = 개별 호스트가 아닌 **계열 공통 문제**. 근본 처방은 재시도 로직 일원화(9벌 복제가 개선 전파를 막음 — A-103과 같은 뿌리). scripts/http_kr.py 신설: 다중 전송경로(IPv4 강제→기본) + **https↔http 스킴 폴백** + 지터 백오프(5→30s+무작위) + 5회 재시도 + 계층 진단 메시지(DNS 성공 여부·시도 패턴). BOK 점검 비치명화 — 원/달러는 FRED DEXKOUS로 대체 가능 |
| 2026-08-12 | A-105 | Bug | TE HTTP 409 — 실패 처리가 스로틀을 자초 | 구 코드: 1차 getMarketsBySymbol 실패 시 **대기 없이 즉시** 2차 getMarketsData(전체 상품 목록, 훨씬 무거움) 호출. 이전 '심볼 재확인 필요' 로그가 이미 2차 경로를 탔다는 증거 — 호출 누적이 구독 한도를 넘겨 409 'Please slow down'으로 전환. 해결: 호출 1회로 축소(2차 제거) + 409/429/slow down/throttle 감지 시 지수 백오프 30/60/120s + 비레이트리밋 오류는 재시도 안 함 + 최종 실패도 비치명(TE_BDI 9개년 수동 업로드로 커버) |
| 2026-08-12 | A-106 | Bug | ARMS recordCount=0 — API 정상, 쿼리 조합 문제 | 응답 {'status':'ok','total':{'recordCount':0}} → 서비스는 정상이고 파라미터 조합에 데이터가 없음(A-103의 TypeError와 원인 다름). 추측 교정 대신 **메타데이터 엔드포인트 자기발견**: /data/arms/{year,state,report,category,farmtype}를 순회해 실제 유효값을 로그 출력 → 다음 실행 로그로 쿼리 확정. 외부 문의 불필요. 비치명 유지(G1 핵심 8변수 미포함) |
| 2026-08-12 | A-107 | Bug | climate 12지역 수집 — 잠재 전면 실패 구조 발견 | 조정자 질문(12개 동시 수집 가능?)에 대한 점검 결과 구조는 가능(지역별 순차+0.3s, 부분 실패 허용)하나 결함 3건: ①**[심각]** Open-Meteo는 daily 미지원 변수가 하나만 섞여도 HTTP 400 → 8변수 일괄 요청이라 **12개 지역 전부** 실패하고 경고만 찍힌 채 잡은 성공 종료(조용한 전면 실패) ②부분 실패 미가시(3/12 성공도 '3개 지역'으로만 표기) ③run(start_year=2017) 하드코딩으로 HISTORICAL_START_YEAR 무시 → 분석창 2010(M-008)과 불일치. 해결: 유효 변수 자동 프로브·확정 + `[집계] 지역 수집 N/12`+지역별 일수 명시 + 2010 기본값. ⚠️샌드박스가 archive-api.open-meteo.com 차단(403)이라 실측 미검증 — Actions 로그로 확인 필요 |
| 2026-08-12 | D-024 | Architecture | 회사 목표 아키텍처 대응 — Nexus 트랙 귀속 2축 분류 | 조정자 공유(11월 CT AWS 통합·Apache Hop ETL 2대·PostgreSQL+ADLS Gen2→**Snowflake EDP 단일화**). 조정자 분류 기준은 *출처* 축과 *저장·서빙* 축이 한 축에 섞여 있어 문자 그대로 적용 시 모순 발생(ADLS 폐지 후 Nexus 착지점 소멸 vs D-021 경계 훼손). **권고: 2축 분류** — ①원천·수집·전처리=외부 트랙(Hop ETL) ②AI 컴퓨트=외부 트랙(Azure ML/MS Foundry, CT의 AWS 통합≠컴퓨트 통합) ③저장·서빙=**공통 EDP(Snowflake `NEXUS_EXT` 전용 DB)**. D-021은 정책이 아닌 **권한 가드레일 3종**으로 강제(물리 분리·내부 스키마 GRANT 미부여·쿼리 이력 감사). 문서: target_architecture_migration_2026_08_12.md |
| 2026-08-12 | D-025 | Architecture | **Supabase·Neo4j 도입 철회 · Snowflake 보류 해제** (R-012 정정) | R-012에서 Supabase 도입(무료 티어)을 권고했으나 회사 아키텍처 확인 후 **철회**. 근거: 회사가 폐지 중인 PostgreSQL을·사외 3rd-party에·이관 3개월 전에 신설하는 **3중 역행**이며 목표 아키텍처 어디에도 없음. 이관 원가 비교 — Supabase 경로 ≈8인일·잔존자산 0 / DuckDB 경로 ≈3인일·parquet 전량 승계(차액 5인일+벤더 심의). Neo4j는 선행조건(validated 엣지 100건) 현재 0건 + 그래프 DB 카테고리 자체 부재로 무기한 보류. **Snowflake는 보류 해제** — 구 근거("내부 웨어하우스 목적 소멸")가 전사 EDP 단일화로 무효화. DuckDB 유지(이식성 100%). Azure ML은 Challenger 단계로 연기(9/10 이전 도입 시 플랫폼·모델 동시 변경으로 성능 교란). **조정자 Supabase 가입 요청 철회** |
| 2026-08-12 | A-108 | Bug | DQSOps 게이트 fail-open — parquet 부재를 '통과'로 처리 | parquet 미발견 시 WARNING으로 조용히 return → 아티팩트 다운로드 실패·경로 오류·선행 잡 실패가 **품질 통과로 위장**. 수정: REJECTED + exit 1(의도적 빈 실행은 DQ_ALLOW_EMPTY=true). 추가로 게이트 결과를 GITHUB_OUTPUT 외에 reports/data_quality/dq_overall_status.txt로도 기록 — Actions 전용 채널 의존을 끊어 Apache Hop 이관 대비 이식성 확보. 로컬 검증: 종료코드 1·상태파일 REJECTED |
| 2026-08-12 | R-014 | Report | 이관 대비 우선순위 — 인프라 구축이 아니라 이식성 | ①data/schemas as-of 컬럼 보강(D-023, Snowflake ODS 설계 전제) ②`git push`를 데이터 저장소로 쓰는 구조 제거(Hop·Snowflake 어디에도 없는 패턴) ③DQ fail-open 수정(A-108 완료) ④아웃바운드 도메인 30여종 목록화(CT 통합 시 방화벽 신청 필수 — 미신청 시 파이프라인 전면 정지) ⑤저장소 1.65GB 비정형을 오브젝트 스토리지로 분리. 마일스톤 충돌: G1 8/31·G2 9/10은 통합 창보다 앞서 **직접 충돌 없음**, 간접 위험 4건(allowlist·시크릿 28종·변경동결·저장소 용량) |
| 2026-08-12 | A-109 | Bug | 관세청 수정 재검증 — 실패 은폐 구조가 남아 있었음(A-101 보완) | 적대적 검증 결과 A-101이 병렬화만 하고 **오탐 버그는 미수정**이었음(주석만 추가, 반환값 그대로). 보완: ①`Outcome` NamedTuple로 **ok/empty/rejected/error 4상태 분리** — `[]`가 '무데이터'와 '요청 거부'를 동시에 뜻하던 구조 제거 ②`_FAILURES` 집계 → 실패 1건이라도 있으면 **종료코드 1**(전량 실패가 초록불+빈 아티팩트로 끝나던 세션45·46 증상 차단) ③`_probe_window_months()` — '1년 이내' 경계가 12개월 inclusive를 거부할 수 있어 12→6→3개월 폭을 선행 프로브로 확정(1,530회 통째 낭비 방지) ④부분 수집 시 기존 xlsx를 `.partial.xlsx`로 분리 저장(17시트→3시트 파괴 방지) ⑤"다음 실행에서 이어서 수집" 문구는 **거짓**이라 정정(재개 상태 미저장 — 매 실행 동일 순서로 소진되어 뒤쪽 품목 영구 누락) |
| 2026-08-12 | A-110 | Bug | climate 수정 재검증 — 조용한 능력 상실·일별 429·ENSO 날짜 오염(A-107 보완) | ①**F3**: `soil_temperature_0_to_7cm`·`soil_moisture_0_to_7cm`는 Open-Meteo archive에서 **hourly 전용**(daily 집계 없음) → A-107의 프로브가 이를 '미지원'으로 조용히 제외해 **대두 작황 설명력 최상위인 토양수분이 상실**될 구조였음. daily 후보에서 사전 제외 + `_fetch_hourly_soil()`로 hourly 수신 후 **일평균 집계**해 복구 ②**F4**: `run()` 기본 2010이 일별 파이프라인에 적용되면 매 실행 12지역×6,000일 재수집 → 무료 티어 시간당 한도 초과(429→`지역 수집 7/12` 매일 반복). `BACKFILL_MODE` 분기 신설 — 비백필은 최근 90일 증분(`CLIMATE_INCREMENTAL_DAYS`) ③**F15**: ENSO ONI가 연 12개 계절값을 **전부 1월 1일**에 기록 → as-of join이 임의 값 선택. `_ONI_SEASON_MONTH`로 계절→중간월 변환(DJF→1월…NDJ→12월) |
| 2026-08-12 | A-111 | Bug | Databento 재검증 — 배타적 end·롤 중복·가격 스케일(A-102 보완) | ①**F7**: `get_range`의 `end`는 **배타적** — `{year}-12-31`이 매년 12/31을 누락(16년×약 11거래일 상실, 커버리지 임계 200일에 안 걸려 영구 미검출). `{year+1}-01-01`로 교정 ②**F8**: `ZL.c.0` 롤 시점에 동일 날짜로 만기·신규 계약 2행 → 무조건 drop_duplicates가 **가격 다른 두 계약 중 하나를 근거 없이 선택**(연 12회×16년≈190회, G2 타깃에 계약 전환 점프 주입). 중복 검출 시 종가 격차 경고 + **거래량 최대 계약 채택**을 명시 규칙화 ③**F11**: 날짜만 검증하고 값은 방치 → SDK 버전에 따라 `_PX_SCALE` 미적용 시 1e9배 오류가 무경고. 종가 5~200 USc/lb 범위 assert 추가 ④**F12**: `_dataset_end` 폴백이 dict 아닌 객체에서 AttributeError→조용히 T-1 회귀(422의 원인 경로). dict·객체 양쪽 처리 + 폴백 사용 시 명시 경고 |
| 2026-08-12 | A-112 | Bug | ARMS·TE 재검증 — '고쳤다'고 기록됐으나 실행 경로는 그대로(A-105/A-106 정정) | ①**F5(ARMS)**: A-106의 '자기발견'이 메타데이터를 **출력만 하고 쿼리에 반영하지 않아** recordCount=0이 100% 재현될 상태였음. `_first()`로 유효값을 선택해 실제 파라미터 구성(미발견 항목은 하드코딩으로 되돌리지 않고 생략). 부가: `r`이 루프 밖 변수라 1번 호스트 500 + 2번 예외 시 500 응답을 물고 나가 `raise_for_status`가 터지며 '비치명' 의도가 무너지던 버그 → 예외 시 `continue`, 200 아니면 일괄 비치명 종료 ②**F6(TE)**: A-105가 **헬스체크만 고치고 `te_connector.py`는 방치** — 심볼 4종 무백오프 연타 후 즉시 전체 상품 목록(실행당 최대 9회 무제어 호출)이 원본 그대로였음. `_te_try()` 헬퍼로 레이트리밋만 지수 백오프(30/60/120s), **스로틀 확정 시 추가 호출 즉시 중단**, `fetch_bdi`·`fetch_cpo` 양쪽 적용 |
| 2026-08-12 | A-113 | Bug | C-08 게이트 잔여 사각지대 — glob 기반이라 파일 없는 커넥터가 조용히 이탈 | A-108로 '0건→REJECTED'는 해소했으나, **일부만 수집된 경우**는 여전히 통과했다(glob은 존재하는 파일만 검증 대상으로 잡으므로 수집 실패한 커넥터는 검증에서 빠짐 = 게이트가 존재 자체를 보증 못함). `EXPECTED_ARTIFACTS` 매니페스트 15종(필수 10·선택 5) 도입 — 필수 산출물 누락 시 REJECTED 행을 추가해 종료코드 1. 로컬 검증: 1종만 존재 시 '필수 9건 누락' 검출 |
| 2026-08-12 | R-015 | Report | 아웃바운드 방화벽 allowlist 작성 (임계경로) | docs/infra/egress_allowlist.yaml 신규 — 커넥터·스크립트·워크플로우 전수 grep으로 **34개 호스트** 추출. 호스트별 용도·커넥터·중요도·유무료 명시. 특이사항: ①`apis.data.go.kr`은 **평문 HTTP 80** — HTTPS 전용 정책 시 예외 승인 필요 ②`stream.aisstream.io`는 **wss(WebSocket)** — HTTP 프록시로 통과 불가(A-042) ③`cds.climate.copernicus.eu`는 미사용 확정(A-100)이라 신청 제외 가능 ④pypi.org·files.pythonhosted.org는 파이프라인 실행 전제라 critical. **제출 목표 8/29** — 미신청 시 CT 통합 즉시 수집 전면 정지(A-069/A-070/A-071이 이미 실증한 패턴) |
| 2026-08-12 | D-026 | Architecture | 이관 쟁점 3건 판정 + Actions 종속 9종 확정 | **J-3**(Azure ML 시점): 정형 parquet 4.3MB로 Champion에 GPU 불요 → 트리거 방식이나, 11월 Actions 정지 시 실행 표면이 필요하므로 **T3=Actions 종료 −30일(10월 초)** 하드 데드라인 신설. **J-4**(이관 착수): *산출 수치를 바꾸는 변경*(모델·컴퓨트·LLM 백엔드)은 9/10까지 동결, *실행 위치·경로 추상화*(egress·as-of·경로 env화)는 즉시 착수 — 둘은 충돌하지 않음. **J-7**(C-08): A-108로 부분 해소, 매니페스트 부재는 A-113으로 마감. Actions 종속 9종(git push 저장소·GITHUB_OUTPUT 게이트·자유 egress·retention 아카이브·STEP_SUMMARY 리포트·RUN_ID 계보·Secrets 단일보관·ubuntu-latest 암묵의존·concurrency 상호배제) **신규 생성 금지**. 이관 수용 기준 R1~R5(스냅샷 불일치 0·피처뷰 <1e-9·지표 <0.5%·F1≥0.85·as-of 위반 0) |
