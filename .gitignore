# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
*.so
.venv/
venv/
env/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Data — API 수집 parquet 파일은 PII·독점 데이터 포함 가능 (CLAUDE.md §data)
data/raw/*.parquet
data/raw/**/*.parquet
# xlsx_exports는 parquet에서 파이프라인이 재생성하는 열람 사본 (아티팩트) — README만 추적
data/raw/xlsx_exports/**/*.xlsx
# 수동 업로드 참조 데이터(Excel·PDF)는 커밋 허용
!data/raw/USDA/**/*.xlsx
!data/raw/FAO/**/*.pdf
!data/raw/**/.gitkeep
!data/raw/**/*.md

# Secrets
.env
*.env.local

# Azure ML / MLflow
mlruns/
mlartifacts/
.azureml/

# macOS
.DS_Store

# IDEs
.vscode/settings.json
.idea/

# Generated reports (pipeline outputs — not committed)
reports/pipeline/*.html
reports/pipeline/*.pdf
reports/pipeline/*.json
reports/data_quality/*.json

# pytest
.pytest_cache/
htmlcov/
.coverage
